# Test Registry

A living map of **feature/surface → what actually covers it → known
gaps**, seeded honestly from the real feature set as of 2026-08-01 (not
an aspirational list) — see `docs/JOURNEY_MANIFEST.md`, `home.html`, and
`netlify/functions/*.js` for the source of truth this was built against.
Gaps are listed on purpose, not papered over.

**Discipline rule (see `AGENT_POLICY.md`'s build/review sections):** a
feature merge must update this file's relevant row(s) — new coverage
added, or what's covered redefined — as part of that same merge. `review`
checks this happened.

Two kinds of automated coverage exist in this repo, and most rows below
cite one or both:

- **`test/*.test.js`** (`npm test`) — mocked backend, runs against a local
  static server. Proves the app's own client/server logic is correct in
  isolation. This is the vast majority of this repo's test coverage.
- **`test/prod-smoke/*.test.js`** (`npm run smoke:prod`) — real backend,
  runs against a real origin (default production). Proves the currently
  *deployed* site actually works end to end for a real new user. Deliberately
  thin — a handful of end-to-end flows, not exhaustive per-feature coverage
  (see that directory's own `README.md`).

A row with only `test/*.test.js` coverage and no prod-smoke row is
**normal, not a gap** — prod-smoke is intentionally narrow. A row with
**no coverage at all** is a real gap, called out as such.

## Auth & accounts

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Signup (`register-account.js`, `js/store.js#signup`) | `test/signup-screen-behavioral.test.js`, `test/funnel-real-chain-behavioral.test.js`, `test/account-store.test.js`, `test/wizard-ui-behavioral.test.js` | — |
| `wizard.html` signup-wall reassurance subtext (`dt_wall_subtext_arm`) — was a live 50/50 A/B, CONCLUDED on 'shown' (founder 2026-08-11, commit `82e6aaf`: "always show wall reassurance" — Facebook Login's removal left the 'hidden' arm looking empty). `assignWallSubtextArm()` now returns the fixed `'shown'` default for any unforced visitor and does not persist it to `localStorage` (nothing left to persist — only the `?wallsubtext=shown\|hidden` founder/QA override path still forces+persists an arm). Test-registry note only, not a behavior change: `test/wizard-ui-behavioral.test.js`'s no-override assertion had gone stale after the conclusion (still expecting a persisted random 50/50) and was corrected to match the shipped, always-'shown' default — tracker item `for-product-auto-pre-existing-test-failu-ts0vyd` | `test/wizard-ui-behavioral.test.js` (the dedicated "wall_subtext_arm A/B" test: both forced arms still render/persist correctly via `?wallsubtext=`, and the unforced default now asserts the fixed 'shown' line renders with no localStorage write) | — |
| Screen 13 layout — vertical distribution across the real viewport (`.fn-signup-stage`/`.fn-stage-spacer`, `fnStageHtml`) (tracker item `for-product-signup-screen-layout-fixes-f-3hclmj`, founder mobile walk 2026-08-03 — "SPREAD IT OUT"; **founder-approved and merged 2026-08-04**). Covers all three screen-13 arms' email steps (`unified`, `reveal`, passwordless) plus the passwordless "code" step (the "passwordless-code wall" the item calls out directly). (The "or" divider this row also used to cover was part of the Facebook Login button, since removed — 2026-08-11.) | `test/signup-screen-layout-spacing-behavioral.test.js` (`.fn-signup-stage` present with real, non-collapsed distribution spacers and the visible content spanning a majority of the viewport on all 3 arms' email steps plus the passwordless code step) | — |
| Screen 13 'reveal' challenger — forming veil, "Send me my dream" copy, save-framing reassurance, graceful preview degradation (tracker item `for-product-build-now-email-wall-fix-1-n-6qvebd`, mock-only — `SIGNUP_REVEAL_CHALLENGER_LIVE` is `false`) | `test/signup-reveal-variant-behavioral.test.js` | Not yet live in the real A/B (founder must approve the mock — `reveal-variant-mock-x7q4.html` — first); no PostHog experiment-readout coverage since nothing is actually splitting real traffic yet |
| Forming veil's animated nebula/aurora background — replaces the old static flat-gradient `.fn-forming-frame` fill with a CSS-only, style-tinted (`fnstyle-cartoon`/`cinematic`/`anime`/`realistic`/`default` via `formingTintClass`), continuously-drifting `.fn-form-nebula` layer (tracker item `for-product-forming-veil-replace-the-fla-nwbe60`, founder feedback: "something more interesting and moving there than just a square color"; **founder-approved (the animated nebula "After" version) and merged 2026-08-04**, along with his verbatim reassurance-copy change on the same screen — the non-record-mode line above the email field now reads "No need to wait — we will send your dream once it's ready to your email:", replacing the old "Your dream is saving to your email... it'll be waiting..." copy) | `test/forming-veil-nebula-behavioral.test.js` (`formingTintClass` pure-logic coverage incl. fail-closed default, the new `.fn-form-nebula` structure replacing the flat background with no video/GIF asset requested, four distinct per-style resolved colors + default fallback with no style chosen, real computed-style change over real wall-clock time proving the keyframes actually run — not just declared — `prefers-reduced-motion` disabling it, and that the pre-existing best-effort real-still-frame swap-in via `tryShowFormingPreview`/`video-status.js` is unaffected), `test/signup-reveal-variant-behavioral.test.js` (`.fn-forming-frame`/`.fn-form-caption`/`.fn-forming-still` coverage still passes against the new markup; updated to assert the new reassurance copy verbatim) | Only applies to the reveal arm today (`formingFrameHtml` isn't called anywhere else) — `home.html`'s separate `.day0card` forming veil is a different, unrelated implementation and out of scope here |
| Passwordless signup — email-only entry, no password ever (`register-account-passwordless.js`, `js/store.js#signupPasswordless`, `start.html`'s `renderScreen13Passwordless`, tracker item `for-product-build-passwordless-signup-fo-at2fko`, founder-decided hybrid, mock-only — `SIGNUP_PASSWORDLESS_LIVE` is `false`) | `test/passwordless-signup.test.js` (create-vs-resolve identity resolution, offline-fallback local commit shape, rate limiting), `test/signup-passwordless-behavioral.test.js` (real Chromium run: no password field ever rendered, email-only submit, reaches screen 14, CompleteRegistration fires exactly once at the same depth via the real `track-conversion.js` call — see `docs/EVENT_TAXONOMY.md` — local state has `password:null`/`emailVerified:false`, the deferred-sheet session marker is pre-set, and undeliverable-email validation) | Not yet live for real traffic (feature-flagged off, same posture as the 'reveal' challenger above — no mock-review artifact exists for this arm yet, unlike 'reveal's `reveal-variant-mock-x7q4.html`, since the founder's own spec (his exact words, tracker item body) already fully specified the UI shape rather than leaving it to a design pass); no `test/prod-smoke/` coverage (payments/signup-variant paths are out of that suite's intentionally narrow scope, same as every other signup arm) |
| **SECURITY FIX, 2026-08-02** (round-2 independent review finding, real, fixed same day): the RESOLVE branch (an already-registered email POSTed to `register-account-passwordless.js`) used to mint a fully usable `authToken` with ZERO proof the caller controlled that inbox — a one-request account takeover of any existing account, given only its email address. Fixed: that branch now sends a fresh code/link and returns NO authToken/username at all (`pendingVerification:true`); a real session is only ever granted via the NEW `login-with-email-code.js` endpoint (a genuine login, mailed-code-as-credential) or a clicked `verify-email-link.js` link (which now also mints a `lib/session-transfer-token.js` token, the same session-transfer mechanism `create-session-transfer.js` uses, redirecting to `/home.html` — the one page that already consumes `?bt=` — instead of the old default `/profile.html`, which never did). `start.html`'s passwordless arm gained a new inline code-entry step (`renderCodeStep`) for this branch. See `netlify/functions/register-account-passwordless.js`'s own header comment for the full incident writeup. | `test/passwordless-signup.test.js` (the exact attacker scenario: a caller who is not the account's owner gets no authToken/username, the victim's own original token is completely undisturbed; `login-with-email-code.js`'s full error surface incl. no-enumeration between "wrong code" and "no such account"; `verify-email-link.js`'s `bt` token is real and independently consumable via `verify-session-transfer.js`), `test/signup-passwordless-behavioral.test.js` (real Chromium: an already-registered email shows the code step and leaves the browser genuinely signed OUT, a correct code completes login WITHOUT firing CompleteRegistration, a wrong code shows an inline error and never advances) | — |
| Deferred email verification — 6-digit code (`verify-email-code.js`, `resend-verification-code.js`, `login-with-email-code.js` for the security-fix login-via-code path above), implicit verification via clicked email link (`verify-email-link.js`), the "next visit" deferred-prompt sheet (`js/email-verify-sheet.js`, wired on `home.html`), an on-demand entry point in `profile.html`'s Settings (tracker item `for-product-no-on-demand-way-to-open-ema-ebj04z`, founder repro 08-07: he dismissed/missed the "next visit" prompt and had no other way to reopen it short of triggering another purchase — a "Verify your email" row, visible only while `DreamStore.getAccountEmailVerified()` is false, opens the same sheet with `{source:'settings'}` and hides itself immediately on a same-session verification, no page reload needed), `lib/email-verification-store.js` | `test/passwordless-signup.test.js` (code correctness incl. attempt-limiting, authToken-only identity for `verify-email-code.js` — never a client-claimed username, link-token single-use, open-redirect guard on `verify-email-link.js`'s `redirect` param, resend no-op-if-already-verified), `test/email-verify-sheet-behavioral.test.js` (real Chromium run against home.html: the sheet auto-opens for an unverified account on a fresh visit, a correct code marks it verified and shows the confirmation view, a wrong code shows an inline error without closing, "Resend code" calls the real endpoint, a verified/password-based account is never shown the prompt, and the session-already-offered marker correctly suppresses a repeat show), `test/verify-email-settings-row-behavioral.test.js` (real Chromium run against profile.html: the row shows only for a signed-in, unverified account and never for a signed-out visitor or an already-verified/password-default account, clicking it opens the real sheet with the settings source tag, a successful in-session verification hides the row immediately without closing Settings, dismissing via "Not now" leaves it visible, and a verification reached through a different entry point still reflects correctly on next Settings open), `test/email-verification-send-cooldown.test.js` (fix, tracker item `for-product-bug-founder-repro-08-09-veri-g71t7u`, founder repro'd: a signup send followed seconds later by `js/email-verify-sheet.js`'s `autoSendOnOpen` — e.g. `shop.html`'s automatic `post_purchase` trigger firing shortly after signup — used to always mail a genuinely fresh code with no server-side cooldown. `lib/email-verification-store.js#createVerification` now takes an opt-in `opts.respectCooldown`, tracks `lastSentAt`, and returns `{reused:true}` — no new mint, no second email — when called again within 60s of the last send while a still-valid code exists; `lib/verification-email-sender.js` skips the Resend call on `reused` while still reporting `ok:true`. **WARM WELCOME (founder-approved 2026-08-12):** this same transactional email now ALSO carries a short warm product-intro welcome block below the code/link ("Welcome to DreamTube 🌙" + what it is: turn your dreams into short AI videos and see what they might mean + a gentle nudge to describe a first dream) — added to `buildHtml`, plain text (no competing CTA button), kept LIGHT so it stays a code-email-first (deliverability). The 6-digit code + verify link remain the most prominent thing at the TOP, and the code is still the first content (the code-extraction the other tests rely on is unaffected — the welcome copy adds no digits); covered by `test/verification-email-welcome.test.js` (code still present + still in the prominent 32px style + still first; verify link + "already signed in" line preserved; welcome greeting/what-it-is/nudge present and positioned AFTER the code). **Opt-in, not uniform** — a ROUND-1 version of this fix applied the cooldown unconditionally and was caught by running the full suite before merging: it broke `register-account-passwordless.js`'s RESOLVE branch (a login mechanism that must mail a fresh code on every submission — it's the account's only way back in, not a bonus) and the pre-existing "rolling window" tests below (repeated `resend-verification-code.js` calls asserting each mints a genuinely distinct code). The cooldown is now threaded ONLY from `autoSendOnOpen` via an `auto:true` request field — `resend-verification-code.js` → `sendVerificationEmail`'s `opts.auto` → `createVerification`'s `opts.respectCooldown`; every other caller (signup's own first send, a manual "Resend" tap, the RESOLVE branch) is completely unaffected. Covers reuse-within-cooldown, a genuine fresh send once the cooldown elapses, a legacy pre-fix record with no `lastSentAt` never misread as recently-sent, the no-opt-in default always sending fresh, the two real HTTP endpoints chained end-to-end with `auto:true` proving exactly one real Resend call, and a regression-guard test proving a manual resend (no `auto`) is never throttled even seconds after signup) | The "before publish"/"before purchase" DEFERRED-PROMPT triggers (as opposed to the server-side GATE, which both enforce — see the gate-list row below) are not wired to interactively open `js/email-verify-sheet.js` in this pass — only the "next visit" and Settings on-demand triggers are; publish is fire-and-forget client-side (see `js/store.js`'s `syncPublishedDreamToFeed`) so has no reaction point today. `shop.html`'s E10 branch DOES open the sheet reactively (see that file's `purchasePack`), but has no dedicated behavioral/Playwright test of its own yet — only the server-side E10 rejection is covered above. |
| Gate list — accounts with `emailVerified:false` are blocked from purchasing (`create-checkout-session-dodo.js`, E10) and publishing to the shared feed (`publish-dream.js`, E8); every pre-existing account-creation path (`register-account.js`) is unaffected/still defaults to verified — see `lib/account-store.js`'s own GATE LIST header comment for the authoritative, exhaustive list | `test/passwordless-signup.test.js` | — |
| Bounce handling — Resend `email.bounced` webhook, hard (Permanent) bounces only, flags `emailBounced` (`resend-bounce-webhook.js`, `lib/resend-webhook-verify.js`) | `test/resend-bounce-webhook.test.js` (real, locally-computed Svix/HMAC-SHA256 signatures — the actual algorithm, not a stubbed-out check) | **Cannot be exercised end-to-end against real Resend** — no live Resend account/webhook subscription in this sandbox; someone with Resend dashboard access still needs to register this endpoint's URL as a webhook (subscribed to `email.bounced`) and set `RESEND_WEBHOOK_SECRET` in Netlify before it ever receives a real delivery — same "can't do this from the sandbox" class as Dodo/Netlify-env-var setup elsewhere in this registry. `emailBounced` is also currently informational only — nothing in this codebase yet refuses an action off it. |
| Login (`account-login.js`) | `test/account-store.test.js`, `test/rename-migration-login-behavioral.test.js`, `test/password-reset-account.test.js` | — |
| Password reset (`request-password-reset.js`, `verify-password-reset.js`) | `test/password-reset-account.test.js` | — |
| Account deletion (`delete-account.js`) | `test/delete-account.test.js`, `test/delete-account-behavioral.test.js` (now covers step 7's `block-store.js` cleanup — a freed username re-registered by a new account does not inherit the old holder's block list) | `test/prod-smoke/session.test.js` (real delete, used for its own probe-account cleanup — proves the endpoint works against real prod, not a dedicated feature test) |
| Auth tokens (`lib/account-auth-token.js`) | `test/account-auth-token.test.js` | — |
| Cross-device session transfer (`create-session-transfer.js`, `verify-session-transfer.js`) | `test/session-transfer.test.js`, `test/session-transfer-behavioral.test.js` | — |
| Account rename/consolidation (one-off admin tools) | `test/admin-diagnose-rename-conflict.test.js`, `test/consolidate-accounts-client-migration-behavioral.test.js`, `test/admin-restore-founder-email.test.js` | These are one-off hardcoded tools per `AGENT_POLICY.md`'s "remove one-off admin panels after use" policy — coverage is expected to be retired alongside the tool itself, not maintained indefinitely. |

## Onboarding funnel (`docs/JOURNEY_MANIFEST.md`)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Journey sequence itself (both journeys' documented screen order) | `test/journey-manifest.test.js` (source-level, pins `SCREEN_RENDERERS`) | — |
| Journey 1 entry render, live on a real deployed origin | `test/prod-smoke/journey1-funnel-render.test.js` | Only checks screen 13 renders — doesn't complete a second full signup (see that file's own header comment for why) |
| Journey 1 full flow (`start.html`) | `test/funnel-signup-navigation-token-guard-behavioral.test.js`, `test/funnel-meta-attribution-cookies-behavioral.test.js` | — |
| **Unify-all-creation-flows — wizard.html retirement** (founder 2026-08-14; SCOPE TRIMMED same day: KEEP logged-in creation on `create.html`, ship ONLY the wizard.html retirement + the someone-photo lock). Organic + ads enter the `/go/` funnel (`index.html`'s "Get Started" already → `/go/?utm_source=organic` on `main`); logged-in creation stays on **`create.html`** (UNCHANGED — it already uses the shared content classifier). `wizard.html`'s own chip-build creation UI (the six-tile chooser + Subject/Action/Style/FreeText/Recap steps) is **retired to a funnel-arrival receiver**: an ANONYMOUS bare/direct hit (no `?resume=1`) **redirects to `/go/?utm_source=organic`**; a SIGNED-IN bare hit goes to **`create.html`** (the logged-in creator, unchanged). Either way no real user sees the chooser/chip steps again. The ONLY live path through the page is the growth-funnel handoff (`?resume=1&caption=...`), which lands DIRECTLY on the signup wall with its pre-email content gate, PostHog identity stitch, Meta cookie persistence, and funnel-character-stash adoption intact; the signed-in funnel-arrival shortcut (skip wall → `home.html?generate=1`) is preserved. NO logged-in "create"/"new dream" CTA was repointed — the shared bottom-nav "+" (`js/bottom-nav.js` `#nav-create`), the Home Tonight CTA / chamber "Begin" / day-0 "Make another" (`home.html`), `result.html`'s "Make another", and `watch.html`'s public "Make another" all keep their original `create.html`/share targets. The `wizard.html` step renderers are RETAINED-BUT-DEAD markup so `SCREEN_RENDERERS`' positional indices (and the wall's own `wizard_step_viewed {step:7}` analytics + progress dots + Back nav) stay byte-stable. | `test/route-organic-to-wizard-behavioral.test.js` (anonymous bare `wizard.html` → `/go/?utm_source=organic`, chooser never painted), `test/life-origin-generate-handoff-behavioral.test.js` (first-time-visitor bare hit → funnel + signed-in-funnel-arrival + returning-visitor→index.html guard all re-asserted), `test/wizard-funnel-arrival-ree-behavioral.test.js` (organic bare hit redirects, fires no REE), `test/wizard-ui-behavioral.test.js` (the wall/code-step tests, re-plumbed to the funnel-arrival path), `test/journey-manifest.test.js` (`SCREEN_RENDERERS` still pinned; retained-but-dead note) | `test/prod-smoke/session.test.js`'s bottom-dock walk (`#nav-create` → `create.html`) is CORRECT again (logged-in stays on `create.html`) — left unchanged. Funnel-side handling for a signed-in user is not needed here since logged-in creation never routes to `/go/` in this trimmed scope. |
| Journey 2 full flow — RETIRED to the funnel-arrival wall (see the unify-all-creation-flows row above). `wizard.html`'s signup wall, code step, content gate, and stale-attempt token guards (the live surface) | `test/wizard-ui-behavioral.test.js` (wall/code-step tests re-plumbed to the funnel-arrival path; the 3 stale-attempt token guards re-expressed via a fresh-submit supersede, since the funnel-arrival wall's Back leaves the page), `test/wizard-chips.test.js` (chip mapping data), `test/wizard-action-chip-curation-behavioral.test.js` (RETARGETED to `create.html`'s live Build Action step — same curation UI), `test/record-mode-behavioral.test.js` | — |
| Subject step multi-select ("Who's the dream about?") — **RETIRED as a UI surface with the wizard chip flow (unify-all-creation-flows, founder 2026-08-14).** Multi-select Subject lived ONLY in `wizard.html`'s now-retired chip-build flow (a bare hit redirects to `/go/`); `create.html`'s Build subject is deliberately single-select, and the funnel-arrival wall takes its characters from the funnel stash. The underlying `subjects`-array mapping logic in `js/wizard-chips.js` is unchanged and still unit-covered. (Original tracker item `for-product-wizard-characters-step-is-si-paxp07`, founder repro 2026-08-02.) | `test/wizard-chips.test.js` (unit-level `subjects`-array coverage: single-entry array matches the singular path byte-for-byte, the founder's own 3-subject worked example, a photo character combined with described characters, 2-subject "and" joining, empty-array fallback, `buildDeterministicStory`'s "with X, Y and Z" joining incl. the "me" + one-other "with" upgrade). (The wizard-UI multi-select behavioral tests + the `sheet-dismiss` section-7c walk were removed with the retired chip flow.) | UI surface retired — no live multi-select Subject to exercise end-to-end |
| **Layout-B wizard redesign** (tracker item `for-product-founder-picked-layout-b-flo--lks7mj`, founder "go live" 2026-08-07 after seven mock rounds on the since-deleted `mock-wizard-b-x7q4.html`) — `wizard.html`'s full pill-row/emoji-chip/progress-dot skin plus its two founder-directed interaction changes: (1) single-select steps (Setting's place pick, Action, Mood, Style — Style per mock round 4) AUTO-advance ~260ms after a tap, with "+ Something else" write-ins, Day/Night, POV, and the "+N more" expander all exempt, and a Continue tap inside the window advancing exactly once; (2) the Subject step's Me row stages+selects immediately AND — since **founder round 8** ("opens nicely for Someone i know but not for Me and it should including the option to upload image") — opens the character sheet in self mode (no name field, describe + photo upload; Cancel exits cleanly with Me still selected and no details, per the 08-04 details-optional ruling, tracker items `for-growth-founder-ruling-08-04-me-someo-w8k8xr`/`for-product-paired-with-growth-funnel-pr-22o77k`; the round-7 inline Me detail input was removed with it — "Someone I know" rows keep theirs; typed/sheet details flush onto the real Me character at signup, never re-asked; a detail-less Me never blocks and never creates a half-empty store character). **Round 8 also**: (a) the editable recap is its OWN step before the wall again (`renderRecap`, "Here's your dream — make it yours", pencil affordance, "Looks good →"; the wall dropped its folded-in copy) — it fires the sanctioned `wizard_recap_viewed {}` instead of a renumbered `wizard_step_viewed` (see `wizard.html`'s `STEP_ANALYTICS`: Subject..FreeText stay 1-6, the wall stays 7, byte-stable); (b) fresh entries (`?entry=index`/direct — never FB return legs or `?resume=1`) meet the mock's screen-1 entry chooser (build/write/speak) as a pre-step outside `SCREEN_RENDERERS`, firing `wizard_entry_mode_chosen {mode, surface:'wizard'}` only — Build → normal flow, Write → jumps to the free-text step with the typed dream going through VERBATIM as the caption (create.html Write-it's contract, no chip-default 'flying' in a written prompt), Speak → `create.html?record=1` via its login gate (no new pre-signup recording surface; hidden in FB/IG webviews). `create.html`'s Build/Write/Record chooser wears the same pill grammar (flash-then-advance on real taps; Record stays synchronous for the getUserMedia gesture). `SCREEN_RENDERERS` re-pinned with `renderRecap` by `test/journey-manifest.test.js` + `docs/JOURNEY_MANIFEST.md` (updated same-commit per that test's contract). One sanctioned analytics ADDITION rode this branch (founder ask 2026-08-07, via Manager): `wizard_entry_mode_chosen` `{ mode: 'build'|'write'|'record', surface: 'create' }`, fired once per committed tap in each of create.html's three chooser handlers (deep-link paths reusing those handlers fire it too; the one direct `startRecordingUI()` path that bypasses `#choice-record`'s handler is deliberately untracked) — measures the chooser %-split and per-mode signup rate | `test/wizard-ui-behavioral.test.js` (the dedicated Layout-B + round-8 sections: auto-advance + no-double-advance; Me tap opens the self sheet — no name field, photo option present, old inline input gone — with the sheet description traced to caption + store flush; Cancel-keeps-Me-selected end-to-end; the recap STEP's editability — pencil affordance, hand-edit beating the late LLM rewrite, surviving Back-from-the-wall, riding the submission as storyText; the entry chooser — shown fresh with the chooser dot active and zero `wizard_step_viewed`, Build firing `wizard_entry_mode_chosen {surface:'wizard'}` once then Subject's usual step:1, Write-mode verbatim-caption trace, Speak routing + IG-webview hiding, and no chooser on `?resume=1`/FB return legs; plus every pre-existing wall/flow test re-walked under the new grammar), `test/wizard-action-chip-curation-behavioral.test.js` (curation re-proven under auto-advance, incl. the write-in row NOT auto-advancing), `test/split-prompttext-storytext-behavioral.test.js`/`test/funnel-real-chain-behavioral.test.js`/`test/sheet-dismiss-behavioral.test.js` (updated walks; NOTE: the wizard chip-build flow these once walked is retired — see the unify-all-creation-flows row above; `test/wizard-photo-upload-sheet-token-behavioral.test.js` was DELETED, its char-sheet token guard now covered on the live `create.html`/`profile.html`/`result.html` sheets by `test/avatar-describe-behavioral.test.js`/`test/result-photo-upload-sheet-token-behavioral.test.js`), `test/home-behavioral.test.js`/`test/create-record-inapp-webview-guard-behavioral.test.js` (chooser routing unchanged under the pill restyle), `test/wizard-ui-behavioral.test.js` (entry-chooser analytics: `wizard_entry_mode_chosen` fires exactly once per Build/Write/Record tap with the right `mode`/`surface`, via the PostHog stub-queue read) | Real-mode mobile walkthrough on the deployed branch preview still pending before founder-ready (AGENT_POLICY REAL-MODE gate); visual verification so far is real-Chromium mobile-viewport screenshots against a local serve |
| `create.html`'s own "Build it" chip flow (`finishBuildWizard`) aligned to the 08-07 founder ruling that already shipped in `wizard.html`'s Build mode (tracker item `for-product-align-create-html-build-it-w-d8k5g5`) — typed "Anything to add?" free text now JOINS the chip-assembled `storyText` (`WizardChips.buildDeterministicStory(chipFields)` + the free text appended, same shape as `wizard.html`'s `computeStoryText`) instead of the older, now-superseded amendment where free text REPLACED the chip story byte-for-byte. `needsStoryRewrite` is now always `true` in Build mode (was gated on "no free text" under the old replace contract) so `style.html`'s own opportunistic `rewrite-dream-story.js` upgrade always attempts to blend chip context + the user's words, exactly like `wizard.html`'s own always-on rewrite call. `promptText`/`assembled.caption` (the generation input) is unaffected — it already carried the free text appended before this fix. Write-it's own verbatim contract (`writeContinue`) is completely untouched | `test/split-prompttext-storytext-behavioral.test.js` (its own "section 2" test rewritten to the new join contract: the deterministic template shows the chip story + free text joined immediately, before the rewrite lands; the rewrite now fires — asserted via `rewriteCalls.length === 1`, where the old test asserted exactly 0; the rewrite's own `promptText` input carries both chip context and the free text; the final saved dream's `storyText`/`caption` settle on the LLM-blended result once it resolves; `promptText` stays the distinct engineered string throughout — mirrors `wizard.html`'s own already-updated "chips WITH free text" test in the same file) | — |
| **Round-9 wizard bug fixes** (founder "find and fix" after the 08-07 live-main REAL-MODE bug hunt) — five fixes: **(1+2)** the recap's story pin is SIGNATURE-based (`wizard.html`'s `storyContentSignature` — chips/characters projected to story-relevant fields, style+POV, free text, entryMode): a settled story (LLM-upgraded OR hand-edited) sticks across any navigation whose content signature is unchanged (wall→Back no longer flashes the raw template or fires a duplicate `rewrite-dream-story` call — the double-LLM-spend half of bug #1 — and an accidental Back→Forward no longer wipes a hand-edit, bug #2), releasing only on a real content change; round 8's blunt "leaving the recap backwards clears the pin" rule is gone. **(3)** the deterministic chips+freetext join is sentence-cased via the new shared `WizardChips.joinStorySentences` (terminal punctuation, capitalized addition, final period — one implementation for `wizard.html` AND `create.html`'s Build-it, same never-drift charter as the chip tables; this joined text is what ships as storyText whenever the rewrite is slow/fails). **(4)** `login.html` renders record-intent arrivals (`?next=/create.html?record=1`, no local account history) SIGNUP-FIRST with recording-framed copy ("Sign up to record your dream"), login one toggle away — presentation only, the existing `isSignup` mode/fields/DreamStore calls untouched; devices WITH account history and plain visits keep "Welcome back". **(6)** the wall renders with NO auto-focused email input (founder iPhone screenshot: render-time focus made iOS open the keyboard + AutoFill bar and scroll the forming veil out of view) — the FB needs-email half-step's render-time focus removed under the same rule, while every remaining `focus()` in `wizard.html` responds to an explicit user action (write-in taps, the code step's post-submit field, and "Use a different email", which now explicitly re-focuses after re-render); recap/free-text steps verified never-focusing and pinned by test. **(5)** `netlify.toml` disables Netlify's Pretty-URLs HTML post-processing (`[build.processing.html] pretty_urls=false`) so deployed HTML is byte-identical to the repo (the transform was silently rewriting shipped links, e.g. `wizard.html?entry=index` → `/wizard?entry=index` — deployed bytes hash-diffed against repo during the hunt); extensionless URLs still serve via Netlify's normal routing, and `sw.js`'s networkFirst carries a note on why the `/wizard` vs `/wizard.html` cache-key split is an offline-only footnote | `test/wizard-ui-behavioral.test.js` (round-9 section: wall→Back repaints the settled LLM story instantly with NO second rewrite call while a real mood change recomputes + fires exactly one fresh rewrite; hand-edit surviving a no-change Back→Forward and releasing on a real change; the sentence-cased join end-to-end into `payload.storyText` with the rewrite failing; the pre-existing 08-07 join test updated to the cased contract; fix #6 — wall and recap render with `document.activeElement` NOT on their inputs, "Use a different email" still focuses), `test/wizard-chips.test.js` (`joinStorySentences` unit table: casing, terminal punctuation incl. ellipsis, trimming, empty base/addition), `test/record-mode-behavioral.test.js` (record-intent login page signup-first for a fresh browser + toggle to login + history-holding device and plain visits unchanged; the pre-existing #37 round-trip test doubles as proof the auth flow is untouched), `test/split-prompttext-storytext-behavioral.test.js` (create.html Build-it join under the shared helper, unchanged expectations — its fixture text was already proper-cased) | Pretty-URLs disable takes effect on the NEXT production deploy — verify post-deploy that live index.html's Get Started href is byte-identical to the repo's (`wizard.html?entry=index`) |
| **Ad-funnel REE instrumentation on `wizard.html`'s wall** (tracking regression fix, 2026-08-11) — when the growth funnel pinned its `flo` arm to 100% (2026-08-10), every caption-carrying (Build/Write) PAID handoff began landing on `wizard.html`'s signup wall instead of `start.html`'s screen 13, and the wall — built for organic traffic only — fired none of the ad funnel's own `funnel_step_viewed(screen:'email_capture')` (the REE metric), `signup_email_entered`, `signup_email_first_variant_shown`, or the `ReachedEmailEntry` Meta CAPI conversion, so all four read ZERO on the first full day at 100% flo (while `signed_up` kept firing from `js/store.js`). Fix (`wizard.html`): `fireFunnelWallReachedEvents()` fires those events — SAME names, `start.html`-identical property shapes (REE `{step:1, screen:'email_capture'}`), `variant:'wizard'` — from `render()` at `WALL_STEP`, and `maybeTrackFunnelEmailEntered()` fires `signup_email_entered` on the first non-empty email blur/submit; ALL gated on `isFunnelHandoffArrival` (`?resume=1` + a `caption`), so organic wizard traffic still fires none of them and the paid REE metric is never polluted. No UX/flow change; no new event names. | `test/wizard-funnel-arrival-ree-behavioral.test.js` (funnel arrival: REE + first-variant fire once each at the wall with the start.html-identical shapes, `ReachedEmailEntry` CAPI POSTed once, `signup_email_entered` fires once on first email blur and not on a second blur; organic `?entry=index` fires none of the four) | Same sandbox limit as every browser-driven row — verified against a local static serve with a `window.posthog` stub + intercepted `track-conversion`, not a real PostHog/Meta endpoint; the real cutover (that live PostHog REE stops reading 0) is confirmable only post-deploy against production traffic |
| **Pre-signup content gate** (founder-directed 2026-08-14) — the explicit-content block used to fire ONLY at generation time, i.e. AFTER email/signup, so an explicit-content seeker fired the `CompleteRegistration` conversion (the metric the Meta campaign optimizes on) BEFORE being blocked, teaching Meta to acquire more of them. New endpoint `netlify/functions/check-dream-content.js` (POST `{caption, characters?}` → `{allowed, tier}`; NO auth/tokens/generation/side-effects; per-IP daily rate-limit on its own `content-gate-ip` scope) runs the SAME decision `evaluateGenerationGate` uses by delegating to `lib/content-classifier.js` (keyword fast-path + LLM tier + the named-other-person-photo threshold drop) — so the pre-signup verdict can never drift from the generation-time one. Fail-safe mirrors the classifier: FAILS OPEN (`allowed:true`) for the normal case on any classifier/transport/rate-limit error (the generation-time gate is the full backstop), FAILS CLOSED only for the named-other-person-photo (anti-NCII) case. Gate is wired into all three dream-creation surfaces BEFORE the email/conversion step: `wizard.html` (the editable recap step's "Looks good →" via `gateRecapThenAdvance` for the organic flow — an explicit dream keeps the visitor on the editable recap with an inline revise message and never reaches the wall; PLUS the funnel-arrival wall submit, gated only on `funnelArrival` since organic was already checked one step back — so the organic wall submit's own delicate submit/Back timing is untouched); `start.html` (the single `renderScreen13` dispatch chokepoint — blocks BEFORE the signup screen renders; `ReachedEmailEntry` moved out of `render()` into the new `dispatchScreen13` so a blocked visitor never fires that Meta signal either; start.html can't edit the handed-off caption in place, so the block screen offers Back-to-funnel, and the growth-repo funnel owns its own matching gate + the primary revise UX); `create.html` (Build/`finishBuildWizard`, Write/`writeContinue`, and transcript/`transcriptContinue` exits, before committing the draft + navigating to style.html → generation — user is already signed in here, so it's a pre-generation UX gate, not a conversion gate). Telemetry: `content_gate_blocked_preemail {tier, surface:'wizard'|'start'|'create'}` on every block. Does NOT modify `content-classifier.js`/`generate-video.js`/`generate-image.js` (reused read-only) | `test/check-dream-content.test.js` (endpoint unit: method/JSON guards fail-open; explicit→blocked, clean/romantic→allowed via `CONTENT_CLASSIFIER_MOCK_TIER`; named-other-person-photo + romantic→blocked, self-photo + romantic→allowed; the deterministic explicit-keyword backstop blocking with no `FAL_KEY` and no classifier call; classifier-error fail-OPEN normal / fail-CLOSED named-photo via a real error stub; missing-`FAL_KEY` fail-open; rate-limit-exceeded fail-open normal while a named-photo payload is STILL evaluated; empty caption safe), `test/presignup-content-gate-behavioral.test.js` (real Chromium, `wizard.html` FUNNEL-ARRIVAL WALL — RETARGETED unify-all-creation-flows 2026-08-14, since the organic recap-step gate is retired and the wall submit is now the live pre-email gate: an EXPLICIT funnel-carried dream, on the email submit, shows the revise message in `#contact-error` and fires NO generation, NO signup/`CompleteRegistration`, and no navigation; a CLEAN dream passes the gate and proceeds to the real pending generation; the `check-dream-content` endpoint is intercepted by inspecting the POSTed caption so the real client wiring — assemble text → POST → act on `{allowed}` — is exercised), and the existing `test/content-gate-generation.test.js`/`test/content-classifier.test.js` (unchanged — the shared classifier/generation-gate decision this endpoint reuses) | **REAL-MODE gate: the classifier's own LLM call is never made for real here** — no `FAL_KEY` in the build sandbox (same recurring limitation as all fal-adjacent work), so every test drives the verdict via `CONTENT_CLASSIFIER_MOCK_TIER` or the fail-open path; a human with `FAL_KEY` should confirm one real explicit caption is blocked pre-signup on a deployed preview before calling this fully closed. `start.html` and `create.html` gates are covered by the endpoint unit tests + code path only, NOT a dedicated browser walk (the `wizard.html` behavioral test is the representative surface); the growth-repo funnel's own matching gate is a separate change driven by the founder. |
| **Someone-photo capability removed / anti-NCII server backstop intact** (founder call 2026-08-14) — a "Someone I know / someone-specific" (non-self) character may carry a NAME + DESCRIPTION only, NEVER an uploaded PHOTO; only the self ("Me") character may attach a photo. Verified already enforced at EVERY client layer (the someone-photo path was never actually shipped — this is a regression lock, not a removal of live code): `js/store.js`'s `saveCharacter`/`resolveCharacters` strip `photoDataUrl` off any non-self character (explicit "safety boundary, not just a UI gap" comment), and `wizard.html`/`create.html`/`profile.html`'s character sheets only show the Describe/Upload-photo toggle for the self sheet (`char-mode-row` display gated `mode === 'self'`). The SERVER-side anti-NCII backstop is deliberately LEFT IN PLACE and unchanged: `lib/content-classifier.js`'s `detectNamedOtherPersonPhoto` + the fail-CLOSED named-photo path in `generate-video.js`/`check-dream-content.js`, and `js/content-block-panel.js`'s `namedPerson` (no-soften) branch. | `test/someone-character-no-photo-behavioral.test.js` (create.html Build sheet: the "Someone I know" sheet shows NO photo toggle / NO photo area — only Describe + name — while the "Me" sheet DOES offer Upload-photo; `DreamStore.saveCharacter` strips `photoDataUrl` off a named non-self character but keeps it for self), and the unchanged `test/content-classifier.test.js`/`test/check-dream-content.test.js`/`test/content-gate-generation.test.js`/`test/content-block-panel-behavioral.test.js` (the server anti-NCII gate + named-photo block panel, still green) | Server gate stays as a pre-emptive backstop for the day a someone-photo path ever ships |
| **Email-verification bonus (+20)** — founder-authorized 2026-08-08 ("keep it on the section that has the bonus tokens... maybe also give 20 tokens for verifying email"; REPLACES the rejected hook-to-Post idea — publish/E8 gate and Post flows untouched). Server: both verification success paths (`verify-email-code.js` explicit code, `verify-email-link.js` implicit link click) grant `entitlements.EMAIL_VERIFIED_BONUS_AMOUNT` (20) once-ever per account via `applyAchievementGrant` under the SHARED `EMAIL_VERIFIED_ACHIEVEMENT_ID` (constants exported from `lib/entitlements.js` so the two endpoints can never drift) — this is that mechanism's second real wiring after the install bonus, same two-phase idempotent marker; gated on `markEmailVerified`'s new `changed` flag (a pre-bonus-era verified account re-verifying never re-grants; the marker backstops regardless), best-effort relative to the verification itself (a grant bookkeeping failure never fails the verify or breaks the link 302). No balance ceiling exists to respect (Economy C removed ceilings — same as every grant). The code path returns `bonus:{granted,amount,balance}` so clients never guess. Client: home.html's Make-it-yours card gains a "Verify your email · +20" row (unverified signed-in accounts only; tap → `EmailVerifySheet.show({source:'bonus_row'})`), flipping to ✓/chip-retired on the sheet's new `dreamtube:email-verified` window event + balance refresh + server-confirmed toast; an install-claimed-but-unverified account keeps the card shrunk to just this row (was: card gone entirely); the sheet's done view adds "+20 tokens added" only when the server reported the grant | `test/passwordless-signup.test.js` (grant lands exactly once on the code path with the response contract + entitlement delta, replayed grant is a marker no-op with balance untouched; link path grants the same once-ever +20 and a consumed-link second click re-grants nothing; already-verified: `markEmailVerified` reports `changed:false` and `resend-verification-code` refuses re-entry — no second grant by any route), `test/entitlements-achievements.test.js` (the underlying two-phase marker's dedicated concurrency coverage, pre-existing), `test/email-verify-sheet-behavioral.test.js` (done-view +20 line only on server-confirmed grant; row visible+chipped for unverified / hidden for verified; row tap opens the sheet; post-verify flip + chip retirement + balance re-fetch; the shrunk install-claimed-but-unverified card state) | — |
| Entry-routing guard: a logged-out visitor with stale local account history (a browser that already held a real account, even after logout — `DreamStore.hasLocalAccountHistory()`) never lands mid-funnel/wizard, always the welcome/choice screen (`index.html`) instead — tracker item `for-product-life-origin-generate-handoff-founder-repro-8yc4wm` | `test/life-origin-generate-handoff-behavioral.test.js`, `test/route-organic-to-wizard-behavioral.test.js` (genuinely-first-time-visitor control case) | — |
| Record-intent preservation across the login round-trip: a logged-out `create.html?record=1` deep-link (Speak/record-it entry) must survive the sign-in guard's redirect and land back in the actual recording flow, not the plain Build/Write/Record choice screen — tracker item `for-product-speak-path-from-the-wizard-e-ycos08` | `test/record-mode-behavioral.test.js` (2 tests: the full `?record=1` round-trip via `login.html`'s own `?next=`, and the plain-path control case) | — |
| Journey 2 full flow, live, real backend, real probe account | `test/prod-smoke/session.test.js` (test 1) | — |
| Generate-during-signup real chain (client → real server handlers) | `test/funnel-real-chain-behavioral.test.js` | — |
| Email deliverability/availability check (`check-email.js`) | `test/check-email.test.js`, `test/email-domain-check.test.js` | — |
| Pending-generation adoption (`start-pending-generation.js`, `claim-pending-generation.js`, `pending-dreams.js`) | `test/pending-dreams.test.js`, `test/funnel-real-chain-behavioral.test.js`, `test/claim-and-verify-pending.test.js` | — |
| A2HS / install nudge | `test/a2hs-install-nudge-journey-behavioral.test.js`, `test/install-first-door-behavioral.test.js` | — |
| A2HS small nudge card (`js/install-nudge.js`) — "install" vs "add to your home screen" CTA-copy unification, matching its own head's phrasing | `test/home-mky-copy-behavioral.test.js` | — |
| Service worker update lifecycle + stale-page prevention (`sw.js`, `js/pwa.js`, `scripts/generate-version.js`'s CACHE_VERSION auto-stamping) — tracker item `for-product-urgent-founder-gets-stale-pa-6dn54z`, four real founder-device staleness incidents across 2026-07-29/08-02/08-03/08-04. Three rounds: (1) `registration.update()`/`controllerchange` wiring + the 30-minute-hidden `visibilitychange` reload fallback (iOS Safari tab-freeze case); (2) CACHE_VERSION now auto-stamped with a fresh, deploy-unique string on every real build (reusing `version.json`'s own commit-based version string, not a second scheme) so sw.js's bytes reliably differ every deploy — a manual bump left most routine content-only deploys byte-identical, so the browser's own update-check had nothing to find; also forces `cache:'no-store'` on the outbound network-first fetch (bypassing the browser's separate HTTP disk-cache layer, not just this file's own Cache Storage usage) and fixed a `response.ok`→`response.status===200` bug (a 206 Partial Content response would throw on `cache.put`); (3) a SECOND, SW-lifecycle-INDEPENDENT safety net (`js/pwa.js`'s `initVersionFallback()`/`checkVersionFallback()`) — rounds 1-2 above are confirmed live and correct server-side on both production origins, but only ever reach a device with an already-registered OLD service worker if the browser's own `registration.update()` byte-check succeeds at least once, which is specifically unreliable in Safari/WKWebView (the founder's real device context); this round polls `/version.json` directly on page load and on the same `hiddenStaleThresholdMs()`-gated `visibilitychange` pattern round 1 already uses (no new timing scheme), comparing against a `localStorage`-recorded last-seen version, and reuses the shared `reloadWhenSafe()` guard (one-shot, typing/recording-safe) on a genuine mismatch — never touches the service worker at all, so it keeps working even if that whole layer is broken | `test/pwa-sw-update-behavioral.test.js` (real Chromium: update/controllerchange/visibilitychange-reload lifecycle, plus a genuine on-disk sw.js byte-change end-to-end via a symlinked temp deploy root — proves the real mechanism, not a synthetic event), `test/sw-fetch-handler.test.js` (a `vm`-sandboxed load of the real, unmodified sw.js source proving the exact fetch options/caching decisions its handler issues — Playwright cannot intercept SW-internal fetches once a worker controls the page, so this is the only way to inspect them without a real browser), `test/generate-version.test.js` (`stampServiceWorkerCacheVersion` unit tests incl. against the real committed sw.js, plus an end-to-end run of the real build script proving version.json and sw.js always agree and differ across two simulated deploys), `test/pwa-version-fallback-behavioral.test.js` (round 3, real Chromium: first-ever check for a browser records a baseline with no reload; a genuine on-disk `version.json` byte change — same symlinked-temp-deploy-root technique as the sw.js test — triggers exactly one reload once the tab resurfaces past the shared stale threshold; the typing guard is shared and respected; a `route.abort()`-forced fetch failure never throws, never fabricates a recorded version, and never reloads; a multi-tab `storage`-event fix — review finding: `LAST_KNOWN_VERSION_KEY` is one shared-per-origin key, so without this a sibling tab whose own check fired later would see the first tab's write and silently skip its OWN reload — proven with two pages sharing one browser context, where the second page never runs its own version check at all and can only reload via the sibling's localStorage write) | No real WebKit/Safari engine exists anywhere in this repo's test tooling (Chromium only) — every test here proves the MECHANISM works, not that a real iOS Safari/in-app-webview client behaves identically; this is an accepted, structural limitation (see each file's own header comment), not a gap to close in this pass. This is a repeat founder-reported device bug, so per this codebase's standing QA rule it stays `waitingFor:ron` for final on-device confirmation even after merging. |

## Home (`home.html`, 2026-08-01 module set)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Overall page / round-4 rebuild | `test/home-behavioral.test.js`, `test/home-round4-behavioral.test.js` | — |
| Day-0/active dream room card (`#day0-card`), incl. the quiet row's four per-action behaviors (founder bug 2026-08-10: Post/Make another/Edit/Delete all just opened `result.html` doing nothing). Each button now performs its OWN action: **Post** → `result.html?id=…&publish=1`, **Edit** → `…&edit=1`, **Delete** → `…&delete=1` (three deep-link intent params `result.html` opens the matching post-modal / edit-delta sheet / delete-confirm modal for on load, then strips from the URL, mirroring its existing `&play=1`); **Make another** → `create.html` after `DreamStore.clearDraft()` (a genuinely new dream, touching this one not at all). `home_day0_card_quiet_action_tapped` tracking preserved on all four. | `test/home-day0-dream-card-behavioral.test.js` (incl. the forming caption's new copy — the old "Ready when your dream is" line is gone; and the per-action quiet-row test now asserts each button's real destination AND — for the three intent params — that the corresponding modal/sheet actually opens on `result.html` and the param is stripped, so no dead param), `test/edit-regeneration-forming-state-behavioral.test.js`, `test/sage-during-generation-behavioral.test.js` (the forming state's own interpretation offer `#d0-sage` — see the Interpretation/Chamber section's row for the full picture) | The `result.html` intent handler is owner-gated (`isMyDream`) — a deep link to someone else's dream with an intent param is a no-op, same as those on-page controls' own visibility gate |
| Ritual module's daily claim button + bonus-tokens countdown (`#ritual-claim`/`#ritual-countdown`) — the countdown ("Next N free tokens in Xh Ym") re-added inside the ritual module itself (tracker item `for-product-bonus-tokens-countdown-vanis-qm96xc`, founder feedback: it had quietly dropped out of an earlier "Home round N" rebuild), lives INSIDE `#ritual` rather than as a separate page element, hides while claimable (the claim button takes its spot), and ticks live via a 60s interval that re-fetches once `nextClaimAt` actually passes (`shop.html` used to carry the same inline-countdown pattern, but its inline free-claim card was removed in the 2026-08-10 copy trim — Home's ritual module is the surviving instance) | `test/home-behavioral.test.js` | — |
| `?generate=1` fresh-in-app-generation handoff — the room card/Tonight-hero must show forming IMMEDIATELY on a brand-new (non-edit) submission, not only once the real pendingJob lands seconds later (worst on a first-ever-on-this-browser-origin account, where nothing else on Home hints anything is happening) — tracker item `for-product-life-origin-generate-handoff-founder-repro-8yc4wm` | `test/life-origin-generate-handoff-behavioral.test.js` | — |
| Ritual module's streak CONSTELLATION (`#ritual-constel`'s `<svg class="constel">`, lit stars + connecting lines) — tracker item `for-product-regression-founder-screensho-17vxr2`, founder-reported missing on his real device despite the code being byte-identical to every approved mock. Real root cause a source-level diff never caught: the `<svg>` was missing `width="84" height="46"` attributes every mock includes, and the one CSS rule referencing `.constel` never actually applied to it (it targets a direct flex child of `.ritual-head`, but the real flex child is the wrapping `#ritual-constel` span, not the svg nested inside it) — so the svg's computed size collapsed to 0×0 in every branch, regardless of streak/week-count data | `test/home-behavioral.test.js` (asserts a REAL non-zero rendered bounding box, not just DOM presence, across all four `renderRitualModule()` branches plus the founder's exact reported state — 5 nights/0-of-3-this-week/claimable) | — |
| Tip of the Day, Get Inspired, Consider Publishing, Make It Yours, ritual/streak module — as a set, live on a real deployed origin | `test/prod-smoke/session.test.js` (test 2) | Per-module deep behavioral coverage (copy variants, edge/empty states, exact interaction details) is NOT this suite's job — check for a dedicated `test/*.test.js` file per module before assuming full coverage; several of these modules (Get Inspired, ritual/streak beyond the daily claim itself) currently have **no dedicated `test/*.test.js` file** of their own — home-behavioral/home-round4-behavioral cover the page broadly, not module-by-module |
| Make It Yours — "install" vs "add to your home screen" copy unification (reward line, done-state, inline install-row CTA + its supporting text, webview-fallback note) — tracker item `for-product-build-ship-today-founder-app-zn9zyy` | `test/home-mky-copy-behavioral.test.js` (renamed from `test/home-hero-whisper-mky-copy-behavioral.test.js` when the Tonight-hero-whisper coverage it used to also carry was deleted, see the row below) | — |
| Bottom dock (`js/bottom-nav.js`, "Bar A") | `test/barA-nav-rollout-behavioral.test.js` | — |
| Webview escape nudge — the quiet, DETECTION-triggered `#webview-card` (Step 1 of 2 install/data-safety nudge; `home_webview_escape_shown`/`_tapped`), shown in every FB/IG/Messenger in-app webview | `test/home-webview-escape-behavioral.test.js` | — |
| **Finished-video playback-FAILURE detect-and-guide (founder-directed 2026-08-14):** distinct from the detection nudge above — the new `#playfail-card` shows ONLY when this account is in an FB/IG/Messenger in-app webview AND the finished dream video on `#d0-video-el` genuinely won't play (an `error`, a rejected `play()`, or a ~7s stall with zero decoded frames — one silent self-heal reload first, never counted while `document.hidden`). It's the actionable moment (a video that won't play), so it reads more prominently than the quiet nudge, and reuses the exact same escape mechanics (`buildAndroidChromeIntentUrl` intent:// on Android, `copyLinkForBrowser` canonical `?bt=`-carrying copy-link on iOS/other) plus a line pointing at the server-side recovery email. Fires `webview_video_playback_failed` `{host, platform, reason}` (the measurement of how often the webview genuinely can't play — distinct from the detection-only `home_webview_escape_shown`) and `webview_video_recovery_tapped`. Cannot false-positive on a not-yet-tapped video: `#d0-video-el` is `muted autoplay`, so there is no "waiting for a tap" state. On the smooth path (video plays fine) it shows NOTHING; the detection nudge above is left as-is. | `test/home-webview-video-playback-failure-behavioral.test.js` (real Chromium: an errored video in an Android IG webview shows the card with the intent:// escape carrying `?bt=` + the email note + one `webview_video_playback_failed{platform:'android'}`; iOS FB webview shows the copy-link action that copies the `?bt=` URL and a persistent note + `{platform:'other'}`; a genuine STALL fires `reason:'stall'`; a video that plays fine in a webview shows no card and fires no failure event; a normal non-webview browser never shows the card even with a broken video) | Implemented on `home.html` only — the post-signup finished-video landing (`home.html?generate=1` → `#d0-video-el`). `result.html`'s dream-room ambient video already has its own webview handling (a one-retry-on-`error` on the `#result-video` element and a full-viewport in-app overlay fallback player for when native fullscreen is blocked); extending this same failure-guidance to `result.html` (which would need the inline escape helpers extracted/duplicated there) is a reasonable follow-up, not part of this build |
| **Tonight CTA / logged-confirmation split (tracker item `for-product-founder-08-07-homepage-hero--015hgp`, founder ruling "kill the square, bare 'What did you dream?' button -> 3-choice screen"):** the old `.hero-tonight` moon-card's UNLOGGED prompt state (eyebrow, h1 title, cycling whisper line, primary pill, Write/Speak/No-recall quiet-link row, day-1 "First night" line) is DELETED entirely and replaced by `#tonight-cta`, a single standalone `<button>` outside any `.hero-card` — text EXACTLY `"What did you dream?"`, no box/background art of its own, navigating to a bare `create.html` (same destination the old pill's for-product-founder-ask-08-05-tell-your--vmfvsk fix already established — this change only replaced the entry-point markup above it, not the destination). `#hero-tonight` itself (id, `.hero-card`/background/sizing CSS, `.logged` class, `#tonight-title`/`#tonight-quote`) is left completely UNTOUCHED and now renders ONLY the "logged tonight" confirmation state — a real dream still routes to the separate `#day0-card` room-card instead, exactly as before this change. Net removal, not addition: Write/Speak remain reachable one tap further via the same choice screen, but "No recall" (`DreamStore.logNoRecallToday()`, untouched in `js/store.js` — the `noRecallDates` data concept is still real and still read by `getDreamLogStatus`/the ritual module/this same logged-confirmation card) currently has **no live UI entry point left anywhere in the app** — the only other place that copy exists, the WhatsApp morning-capture template's "No-recall" button (`netlify/functions/send-whatsapp-morning-capture.js`), sends a template message only; no receiver function exists yet to actually record a tap on it (confirmed via `netlify/functions/dream-webhook.js`'s own doc comment: "config-gated stub today (no real WhatsApp Business Cloud API wiring/approved template yet)"). Flagged explicitly on the tracker item and in the PR description for founder preview, not silently dropped. | `test/home-behavioral.test.js` (button's exact text/class/no-.hero-card-ancestor/tap-target size, navigates to the bare 3-choice screen, Write it/Record it still reach their correct destinations from there, D1 account sees only the bare button with no day-1 sub-copy anywhere, analytics still fires `home_today_entry_tapped`/`home_card_tapped` off the new button, no horizontal overflow/tap-target regression), `test/home-day0-dream-card-behavioral.test.js` (the retained `#hero-tonight.logged` card still renders correctly and independently of `#day0-card`/the unlogged CTA in every state that touches it), `test/out-of-tokens-purchase-sheet-behavioral.test.js` (the underlying Home screen — now `#tonight-cta`, not `#hero-tonight` — stays visible under the purchase-sheet overlay) | **Held for founder preview before merge (changed-screen rule) — not yet merged to `main`.** This is a real product-behavior change beyond visual simplification (the home-screen "No recall" entry point above), not just a gap in test coverage. |

## Generation (video/image)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| `generate-video.js` (prompt build, guardrails, model rotation/config, tokens, Turnstile) | `test/generate-video-build-prompt.test.js`, `test/generate-video-model-rotation.test.js`, `test/generate-video-model-config.test.js`, `test/generate-video-tokens.test.js`, `test/generate-video-turnstile.test.js`, `test/generate-video-audio-toggle.test.js`, `test/generate-video-image-to-video.test.js` | — |
| **Style-integrity guardrail** — `buildPrompt`'s `styleIntegrityClause(caption, style)`, appended as the prompt's second clause (right after the caption, before characters/camera/scenery/the style modifier itself). **Two generations of the same underlying bug:** (1) tracker item `for-product-founder-ask-08-04-style-must-ezz8uf` (commit `0500852`), founder repro: caption "I am running back and forth to bring some important things for my family" + style Anime produced a video where the dreamer was rendered AS a flying dragon — fix originally phrased the guardrail around "the dreamer" specifically; (2) tracker item `for-product-bug-founder-recurring-style--srehi9` (2026-08-11), SAME bug, BROADER trigger: a bare third-person caption "a woman" (no "I", no first-person framing) + an anime/specific style again produced a flying dragon with no woman at all — "the dreamer" didn't reliably bind onto a subject the caption never calls "I", and a 2-word caption has almost no content of its own to compete with a vivid style descriptor. **Current fix**: the clause now binds to "the subject this dream describes", explicitly spelling out that this covers both first-person ("I") and third-person/generic phrasing ("a woman", "a man", "a child"); a second, additional sentence — quoting the caption back verbatim and explicitly ruling out an animal/creature/non-human subject — fires only when the caption is at/under `SHORT_CAPTION_MAX_WORDS` (10) words, i.e. too short to plausibly outweigh the style modifier's own vividness on its own; a longer caption (like the original 08-04 founder repro, 14 words) does not get this reinforcement, since it isn't needed there. Still style-agnostic (applies to every style, not just Anime/Cartoon) and still deliberately doesn't override genuine transformation content the dream text itself describes (`unless the dream itself explicitly describes a transformation`). Shared by both generation entry points, since `start-pending-generation.js`'s pre-signup path calls this same exported `buildPrompt`, not a separate copy. **ETHNICITY NEUTRALITY (founder 2026-08-11, "go"; REFRAMED 2026-08-13, tracker item `for-product-investigate-no-specific-ethn-zz0fyv`):** founder report 2026-08-13 that a real video (example user Joyrobb17) still came out with a specific ethnicity assigned. The clause was negative-only ("do not assign the subject any specific ethnicity, skin tone, or racial appearance unless the dream text explicitly specifies one") — negative instructions are well-documented to be followed more weakly than a positive instruction stating the desired outcome directly, for these models. `styleIntegrityClause` (video) and `buildImagePrompt` (image) were both reframed to LEAD with a positive instruction — "render the subject with a neutral, unspecified ethnicity, skin tone, and racial appearance by default" — immediately followed by the original negative wording as reinforcement rather than the sole instruction, then the exact same "unless [the dream text/the description] explicitly specifies one" carve-out, preserved verbatim and unweakened, so a dream that DOES describe an ethnicity is never suppressed. Both files' clauses stay parity-matched. | `test/generate-video-build-prompt.test.js` (original 08-04 founder-repro caption + Anime still produces the guardrail, still correctly ordered ahead of the style modifier, and correctly does NOT get the short-caption reinforcement since it has 14 words of its own content; guardrail present for every style incl. an unrecognized one; guardrail coexists correctly with the pre-existing self-photo reference-image pointer line/description; 08-11 coverage — bare short third-person captions ("a woman", "a man", "a child", "a dog running") get the guardrail's explicit third-person binding text AND the short-caption reinforcement sentence quoting the caption verbatim and naming the actual observed failure mode (animal/creature); an 11-word caption sits just over the threshold and correctly does not get the reinforcement; the reinforcement composes correctly with a self-photo character's `charTextParts`/`photoSelf` text without conflict; an empty caption gets the base guardrail but no reinforcement, since there's no subject text to quote; NEW 08-13 coverage — the ethnicity clause's positive lead is present and appears before the negative reinforcement text, which appears before the unchanged carve-out) and `test/generate-image.test.js` (parity: `buildImagePrompt` appends the same reframed sentence with its carve-out; NEW 08-13 coverage — same positive-lead-before-negative-before-carve-out ordering assertions as the video test) | **Real generation+eyeball verification NOT done for any generation of this fix, including the 08-13 reframe** — no `FAL_KEY` in the build sandbox; someone with fal.ai access must run at least one real generation with a caption that states no ethnicity, across both the video and image paths, and confirm the subject's ethnicity comes out visibly unspecified/varied across repeated runs (not just that the fal call succeeds) before calling this fully resolved. **Joyrobb17's specific example was NOT root-caused** — the build sandbox has no admin/DB credentials to query `admin-media-library-data` (owner-gated `verifyOwnerCredentials`), so it's unconfirmed whether that specific video predated the clause's 08-11/08-12 deploy, was a photo-based Me/someone dream (ethnicity legitimately sourced from a reference photo, out of scope for a text clause), or explicitly named an ethnicity in the user's own prompt (the carve-out, working as intended) — any of those would mean the clause was never actually the failure mode for that example. The 08-13 reframe is a real, defensible improvement to the negative-instruction weakness regardless of which of those turns out to be true, but it is not proven to be what fixed that specific report. |
| Audio/dialogue generation — `generate_audio` unconditionally `false` for EVERY video generation path (`generate-video.js`'s plain/reference-to-video/image-to-video/PixVerse-rotation calls, and `start-pending-generation.js`'s own independent pre-signup computation), overriding whatever `audioOn` the client sends — tracker item `for-product-turn-off-audio-dialogue-gene-ooeyoj`, founder directive 2026-08-02 (cost, and to stop Veo's native audio from lip-syncing invented dialogue the user never wrote). Supersedes the 2026-07-28→07-29 history of a client-honored toggle + a retired owner/IL force-off (`resolveGenerationProfile`'s `forceAudioOff` field removed as dead weight; `profile` label survives for cost-attribution logging only). **Still in effect, unconditionally, untouched by the music-bed row below** — the generated video FILE itself is, and will remain, permanently silent; the `audioOn`/`musicStyle` payload plumbing (`js/store.js`, `home.html`/`result.html`'s regenerate/turn-into-video paths) is left intact and inert, a trivial flip back on if ever reversed. style.html's own Audio & music toggle, previously shown disabled here (`.toggle-switch.disabled`), briefly became real (music-bed row below) and has since been removed entirely — see that row, not this one, for the current state | `test/generate-video-audio-toggle.test.js` (every call shape — plain, reference-to-video, image-to-video-adjacent, PixVerse rotation — always `generate_audio:false` regardless of `audioOn`/`musicStyle`/`OWNER_EMAIL`/geo/condensing; `resolveGenerationProfile` no longer returns a `forceAudioOff` key), `test/start-pending-generation-audio-force.test.js` (the same unconditional-off contract on this file's own independent `generateAudio` computation, incl. its self-photo reference-to-video branch), `test/audio-toggle-behavioral.test.js` (real Chromium: every generation still only ever sends the permanently-inert `audioOn:false`/`musicStyle:null`/no `musicBedOn` at all to `generate-video.js`; `regenerateDream`'s pre-existing `audioOn:true` client payload is confirmed to still be sent — deliberately inert plumbing, not a behavior change) | — |
| **Music-bed playback (CLIENT-SIDE ONLY, not server-side audio generation)** — tracker item `for-product-build-founder-approved-08-03-jlkjy9`. Shipped in two stages the same day: (1) style.html's Audio & music toggle went real, per-dream, default ON (Option B); (2) hours later, **founder simplification: "NO TOGGLE. Since the music beds are free, music is simply ALWAYS ON — every dream video plays with its style-matched bed, no user choice."** The toggle, its markup (`#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub`), and the `musicBedOn` field it wrote onto a draft/dream are now removed entirely — no disabled/placeholder UI left behind (matches the precedent this same tracker item already set when it removed the old `#music-style-row` chip picker outright rather than disabling it). **Current, permanent state:** a reusable, one-time-generated, style-matched AMBIENT MUSIC BED (`assets/music-beds/{cartoon,cinematic,anime,realistic}.wav`, served as plain static files — no build step, no new endpoint) plays for **every** dream that has a real video and a known style, full stop — `js/music-bed.js`'s `eligible()` no longer reads `musicBedOn` in any form, so a dream generated/published during either earlier stage (carrying a stale `musicBedOn:false`/`true`, or predating the feature entirely with no such field) is treated identically to a brand-new one — no lingering opt-out from old data. Never anything model-generated — the row above's `generate_audio:false` fix is completely untouched. Playback is a synced `<audio>` element (`js/music-bed.js`) that starts/stops with the video's own play/pause state and shares the video's own device-level mute preference (`DreamStore.getSoundPref`/`setSoundPref`) — wired into result.html's player and explore.html's feed (home.html's own ambient preview thumbnail and the admin-only media-library lightbox stay deliberately scoped OUT, see `js/music-bed.js`'s and this row's own detail below); **mute stays available in the player exactly as always.** **Implementation-choice tradeoff, stated explicitly (per the tracker item's own ask):** client-side playback, not muxed into the video file at generation — ships without a new ffmpeg/vendor dependency, but means a downloaded or externally-shared video file carries no audio at all; the bed only ever plays inside DreamTube's own player UI. Root-caused via systematic-debugging: a freshly created, `preload="none"` `<audio>` element's first `.play()` call, made synchronously inside an `IntersectionObserver` callback, intermittently (~1 run in 3 in headless Chromium) silently never started (no error/event at all) — fixed with a bounded, event-driven retry (the video's own recurring `timeupdate`, never a blind timeout) in both result.html and explore.html. **Known gap, scoped out deliberately:** `watch.html` (the retention-email "watch it" link) and `claim-dream.html` (the pre-signup claim moment) read a dream through server-side projection endpoints (`get-shared-dream.js`, the claim flow's own endpoint) that don't carry `style` at all — extending those would mean threading a new field through `dream-sync.js`'s private-dream schema and those endpoints' own response projections, a materially larger, separate change from the client-only surfaces this pass covers; both stay silent (unchanged from before this feature) until a follow-up does that work. **Update 2026-08-04:** the four style beds described here are now selection TIER 2 — the permanent fallback reached via `js/music-bed.js`'s new `urlForDream` when a dream has no usable mood. Nothing in this row's behavior changed (see the mood-keyed row directly below, whose zero-regression coverage proves exactly that); `urlForStyle` is unchanged and still directly tested. | `test/music-bed-behavioral.test.js` (all 4 bed assets resolve as real static files at their production path; `js/music-bed.js`'s `urlForStyle`/`eligible` contract — a real videoUrl + known style is the ONLY requirement now, confirmed identical across a stale `musicBedOn:true`, a stale `musicBedOn:false`, and the field missing entirely; an unknown style or missing video still fails closed to no bed; result.html and explore.html both confirmed, via real Chromium, to play/pause/mute a bed in sync with the video for every eligible dream regardless of any historical `musicBedOn` value), `test/audio-toggle-behavioral.test.js` (confirms the toggle's markup — `#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub` — is fully absent from the DOM on both media types, not just hidden; `musicBedOn` confirmed never sent to `generate-video.js` and never present on a finished/regenerated dream record) | `watch.html`/`claim-dream.html` playback (see detail column — real, scoped-out gap, not silently missed); home.html's own ambient preview thumbnail and the admin-only media-library lightbox are deliberate silent-by-design exclusions, not gaps (see `js/music-bed.js`/explore.html's own comments for why) |
| **Mood-keyed music beds (selection tier 1) + `dream.mood` persistence + the audition page** — tracker item `for-product-founder-08-04-evening-music--jfjco0`, FINAL-FINAL scope (founder insight 2026-08-04: a dream's VISUAL style is a weak signal for what it should sound like; the dream-builder wizard's MOOD step is the right one). Three parts. **(1) The persistence bug, fixed.** The wizard's Mood step (step 4 of 5 — peaceful/joyful/dreamy/mysterious/tense/epic, `js/wizard-chips.js`'s `MOOD_CHIPS`) reached `assembleCaption`/`buildDeterministicStory` — i.e. the generated prompt/story TEXT — and was then dropped on the floor at `create.html`'s `finishBuildWizard` `setDraft` and `wizard.html`'s `draftPatch`, so it never survived onto the dream record and nothing downstream could key off it. Now threaded end to end: wizard → `state.draft.mood` → `home.html`'s `startFreshGenerationFromDraft` → `startGeneration` → `savePendingJob` → `finalizeDream` → `dream.mood`, plus the pre-signup route that never touches `generateVideo` at all (`adoptPendingGeneration(..., mood)` → `resumePendingJob`'s re-stamp), plus into the shared feed record (`publish-dream.js`, validated against a fixed `KNOWN_MOODS` list — this value lands in a world-readable blob and is read back into an asset path, so an unbounded string is rejected). A regenerate/edit PRESERVES the dream's existing mood (those flows have no mood picker; dropping it mid-edit would be an unrequested behavior change). **SKIP vs. Continue is a real distinction** — both wizards pre-highlight the default chip, so `moodKey` alone can't tell them apart; a new `moodSkipped`/`buildMoodSkipped` flag feeds the shared `WizardChips.persistedMood(moodKey, skipped)`, which returns null for skipped / '+ Something else' free text / anything unrecognized. **Prompt and story assembly are deliberately untouched** — a skip still contributes the default mood language it always did, so what actually gets generated is byte-identical to before this field existed. **(2) The selection tier** (`js/music-bed.js`'s new `urlForMood`/`urlForDream`, now called by result.html and explore.html in place of `urlForStyle`): the dream's own mood first, the four visual-style beds as the **permanent** fallback (not a migration step — they are what a skipped/free-text/legacy mood resolves to, forever). **(3) `bed-audition-x7q4.html`** — owner-page convention (unguessable name + `noindex,nofollow`, no password since it calls no backend), one row per mood, a reference player for the four current beds, two candidate slots, one-winner-per-mood picking persisted across reloads, and a copyable picks summary. **TIER 1 IS LIVE (2026-08-07, same-day full cycle):** the 12 candidate tracks (2 per mood, fal-ai/stable-audio 45s seamless loops — generated at 47s, last 2s crossfaded into the first 2s — 44.1kHz/16-bit stereo, gain-matched to the four style beds) were generated and committed, the founder auditioned all 12 on `bed-audition-x7q4.html` and picked one winner per mood (**peaceful-A, joyful-B, dreamy-A, mysterious-B, tense-B, epic-B**), each winner was promoted in place to `assets/music-beds/moods/<mood>.wav`, `MOOD_FILES` was filled with all six, and — per the tracker item's own no-dead-assets instruction — the six losing candidates AND the audition page itself were deleted. A dream with a real chosen mood now hears its mood bed on every playback surface; a skipped/free-text/legacy mood still falls back to its visual-style bed byte-identically to before (tier 2, permanent). The four style beds are untouched at `assets/music-beds/<style>.wav` | `test/mood-music-bed-behavioral.test.js` (the zero-regression proof across all 30 mood × style combinations with `MOOD_FILES` empty, plus a guard that it IS still empty so committing a real track forces this row to be revisited; tier 1 winning over the visual style once a track exists, proven by populating `MOOD_FILES` at runtime as a stand-in — a partially-delivered 2-of-6 state, not just all-or-nothing; skipped/free-text/unknown/absent mood and the both-tiers-fail case all falling back or failing closed; the three copies of the mood key list — `MOOD_CHIPS`, `music-bed.js`'s `MOOD_KEYS`, `publish-dream.js`'s `KNOWN_MOODS` — asserted identical since they can't import one another; real Chromium walks of create.html's "Build it" Mood step for chip/Skip/free-text/back-then-answer, asserting both the persisted `draft.mood` AND that the generated prompt text is unchanged; mood surviving a full mocked-transport generation onto `dream.mood` and then driving result.html's actual `#result-music-bed` src; the no-mood path producing exactly today's record and today's bed; a RESUMED adopted pending job keeping its mood — the pre-signup funnel's only route; regenerate preserving mood; explore.html feed cards keyed off a record's mood with legacy no-mood records unaffected; and the six LIVE mood beds: tier 1 resolving every real mood × every style to that mood's own committed winner bed, the no-mood/free-text/legacy path still resolving byte-identically to `urlForStyle` (the zero-regression guarantee, scoped to the dreams it still applies to), a future-mood-key-without-a-track guard falling back rather than going silent, and an assets-integrity test — all six winner WAVs valid RIFF/WAVE and full-length on disk, served 200 at their production URLs, one genuinely decoding and playing in real Chromium, plus explicit assertions that the candidates directory and the audition page are GONE. The audition-page picking-flow tests were removed with the page itself when the audition concluded — the flow they covered no longer exists), **the cross-device sync path, which takes BOTH halves and originally had only one** — `test/dream-sync.test.js` covers the SERVER half only (`mood` surviving the private-dream upsert/GET round trip once it reaches the handler — it had to be added to `dream-sync.js`'s `DREAM_FIELDS` allowlist; plus the no-mood dream syncing exactly as before), but it calls the handler directly with a payload hand-built by its own `dreamPayload()` helper, so it proves nothing about whether the real client ever puts `mood` on the wire. **It didn't** — review on this branch caught that `js/store.js`'s `attemptPrivateDreamSync` builds its upsert body from an explicit hand-maintained field list that omitted `mood`, so the value was correct in local state, correct in the server allowlist, and silently dropped in transit; a dream restored on a new device would still have come back moodless. Fixed, and the wire contract itself is now covered in `test/mood-music-bed-behavioral.test.js` (a real generation's mood asserted present in the actual outgoing dream-sync POST body, and the no-mood path asserted to send an explicit `mood: null` rather than an absent key) — both verified to fail against the unfixed client and pass with it, `test/wizard-chips.test.js` (`persistedMood` unit coverage: all six real keys, the Skip-vs-Continue-on-the-default ambiguity, free text, fail-closed on unknown, and an explicit assertion that it changes neither `assembleCaption` nor `buildDeterministicStory`) | **Real-mode/mobile verification of the live mood beds on the deployed site is still pending** (`AGENT_POLICY.md`'s REAL-MODE gate) — everything above is verified against a local serve in real Chromium at mobile viewport; one post-deploy listen on a real phone (autoplay gesture path included) should confirm before this is called fully done. No `dream.mood` backfill for dreams created before this shipped (forward-only, same as `createdAt`/`storyText`/`modelUsed`) — they fall back to their style bed, which is what they already had. `claim-dream.html`'s `saveClaimedDream` path carries no mood (the abandoned-dream re-engagement link resolves server-side through `verify-pending-claim.js`, which has no mood field) — falls back to the style bed, same as `watch.html`/`claim-dream.html`'s pre-existing scoped-out gap in the row above. Free-text moods are NOT keyword-mapped (explicit scope cut in the tracker item); the typed text still reaches `promptText`, so a later pass has something to work from |
| **One-time sound-discoverability nudge** — tracker item `for-product-p1-founder-repro-08-04-12-40-6icx89`. Root cause: `DreamStore.getSoundPref()` (`js/store.js`) defaults every video/bed to MUTED until a user has explicitly tapped a mute/unmute control — harmless for this app's entire history because every video was permanently silent (row above), so nobody ever had a reason to tap unmute. The music-bed row above shipped the first real audio this app has ever had, inheriting that same mute-by-default with zero affordance telling anyone sound now exists — the founder's own real-device repro (a brand-new dream, total silence) confirmed this affects virtually the entire installed base, not an iOS-specific autoplay-policy issue. Fix (deliberately NOT flipping the default to unmuted — real browser autoplay policy would just fight an unmuted autoplay attempt with no gesture, trading one silent-failure mode for a dishonest one): a toast (`🎵 Tap for sound`) + a bounded pulse (`.sound-nudge-pulse`, `css/styles.css`, reusing the existing `pulse` keyframes already used by `.rec-dot` rather than inventing new motion) on the EXISTING mute icon — no new modal/banner, since the mute button itself already works and already shows the honest current state. Fires exactly ONCE per device (not per page/per video), gated on two new device-level `js/store.js` flags: `hasEverSetSoundPref()` (has `dreamtube_sound_on` ever been explicitly written — distinct from `getSoundPref()`'s own boolean, which can't tell "explicitly muted" apart from "never touched") and `hasSeenSoundNudge()`/`markSoundNudgeSeen()` (`dreamtube_sound_nudge_seen`, marked the instant the nudge is shown, not on any later tap). Wired into the only two surfaces that actually render a playable bed-eligible video: result.html's `render()` (fires whenever a video dream with a bed is shown) and explore.html's `videoObserver` callback (fires the moment a bed-eligible card actually becomes the playing one). home.html/start.html/style.html were checked (`MusicBed`/music-bed grep) and confirmed to only reference the feature in comments — no playable bed-eligible video renders there, so no nudge wiring needed | `test/sound-nudge-behavioral.test.js` (fires exactly once on a fresh device for a bed-eligible video on both result.html and explore.html; does not re-fire on a second visit/video, including across pages, once already seen on this device; does not fire once sound has been explicitly touched via a real mute-button tap or a pre-existing `dreamtube_sound_on` value even without a matching tap; never fires for a dream with no eligible bed, and confirms the "seen" flag stays false in that case so a later eligible dream can still nudge) | — |
| **Music-bed playback (CLIENT-SIDE ONLY, not server-side audio generation)** — tracker item `for-product-build-founder-approved-08-03-jlkjy9`. Shipped in two stages the same day: (1) style.html's Audio & music toggle went real, per-dream, default ON (Option B); (2) hours later, **founder simplification: "NO TOGGLE. Since the music beds are free, music is simply ALWAYS ON — every dream video plays with its style-matched bed, no user choice."** The toggle, its markup (`#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub`), and the `musicBedOn` field it wrote onto a draft/dream are now removed entirely — no disabled/placeholder UI left behind (matches the precedent this same tracker item already set when it removed the old `#music-style-row` chip picker outright rather than disabling it). **Current, permanent state:** a reusable, one-time-generated, style-matched AMBIENT MUSIC BED (`assets/music-beds/{cartoon,cinematic,anime,realistic}.wav`, served as plain static files — no build step, no new endpoint) plays for **every** dream that has a real video and a known style, full stop — `js/music-bed.js`'s `eligible()` no longer reads `musicBedOn` in any form, so a dream generated/published during either earlier stage (carrying a stale `musicBedOn:false`/`true`, or predating the feature entirely with no such field) is treated identically to a brand-new one — no lingering opt-out from old data. Never anything model-generated — the row above's `generate_audio:false` fix is completely untouched. Playback is a synced `<audio>` element (`js/music-bed.js`) that starts/stops with the video's own play/pause state and shares the video's own device-level mute preference (`DreamStore.getSoundPref`/`setSoundPref`) — wired into result.html's player and explore.html's feed (home.html's own ambient preview thumbnail and the admin-only media-library lightbox stay deliberately scoped OUT, see `js/music-bed.js`'s and this row's own detail below); **mute stays available in the player exactly as always.** **Implementation-choice tradeoff, stated explicitly (per the tracker item's own ask):** client-side playback, not muxed into the video file at generation — ships without a new ffmpeg/vendor dependency, but means a downloaded or externally-shared video file carries no audio at all; the bed only ever plays inside DreamTube's own player UI. Root-caused via systematic-debugging: a freshly created, `preload="none"` `<audio>` element's first `.play()` call, made synchronously inside an `IntersectionObserver` callback, intermittently (~1 run in 3 in headless Chromium) silently never started (no error/event at all) — fixed with a bounded, event-driven retry (the video's own recurring `timeupdate`, never a blind timeout) in both result.html and explore.html. **Known gap, scoped out deliberately:** `watch.html` (the retention-email "watch it" link) and `claim-dream.html` (the pre-signup claim moment) read a dream through server-side projection endpoints (`get-shared-dream.js`, the claim flow's own endpoint) that don't carry `style` at all — extending those would mean threading a new field through `dream-sync.js`'s private-dream schema and those endpoints' own response projections, a materially larger, separate change from the client-only surfaces this pass covers; both stay silent (unchanged from before this feature) until a follow-up does that work. | `test/music-bed-behavioral.test.js` (all 4 bed assets resolve as real static files at their production path; `js/music-bed.js`'s `urlForStyle`/`eligible` contract — a real videoUrl + known style is the ONLY requirement now, confirmed identical across a stale `musicBedOn:true`, a stale `musicBedOn:false`, and the field missing entirely; an unknown style or missing video still fails closed to no bed; result.html and explore.html both confirmed, via real Chromium, to play/pause/mute a bed in sync with the video for every eligible dream regardless of any historical `musicBedOn` value), `test/audio-toggle-behavioral.test.js` (confirms the toggle's markup — `#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub` — is fully absent from the DOM on both media types, not just hidden; `musicBedOn` confirmed never sent to `generate-video.js` and never present on a finished/regenerated dream record) | `watch.html`/`claim-dream.html` playback (see detail column — real, scoped-out gap, not silently missed); home.html's own ambient preview thumbnail and the admin-only media-library lightbox are deliberate silent-by-design exclusions, not gaps (see `js/music-bed.js`/explore.html's own comments for why) |
| `GENERATION_MOCK_MODE` / `GENERATION_TEST_DURATION` themselves | `test/generate-video-mock.test.js`, `test/video-status-mock.test.js` | — |
| `generate-image.js` / `image-status.js` | `test/generate-image.test.js`, `test/image-status.test.js`, `test/image-status-refund.test.js`, `test/image-generation-style-toggle-behavioral.test.js`, `test/image-generation-turn-into-video-behavioral.test.js`, `test/image-status-rehost.test.js` | — |
| `video-status.js` (polling, refunds, model-agnostic) | `test/video-status-mock.test.js`, `test/video-status-model-agnostic.test.js`, `test/video-status-refund.test.js`, `test/video-status-rehost.test.js` | — |
| **Generation-time/fail-rate instrumentation** — tracker item `for-product-track-avg-video-generation-t-2ci8ue` (founder ask, 2026-08-10): average generation time + a 3-minute-overrun alert, and the true fail/vanish rate, were completely unmeasurable before this build — live PostHog data showed `tokens_refunded` firing 0 times against 205 `video_created` over 60 days, and no generation-duration, poll-timeout, or vanish event existed at all. Instrumentation-only, no recovery-UX change (see `docs/EVENT_TAXONOMY.md`'s own `video_created`/`generation_slow`/`generation_failed` entries for the full event spec). Three additions, all in `js/store.js`: (1) `video_created` gains `duration_ms` (`startGeneration`'s hoisted `startedAt`, threaded through `finalizeDream`'s `extra`); (2) new `generation_slow` event, fired alongside `video_created` only when `duration_ms` crosses the founder's own explicit `GENERATION_SLOW_THRESHOLD_MS` (180000ms) threshold; (3) new `generation_failed{reason,mediaType,elapsed_ms,model}` event, fired from `startGeneration`'s outer `.catch` (covers every real fal error, a poll timeout (E301), a poll network failure (E302), and a submission-time failure — E1xx/E4xx/E303/E399 — deliberately SKIPPED for a stale/superseded edit's already-silently-ignored late failure) and separately from `attemptPrivateDreamSync`'s own terminal give-up (a new synthetic `E304: dream_sync_unconfirmed` reason — the observable shape of the P0 data-loss "vanish" bug, tracker item `for-product-p0-data-loss-founder-repro-0-6bzvv1`/commit `7b7828c`, recurring; this OBSERVES that shape, it does not re-fix the sync mechanism). Investigation finding on the `tokens_refunded` silence: no code-level wiring bug found in the refund→event pipeline (`refundAndReport`/`refundTokensOnce`/`lib/job-owners.js`'s fail-closed-by-design authorization/`lib/posthog-capture.js`'s production-safe test guard all read correctly wired) — most likely explanation is that `tokens_refunded` only ever fires for the narrow E205/E208 subset, while a real transport-level hiccup (E203/E204/E206/E207, never refund-eligible by design) still fails the user-visible generation the same way; `generation_failed`'s full reason-code breakdown will make this empirically answerable once real data accumulates, rather than requiring more guesswork now | `test/generation-instrumentation-behavioral.test.js` (real Chromium: `duration_ms` present and numeric on `video_created`; `generation_slow` boundary-tested just-under/just-over 180000ms, with matching `duration_ms`/style/mediaType/modelUsed; `generation_failed` on a real fal error (video E205 and image E508) with correct `reason`/`mediaType`/`elapsed_ms`/`model`; `generation_failed` on a client-side poll timeout (E301) with zero network calls made, proving `MAX_POLL_MS` alone trips it; `generation_failed` deliberately NOT firing for a stale/superseded edit's late failure (two-page race, mirrors the existing stale-completion-guard test's own setup); `generation_failed` firing `E304: dream_sync_unconfirmed` once `attemptPrivateDreamSync`'s full local retry budget is genuinely exhausted for a completed, still-unconfirmed dream) | Real-mode/production verification that these events actually land correctly-shaped in the live PostHog project is still pending (analytics-only additive change, so it didn't require the founder-preview gate before merge per this build's own scope — see `AGENT_POLICY.md`'s REAL-MODE section) — someone with PostHog dashboard access should confirm real volume/shape once this is live, and specifically watch whether `generation_failed`'s reason-code distribution actually explains the `tokens_refunded` silence noted above |
| **Error-status normalization for a fal-side status-check transport hiccup** (tracker item `for-product-netlify-cost-deploys-are-97--9vbpd4`, investigating an observed 17.6% request error rate): `video-status.js`'s E203/E204 and `image-status.js`'s E503/E504 (a non-JSON or non-OK response from fal's own STATUS-check endpoint — polled every 10s for every in-flight generation) used to propagate fal's raw upstream HTTP status as this endpoint's own response status; now normalized to 200 with the error in the JSON body, matching the pattern this same file already used one step later for E206/E207 (`video-status.js`) and E506/E507 (`image-status.js`) — a fal-side transport hiccup on our own upstream call was never a failure of this endpoint, and `js/store.js`'s `pollUntilDone` only ever reads the parsed body's `error` field, never `res.status`, so this is a pure metrics fix with zero client-visible behavior change | `test/video-status-refund.test.js` (new E203/E204 statusCode:200 assertions), `test/image-status.test.js` (new E503/E504 statusCode:200 assertions) | Root cause of the observed 17.6% figure is NOT confirmed — this sandbox has no access to real Netlify Observability/production traffic logs, so this fix addresses a real, code-confirmed inconsistency (verified against this same file's own already-normalized E206/E207/E506/E507 pattern) and a plausible contributor given call frequency, not a proven fix for that specific number. |
| Own-storage media re-host on completion (`lib/media-rehost.js`, `lib/media-status.js`, `video-file.mjs`, `image-file.mjs`), including the streaming-function `MAX_STREAMABLE_BYTES` size gate + oversized-redirect defense-in-depth, and the `force`-bypass option repair tooling uses — tracker items `for-product-owner-media-library-page-fou-1fwxaw`, `for-product-urgent-founder-repro-on-drea-uq3a36` (SIZE fix), `for-product-urgent-reopen-video-repair-p-cyp8np` (`force` option) | `test/media-rehost.test.js` (including the SIZE-gate and `force`-option cases), `test/media-status.test.js`, `test/media-file-functions.test.js` (including each file's own oversized-redirect defense-in-depth check), `test/video-status-rehost.test.js`, `test/image-status-rehost.test.js`, `test/dream-webhook.test.js` (the funnel webhook's own separate re-host path), `test/prod-smoke/media-serving.test.js` (real, currently-deployed `video-file.mjs`/`image-file.mjs` actually serving a real stored file — 200/302 + correct content-type — against a known-good production fixture key; the module-load-crash class of regression `test/media-file-functions.test.js`'s mocked suite structurally cannot see, per that file's own header comment — tracker item `for-product-add-a-prod-smoke-assertion-f-bqt2sh`) | Best-effort by design (see `lib/media-rehost.js`'s header comment) — a re-host failure is expected to leave the fal URL in place, covered explicitly, not a gap. No live `FAL_KEY`/real fal media in this sandbox to confirm actual byte-for-byte fidelity against a real fal response — mocked fetch responses only, same limitation this codebase's other fal-adjacent tests already accept. The new prod-smoke image-serving check is opt-in via `MEDIA_SMOKE_IMAGE_KEY` and currently skips — no verified real image key was available to hardcode; see that test file's own header comment. |
| Legacy broken-metadata repair sweep for own-storage media re-hosted BEFORE the SIZE fix shipped (`admin-repair-oversized-media.js`) — tracker item `for-product-urgent-reopen-video-repair-p-cyp8np` | `test/admin-repair-oversized-media.test.js` (auth, pagination across accounts→feed same as the backfill sweep, the cheap metadata-only-backfill tier vs. the real fal-refetch tier, `fal_result_gone`/`missing_or_invalid_source_operation_name`/`missing_fal_key`/`blobMissing` failure paths all reported not crashed, `dryRun` performing zero writes/fal calls, `targetDreamIds`/`targetKeys` filtering) | Never executed against real production data or a live `FAL_KEY` from this sandbox (no live `FAL_KEY`, no live Netlify deploy target, no real owner password available here) — every fal call and Blobs read/write is mocked. A human with live access (Manager or Ron) must run it against production and verify the specific records per the founder's own stated DONE bar before this tracker item can close; see that item's own notes for the exact request shape. |
| Edit-delta mechanism (`realign-dream-prompt.js`, the edit sheet) | `test/edit-mechanism-behavioral.test.js`, `test/realign-dream-prompt.test.js` | — |
| Edit/regen forming-state + stale-completion race guard | `test/edit-regeneration-forming-state-behavioral.test.js` | — |
| Edit/regen forming veil + reload persistence, live on a real deployed origin (the i2yzqo regression class) | `test/prod-smoke/session.test.js` (test 5) | — |
| Avatar generation (`generate-avatar.js`) | `test/generate-avatar.test.js`, `test/avatar-describe-behavioral.test.js` | — |
| Record-it voice capture + transcription (`transcribe-audio.js`, `transcribe-status.js`, real `MediaRecorder`/`getUserMedia`) | — | **Real gap.** `test/transcript-text-review-behavioral.test.js` covers reviewing/editing ALREADY-transcribed text, not the actual recording/transcription call chain. No test exercises `transcribe-audio.js`/`transcribe-status.js` directly. |
| Model cost categorization/reconciliation (`fal-cost-categorize.js`, `reconcile-fal-cost.js`) | `test/fal-cost-categorize.test.js`, `test/reconcile-fal-cost.test.js` | — |

## Dreams: storage, publish/unpublish, feed, social

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Local dream store / edit history | `test/dream-store.test.js` | — |
| Server-side private dream persistence (`dream-sync.js`) | `test/dream-sync.test.js`, `test/dream-sync-client-behavioral.test.js` | — |
| Post-generation sync confirmation + retry (`js/store.js#syncPrivateDreamBestEffort`/`retryUnconfirmedPrivateDreamSyncs`) — tracker item `for-product-p0-data-loss-founder-repro-0-6bzvv1`: a completed, token-charged generation could silently never reach the server-side private-dream store at all (present locally at best, absent on any other device/browser), with no retry and no way to tell later whether the write had landed. Fixed with a local `syncConfirmed` flag + retry-with-backoff (genuine transient/5xx failures) + a load-time sweep that retries any not-yet-confirmed dream the next time ANY page loads with a real `authToken` on file — no fresh login required, unlike `reconcilePrivateDreamsFromServer`'s own login-only catch-up push | `test/private-dream-sync-navigation-race.test.js` (a real transient server-side 5xx retried to success; a dream generated while `state.user.authToken` is null — a real, documented `attemptLocalLogin` fallback shape, not a contrived state — never even attempts a sync, then reaches the server automatically on the next ordinary page load once a real token is on file, with no explicit login/logout) | The write-side mirror of `nsbbg5`'s navigation-abort race (an in-flight `dream-sync` POST outrun by same-tab SPA navigation or the tab closing) was investigated directly but could NOT be reliably reproduced as a genuine server-side loss in this harness — Netlify Functions generally complete server-side once a request is dispatched, regardless of client fate — so it is not confirmed as a live mechanism, only defended against as a matter of course by the same retry/confirmation fix. See the build session's own report for the full investigation. |
| **Server-side durable persist at completion (E304 `dream_sync_unconfirmed` / P0 "vanish" backstop)** — the completed video reached the durable private-dream journal (`lib/dream-store.js`) ONLY via the client's own `dream-sync` (`js/store.js#attemptPrivateDreamSync`); for the FB/IG in-app-webview cohort (localStorage wiped between sessions) a client that closed/was wiped before its sync confirmed stranded the already-billed video in pending-dreams, never attached to the account (E304 was the single biggest technical failure code: ~24 users, 62 events / 7 days). Fixed at the SERVER: `dream-webhook.js` now, on completion (the `markReady`-won branch AND the already-`claimed` branch), resolves the pending record's email to a real account (`accountStore.getByEmail`) and — only when it resolves (a genuinely pre-signup/anonymous visitor is skipped, no username to key by) — persists the finished video into that account's durable store via `lib/dream-store.js#backfillServerDream`; `reconcilePrivateDreamsFromServer` pulls it back on the next load. Best-effort (wrapped, never breaks the webhook's own ack). **DEDUP is by `sourceOperationName`, not id** (the client mints a random `d…` id, the server persist mints its own operation-derived `serverDreamId`): `backfillServerDream` is insert-if-absent by operation (never downgrades a richer client copy); `upsertPrivateDream` now replaces-by-operation (a later client sync of the same generation collapses the server backstop into one record, client id + rich fields win) and collapses accidental dupes; `reconcilePrivateDreamsFromServer` matches by id OR operation, preserves the local id on an operation-merge, and re-stamps `ownerHandle` to the session handle on ingest (so a server record keyed by the canonical lowercase handle still shows under a differently-cased fresh-device login). **RECOVERY EMAIL (founder-approved scope extension, same root cause/file):** on iOS the FB/IG webview can't be force-escaped, so the recovery path is save-server-side AND email a link that opens the user's REAL browser. The "unwatched dream" nudge (`mark-generation-completed.js#maybeEnqueueUnwatchedNudge`) was enqueued ONLY client-side (`home.html` → `mark-generation-completed`), so a webview user who left first barely got it. `dream-webhook.js` now ALSO enqueues it server-side in the already-`claimed` branch (a signed-up user, no abandonment email, no `readyAt` — exactly the gap), reusing the exported `maybeEnqueueUnwatchedNudge` verbatim (no drift); convergence to exactly ONE nudge is the store's own `markPending` `onlyIfNew` CAS keyed by `operationName`, so a later client enqueue for the same generation is a no-op. Not called in the markReady-won branch (the abandonment email is already the recovery there, and `readyAt` is set). **Prompt delivery:** a recovery-flagged queue record uses a SHORTER, env-tunable pre-send floor (`RECOVERY_NUDGE_DELAY_MS`, default 3 min) instead of the standard `UNWATCHED_NUDGE_DELAY_MS` (default 7 min, unchanged) so the recovery email fires promptly for a leaver, while an ordinary client-enqueued nudge keeps the standard delay — the watched/active suppression (scan step-1 viewed check + the sender's own send-time viewed re-check) is untouched, so the shorter floor only changes how soon we CHECK-and-send, never whether a watched dream is suppressed | `test/dream-store.test.js` (`serverDreamId` determinism/id-shape; `backfillServerDream` insert + idempotency; both orderings — client-syncs-then-server-persists never downgrades, server-persists-then-client-syncs collapses to the client id with rich fields — proven to converge to exactly one; upsert collapses pre-existing dupes for one operation; null-operation dreams still dedup by id only; two operations stay two dreams), `test/dream-webhook.test.js` (a registered account's completion persists the durable dream with the exact field mapping — storyText→caption, engineered caption→promptText, unknown mood→null; the no-account and no-operationName cases persist NOTHING and never throw while the webhook still acks+notifies; the already-claimed branch persists too; end-to-end webhook-persist + client upsert of the same operation converge to one), `test/dream-sync-client-behavioral.test.js` (real Chromium: `reconcilePrivateDreamsFromServer` RESTORE of a never-locally-present server dream into the journal — the target vanish case; the ownerHandle re-stamp so a canonical-lowercase server dream shows under a differently-cased session handle; DEDUP of a server copy + this device's own local copy of the same operation under different ids into one journal entry, local id preserved, newer server fields merged). **Recovery-email enqueue** (`test/dream-webhook.test.js`): the claimed branch enqueues the unwatched-dream nudge server-side with no client `mark-generation-completed` call; a subsequent client enqueue for the same operation does NOT double-enqueue (the `onlyIfNew` CAS no-op), triggeredAt unchanged. **Short-delay** (`test/send-unwatched-dream-nudges.test.js`): a recovery-flagged dream SENDS after the short floor while a standard-enqueued dream of the same age still WAITS; a watched recovery dream is still suppressed on the short floor; `RECOVERY_NUDGE_DELAY_MS` tunes the recovery floor. The existing `test/unwatched-dream-nudge-enqueue.test.js` / `test/send-unwatched-dream-nudges.test.js` suites (owner/video-only/overlap-guard/claimed-no-readyAt decisions, and the end-to-end enqueue→send) stay green | Covers only the FUNNEL path (`start-pending-generation.js` → pending-dreams → `dream-webhook.js`), the "stranded in pending-dreams" cohort this targets — a PURE signed-in generation (`create.html` → `generate-video.js`, no pending record, no fal webhook) has no server-side `videoUrl` at its completion choke point (`mark-generation-completed.js`'s `verifyOperationCompleted` deliberately never fetches the result URL, and the fal call is out of scope to change), so that path stays covered only by the existing client sync/retry/beacon backstop above — a real, documented remaining gap for a later pass. No live `FAL_KEY`/real deployed webhook reachable in this sandbox (`AGENT_POLICY.md` REAL-MODE gate): the persist + dedup are proven end-to-end against the in-memory Blobs mock and real Chromium reconcile, but a real fal completion firing the deployed `dream-webhook.js` against real Blobs, plus the restore on a genuinely wiped real webview, remains to be confirmed post-deploy. |
| Admin diagnose/repair for a generation that was charged but never got a surviving dream record (`admin-diagnose-lost-generations.js`, `admin-repair-lost-generations.js`) — tracker item `for-product-p0-data-loss-founder-repro-0-6bzvv1`, cross-references `lib/job-owners.js` (who submitted a job) against `lib/dream-store.js` (who has a finished dream record) by operationName, per-account or swept across all accounts since a timestamp; repair re-queries fal's own result endpoint and writes a real recovered dream record (placeholder caption unless a human supplies `captionOverride`/`styleOverride` — job-owners.js never records the original caption/style) | `test/admin-diagnose-lost-generations.test.js`, `test/admin-repair-lost-generations.test.js` | Cannot be run against real production from this sandbox (no live `FAL_KEY`/owner password) — built and tested against mocked fixtures only; whoever has real production access must actually run it. `lib/job-owners.js` has no secondary index by account or date, so both tools page over its full key list — fine at this app's current scale, would need real pagination discipline (already built in, via `cursor`/`nextCursor`) at a much larger scale. |
| `audit-x7q4.html` — owner-only one-tap browser runner for `admin-diagnose-lost-generations.js`'s `mode:'sweep'` (built because the endpoint needs the founder's REAL password, which no agent sandbox holds — see that file's own header comment). **BUG FIX (08-03, same tracker item, follow-up sweep result 37/20/5):** the page used to dump the sweep's full-window `details` array (every job-owner record in the window, both `hasDream:true` and `hasDream:false`) into a single pane labeled just "Details," directly under the "Lost jobs" stat — reading exactly like a list of lost jobs, which is what produced this tracker item's own reported anomaly (a `hasDream:true` row appearing to be "in the lost list"). The backend's own `lostJobs`/`accountsAffected` counters were independently re-verified correct (never include a `hasDream:true` row — see the passing `admin-diagnose-lost-generations.test.js` suite); this was a front-end display bug only. Now splits the dump into three explicitly labeled sections — lost (needs review, open by default), unresolved (job owner's account not found), and found (has a dream, explicitly marked "NOT part of the lost count") — plus a lost-jobs mediaType (video/image) breakdown, since `generate-image.js`'s completion path (`js/store.js`'s `finalizeDream`/`syncPrivateDreamBestEffort`) is identical in shape to the video path, not a thumbnail/preview mechanism carved out by design — a lost image job is genuine data loss too, just a cheaper one (10 tokens vs 100, see `generate-image.js`'s `IMAGE_TOKEN_COST`) | `test/audit-lost-generations-page.test.js` (real Chromium run: a mocked sweep response with a deliberate mix of lost/found/unresolved rows across both mediaTypes proves a `hasDream:true` row is never shown in the lost pane and lands in the found pane instead, the lost-count/mediaType-breakdown stats match, an all-clear sweep renders "(none)" rather than an empty string, and a wrong password shows an inline error without ever revealing the results panel) | Still cannot be run against real production from this sandbox (no owner password) — the actual 37/20/5 sweep result and its per-job disposition (healed-by-the-day's-earlier-retry-fix vs genuine-loss vs unresolved) needs someone with the real founder password to re-run `audit-x7q4.html` itself; this fix only corrects how that person reads the result once they do. |
| Publish/unpublish (`publish-dream.js`, `unpublish-dream.js`) | `test/publish-dream.test.js`, `test/publish-unpublish-dream-auth.test.js`, `test/publish-dream-avatar-thumbnail-behavioral.test.js`, `test/publish-dream-avatar-validation.test.js` | — |
| Publish/unpublish, live, real feed, real probe dream | `test/prod-smoke/session.test.js` (test 6) | — |
| Shared feed (`get-feed.js`), windowing | `test/feed-windowing-behavioral.test.js`, `test/get-feed.test.js` (plain-handler-level: method/CORS, empty-store, Dream of the Day resolution) | — |
| **Public-response internal-field stripping** (`lib/public-feed-record.js`, tracker item `minor-like-dream-js-internal-dedup-marke-8qztvq`): `get-feed.js`'s `feed` array and `get-profile.js`'s `dreams` array used to serialize the raw feed-index record with no field whitelist, leaking three internal bookkeeping markers into every public response — `_lastLikeOp` (`like-dream.js`, retired from new writes by its CAS rewrite but still present on any older record), `_lostLikeRepairs` (`admin-repair-feed-likes.js`), `_lastCommentCountOp` (`lib/feed-comment-count.js`, still actively written). Both endpoints now run every record through `lib/public-feed-record.js` before serializing — a convention-based strip (any top-level key starting with `_`, matching this codebase's own consistent internal-marker naming convention — see that file's own header comment for the whitelist-vs-blocklist reasoning) rather than a hardcoded list of the three known field names, so a future internal marker following the same convention is covered automatically. Low severity (opaque marker values, no PII) — a hygiene fix, not a security fix; response shape otherwise unchanged. | `test/public-feed-record.test.js` (the helper in isolation, incl. a future-hypothetical `_`-prefixed field), `test/get-feed.test.js` and `test/get-profile.test.js` (endpoint-level: the three named markers plus every real public field a client reads both proven present/absent in the actual HTTP response body) | Only protects markers that follow the underscore convention — a future internal field added WITHOUT a leading underscore would still leak (an accepted residual risk, same low/no-PII severity as today, not a new failure mode — see the helper's own header comment) |
| Explore feed rendering, avatar fallback | `test/explore-avatar-fallback-behavioral.test.js` | — |
| Likes (`like-dream.js`) — as of tracker item `for-product-p1-urgent-fresh-signup-can-d-qhrrqy`'s follow-up (2026-08-07), the feed-index write goes through `lib/blobs-cas.js`'s REAL compare-and-swap write instead of `lib/blobs-retry.js`'s verify-loop mitigation, closing the residual "stale initial read racing an already-committed prior write" gap round-1 review flagged as still open on the original `fyqks0` fix (see that fix's own PR history) | `test/like-dream.test.js` (incl. a stale-CAS-read-is-atomically-rejected proof and a real independently-deserialized-reads concurrency test), `test/notify-likes-badge-behavioral.test.js` | — |
| Block / report (`block-user.js`, `report-dream.js`) | `test/block-user.test.js`, `test/report-dream.test.js`, `test/report-block-behavioral.test.js` | — (previously: admin-side moderation queue had no consumer/coverage at all — see the moderation review row below, which closed that gap) |
| **Moderation LOG** (blocked-generation prompt text; `lib/moderation-log-store.js`, `get-moderation-log.js`, `moderation-log-x7q4.html`; founder-approved 2026-08-14) — DISTINCT from the moderation REVIEW row below (that is user-*reported* published content via `report-dream.js`/`lib/moderation-store.js`; THIS captures the prompt text OUR OWN content filter REFUSED to generate, which previously reached only PostHog as a reason code, never stored as text). New durable Blobs store `dreamtube-moderation-log` (one pure-CREATE key per record, sortable zero-padded-epoch-millis key prefix so `list()` answers newest-first without reading bodies first — mirrors `lib/deploy-log-store.js`'s per-key/list shape, uses the same `@netlify/blobs` client; NOT the once-ever `blobs10` CAS the send-guards use). Record: `{ ts, reason, promptText, mediaType('video'|'image'), user, source, operationName }`. Bounded RETENTION (this deliberately stores explicit/sensitive user content): best-effort-pruned on every append to keep ≤500 records AND drop anything older than 30 days (a pure list+delete, no read-modify-write). Capture hooks are BEST-EFFORT (own try/catch, `console.error` only) at EVERY content-block emission point in `generate-video.js` and `generate-image.js` — the E116 pre-gate (reason `E116`) AND the fal-side content-policy rejection at submission (reason `content_policy_violation`, detected via this codebase's OWN controlled "flagged by the safety system" humanized wording, not fal's raw detail) — so a store failure can never alter or delay the block response the user gets. A new `source` payload field (PostHog `properties.source`, optional/untrusted) is parsed on both handlers and threaded through. Read endpoint `get-moderation-log.js` is owner-gated with the EXACT `normalizeEmail(OWNER_EMAIL)` vs `?email=` gate `get-moderation-reports.js`/`add-tracker-item.js` already use (no new auth scheme), optional `?limit=` default 200, records newest-first. Owner page `moderation-log-x7q4.html` mirrors `moderation-x7q4.html`'s owner-identity (`DreamStore.getCurrentUser`/`getAccountEmail` + server 403) and reused `css/styles.css` styling, read-only (no mutations), with plain-English reason labels (E116 = explicit sexual content, E207 = sensitive/possible-minor, content_policy_violation = flagged by fal safety). Linked from `admin.html` next to the moderation review link. | `test/moderation-log-store.test.js` (`keyFor`/`millisFromKey` round-trip + fixed-width lexicographic ordering, `normalizeRecord` defaulting + promptText cap, append+list, list newest-first + limit, retention: prunes beyond MAX_RECORDS keeping the newest, and prunes a >30d record on the next append via a seeded old key), `test/moderation-log-capture.test.js` (a blocked generation writes exactly one record: generate-video E116 → reason `E116`/mediaType `video`/promptText=caption/user=email/source passthrough, generate-image E116 → mediaType `image`, both handlers' fal-side content_policy_violation → reason `content_policy_violation`, an ALLOWED clean generation writes NO record, and a NON-content fal rejection writes NO record — classifier driven by `CONTENT_CLASSIFIER_MOCK_TIER`, fal rejection driven by a stubbed content_policy_violation detail), `test/get-moderation-log.test.js` (owner gate: wrong method → E1, missing `OWNER_EMAIL` → E2, non-owner/missing email → E3 with no record content leaked, owner → 200 newest-first, case/whitespace-insensitive owner match + `limit` respected) | Best-effort by design — a store/prune failure is swallowed so it never affects generation; the `user: email \|\| 'anonymous'` fallback's anonymous branch is defensively present but not reachable via these two handlers (the token-balance gate at E112/E412 fires first when there's no email, before the content gate). `moderation-log-x7q4.html` is an owner tool and was NOT browser-exercised here (no Playwright row) — it is a near-verbatim structural copy of the covered `moderation-x7q4.html`. No `test/prod-smoke/` coverage — same posture as every other row in this section without one. REAL-MODE note: no `FAL_KEY` in this sandbox, so a real fal content_policy_violation was not exercised against a live model — the fal-side capture is proven only against a stubbed rejection. |
| Moderation review page (`moderation-x7q4.html`, tracker item `for-product-build-the-moderation-review--sbf5ol`) — the first (and, until this, only) consumer of `get-moderation-reports.js`, which previously had zero callers anywhere in the app despite `report-dream.js` filing real reports into it. Owner-email-gated (same client-cached-email + server 403-on-mismatch pattern as `admin.html`'s paywall toggle, not `media-library-x7q4.html`'s heavier real-password gate — matches `get-moderation-reports.js`'s own existing gate shape, no new auth mechanism introduced), unpaginated full-list newest-first (matches that endpoint's own no-pagination design, "volume is ~zero" per the tracker item). New additive `handled`/`dismissedAt` fields on the stored report record (`lib/moderation-store.js`'s `markHandled` — a one-way signal, no un-handle control, modeled directly on `tracker-store.js`'s `reviewedAt`/`startedAt` precedent) and a new small owner-gated write endpoint `mark-report-handled.js` (same error-code-scheme/owner-check shape as `admin-paywall-toggle.js`/`update-tracker-item.js`). Linked from `admin.html` alongside the media library/post pack links. | `test/mark-report-handled.test.js` (owner-gate + 403/404/400 rejections, real persistence across a second `getReports` read, the one-way-signal idempotency — marking an already-handled report handled again never overwrites the original `dismissedAt` — and that `markHandled` never touches an unrelated report), `test/moderation-review-page.test.js` (Playwright: login-redirect, non-owner 403 shows the unauthorized state not the list, newest-first ordering, dream-report fields (dream id/caption/reporter handle/reason) vs comment-report fields (comment text/author, null reporter renders "anonymous") both rendering correctly from the same list, the Handled tag/no-button visual state vs. the unhandled Mark-handled-button state, a genuine persistence test — click Mark handled, reload the page entirely, confirm a fresh GET already shows it handled — and a failed-mark-handled negative-proof reverting the button with no false "handled" state) | Same accepted "fine at current scale" unpaginated-full-scan tradeoff as `admin-media-library-data.js`/`get-moderation-reports.js` itself — revisit only if report volume ever meaningfully grows past today's near-zero baseline. No automated action on a report (no auto-hide/auto-suspend) — marking handled is purely a founder bookkeeping signal, unchanged from `report-dream.js`'s own original "no automated content filtering ML" scope decision. No `test/prod-smoke/` coverage — same posture as every other row in this section without one. |

| Sharing (`share-dream.js`, `get-shared-dream.js`) | `test/share-dream.test.js`, `test/share-sheet-behavioral.test.js` | — |
| Channel republish license consent | `test/channel-republish-license-behavioral.test.js` | — |
| "Publish"→"Post" user-facing verb rename (founder copy experiment, 2026-08-08): every visible UI string saying Publish/Published/Publishing now says Post/Posted/Posting (`result.html` quiet link + confirm modal + toast, `home.html` day-0 card + "Consider posting a dream" + tip, `explore.html` empty state, `profile.html` stats/FAQ/delete-account list, `u.html` stats/empty states, plus the same-verb swaps in `terms.html`/`privacy.html`). Analytics event names (`video_published`, `publish_*`), the dashboard's `published` series, function/identifier names (`publish-dream.js`, `publishDream`, `isPublished`) and "Public"/"Private" wording are all deliberately unchanged. | Copy pins updated in place: `test/ui-behavioral.test.js`, `test/channel-republish-license-behavioral.test.js`, `test/home-day0-dream-card-behavioral.test.js`, `test/social-layer-v2-profile-behavioral.test.js` | No repo-wide sweep test guarding against the old verb reappearing (a naive sweep would false-positive on the many legitimate `publish*` identifiers — unlike `test/shop-pricing-copy-sweep.test.js`'s old-pack-id sweep, where the old strings should appear nowhere) |
| **Social Layer v2 slice 1 — "profile shell + share"** (`docs/SOCIAL_LAYER_V2_DESIGN.md`, tracker item `for-product-build-social-layer-v2-direct-34047c`, Direction B "Dream Journal Feed"): new public-profile page `u.html` (no login gate, like `explore.html`) — identity block, bio, PUBLIC/PRIVATE segmented toggle (owner-only, pure navigation between this page and `profile.html`), Share-this-profile button, full-width 9:14 dream cards with windowed/virtualized muted-autoplay video (mirrors `explore.html`'s own batching+hydrate/dehydrate mechanism, scoped down for this page's normal-scroll — not scroll-snap — layout), heart/comment-count(real as of slice 2, see the row below)/share action row, reserved-but-inert interpretation seam, and the "interpretations stay private" note. Backing endpoints: `sync-profile.js` (token-gated write, called on `profile.html`'s Edit-profile save AND on first publish — see `js/store.js`'s `syncProfileToServer`), `get-profile.js` (public GET, feed filtered by `ownerHandle` + `publishedCount` + `commentCounts`, filled in as of slice 2 — see the row below), `share-profile.js` (og:-preview endpoint mirroring `share-dream.js`, bare-avatar `og:image` per this slice's own scope — a branded/hosted image is an explicit later fast-follow, see that file's own "KNOWN LIMITATION" comment). New durable store `dreamtube-profiles` (`lib/profile-store.js`, one record per lowercased username, via `lib/blobs-retry.js`'s bounded retry). `profile.html` gained the bio field (Edit-profile sheet, IG-parity 150-char cap, local cache in `state.profileBioByUser`) and the PUBLIC/PRIVATE toggle (private view unchanged; PUBLIC navigates to `u.html?handle=<me>`) plus a server-truth "N published" count fetched alongside the pre-existing device-local "M created" count. `js/share-sheet.js` gained an optional `shareUrl`/`hideSave` override on `show()` so the profile-share button can reuse its existing navigator.share → clipboard → unsupported-toast ladder against `share-profile.js` instead of `share-dream.js`. `mock-profile-a-x7q4.html`/`mock-profile-b-x7q4.html` (the throwaway Direction A/B visual mocks) deleted — no longer needed once the real page exists. | `test/profile-store.test.js` (lib-level: round-trip, full-overwrite-not-patch semantics, "@"/case normalization, read-failure degrade), `test/sync-profile.test.js` (method/JSON/field validation incl. the 150-char bio boundary and avatar size/type validation, auth-token verification, the E6 owner-mismatch security case — a verified token for one username cannot write a different handle's profile — and that a rejected request never writes anything), `test/get-profile.test.js` (missing-handle 400, the three `exists` shapes — neither profile nor dreams / profile-only / dreams-only, i.e. published-without-ever-syncing-a-profile — newest-first dream filtering, case/"@"-insensitive handle matching, confirms no viewer-relative `likedByMe`/`mine`/`blockedByMe` leak into this bare public GET, CORS), `test/share-profile.test.js` (mirrors `test/share-dream.test.js`'s conventions: missing/nonexistent-handle redirects to `u.html`, real og:title/description/image incl. singular "1 dream" phrasing, avatar-vs-branded-fallback image selection, a malicious `displayName` never leaking unescaped into the response, host-header handling), `test/social-layer-v2-profile-behavioral.test.js` (real Chromium: `u.html`'s not-found/loaded/zero-dreams/fetch-error+retry states, a `<script>`-laden caption rendering as inert literal text never executing (XSS check), optimistic like with server reconciliation AND a rapid-second-tap-while-pending per-instance guard, a signed-out visitor's like tap routing to the signup nudge with no network call, owner-preview toggle visibility + both counts + PRIVATE navigating back to `profile.html`, `profile.html`'s bio field/counter/save round-trip and its own PUBLIC toggle navigating to `u.html?handle=<me>`, and a 44px-minimum tap-target check on every new interactive element this slice introduces) | Slice 3 (follow — `dreamtube-follows` store, Explore "Following" chip) is NOT built here, per the design doc's own three-slice build order. The branded share-preview `og:image` (design doc's open decision #2) is an explicit, non-blocking fast-follow, not built here — see `share-profile.js`'s own header comment for the real-world data-URI-vs-hosted-URL caveat this leaves in place until that follow-up. No `test/prod-smoke/` coverage — this is ordinary new-feature-behind-no-flag coverage, same posture as every other row in this section that has none. **Fixed 2026-08-08** (tracker `for-product-u-html-comments-teaser-click-u3lt6i`): `u.html`'s `.scroll-area` never reserved bottom clearance for the overlaid `.night-dock` bottom nav, unlike every sibling night-dock page (`result.html`/`profile.html`/`home.html`, each of which has its own `--nav-clearance` padding-bottom override) — the last card's comments-teaser could sit under the nav bar and swallow the tap. Fixed with `.scroll-area{ padding-bottom:calc(var(--nav-clearance) + 26px); }` in `u.html`, matching `result.html`'s own convention; covered by `test/social-layer-v2-profile-behavioral.test.js`'s "View all N teaser" test (previously the file's one failing test, now passing). |
| **Social Layer v2 slice 2 — "comments"** (`docs/SOCIAL_LAYER_V2_DESIGN.md`, tracker item `for-product-build-social-layer-v2-direct-34047c`, design doc's own state E; **one-level THREADING added on top of this, tracker item `for-product-founder-go-08-07-add-threadi-zb3j26`, founder-approved next increment — see the dedicated paragraph below**), public, first-ever hostile-UGC text surface in this app. New durable store `dreamtube-comments` (`lib/comment-store.js`, one record per dreamId -> comment array, `lib/blobs-retry.js`'s bounded retry, same append-only idempotency shape as `lib/moderation-store.js`). Endpoints: `get-comments.js` (public GET, newest-first), `add-comment.js` (token-gated POST, trims + rejects over 300 chars server-side independent of the client's own live counter, per-account daily rate limit via `lib/rate-limit.js`), `delete-comment.js` (token-gated POST, commenter-or-dream-owner permission model). `commentCount` denormalized onto the shared feed record (`lib/feed-comment-count.js`, mirrors `like-dream.js`'s own STALE-READ-CLOBBER-FIX idempotent-opId-marker shape almost verbatim, same accepted RMW race, same fail-open-on-exhaustion posture; `_lastCommentCountOp` used to leak into the public feed/profile response the same way `_lastLikeOp`/`_lostLikeRepairs` did — tracker item `minor-like-dream-js-internal-dedup-marke-8qztvq`, now fixed by `lib/public-feed-record.js`, see this doc's own row for it above). `report-dream.js`/`lib/moderation-store.js` additively extended with a `targetType:'dream'\|'comment'` field (defaults to `'dream'`, every pre-existing caller unaffected) so a comment can be reported with its own client-supplied snapshot (`commentId`/`commentText`/`commentAuthorHandle`), same trust level as the existing dream-report snapshot fields. New shared `js/comment-sheet.js` bottom sheet (reuses `.sheet-overlay`/`.sheet`/`js/sheet-dismiss.js` — deliberately a SINGLE scroll region via a `position:sticky` composer rather than a second nested scrollable list, so `js/sheet-dismiss.js`'s existing drag-vs-scroll `scrollTop` check needs no changes — see that file's own SCROLL SHAPE header comment), mounted in TWO places: `u.html` (each Dream Journal card's real comment-count action + top-2 preview lines + "View all N"/"Add a thought…" teaser, `loadCommentPreviewFor`) and `explore.html`'s feed-action rail (design doc's own open decision #3 — the spec's recommended default was followed, but this is flagged in the PR itself as still open for founder confirmation, not silently treated as settled). Per-comment overflow menu: Delete (own comment, or any comment if you're the dream's owner), Report, Block — all `esc()`'d through a local copy (not a `js/dream-cards.js` dependency, since `explore.html` doesn't load that file), `dir=auto` on comment text/composer, per-instance `currentGen` token guard on the optimistic post/delete flows (this codebase's documented recurring stale-async-callback bug class). **THREADING (one level deep, added after the above shipped)**: comments gained an additive `parentId` field (null for top-level, the parent's own id for a reply — same "opaque additive field" precedent `lib/moderation-store.js`'s own `targetType` addition set; see `lib/comment-store.js`'s own header comment). `get-comments.js`'s response stays one FLAT array (parentId just rides along) rather than a nested tree — `js/comment-sheet.js` groups client-side at render time into `.cs-thread`/`.cs-replies` blocks. `add-comment.js` accepts an optional `parentId`, validates it against a fresh store read (must name a REAL top-level comment on the SAME dream — E11 if not found, **E12 rejects a reply-to-a-reply outright rather than silently flattening it**, matching how this app's own Instagram-style reference behaves). `delete-comment.js` CASCADE-deletes a top-level comment's replies in the same `commentStore.deleteComments` (plural) write, rather than a "[deleted]" placeholder that preserves them — this codebase has no existing soft-delete concept for anything, so full removal stays consistent with every other delete here; deleting a lone reply removes only that row. `commentCount`/the sheet's own "Comments · N" title count the WHOLE tree (top-level + replies), not just top-level rows, so the badge never undercounts what opening the sheet actually shows. Client: a "Reply" text action renders only on top-level rows (never on a reply — the client half of the one-level-deep limit, E12 is the server half); tapping it scopes the SAME composer via a "Replying to @handle" banner with a cancel-back-to-plain-post control; a failed reply post deliberately leaves the composer still scoped to retry, a successful one exits reply mode. `u.html`'s own top-2 comment-preview line fetch (`loadCommentPreviewFor`) was additionally filtered to top-level-only (`!c.parentId`) so a bare out-of-context reply line never renders there — that page has no room for the sheet's own indented thread structure. | `test/comments.test.js` (`lib/comment-store.js`/`lib/feed-comment-count.js` unit coverage incl. append/delete idempotency and the floor-at-0/not-found-returns-null cases, `get-comments.js`/`add-comment.js`/`delete-comment.js` full validation + auth + rate-limit + the E9 owner-mismatch SECURITY case — a verified token for one username cannot post as a different handle — + the PERMISSION matrix for delete (commenter / dream owner / forbidden third party / dream no longer on the feed), a server-side XSS negative-proof confirming storage preserves raw bytes verbatim — escaping is the RENDER-side responsibility only — `get-profile.js`'s `commentCounts` now genuinely filled in, `report-dream.js`'s additive comment-target-type coverage, **PLUS threading**: a reply with a valid `parentId` is stored/returned with it and top-level comments default to `parentId:null`, `parentId` naming a nonexistent comment on the same dream -> E11, `parentId` naming a comment that exists but on a DIFFERENT dream -> E11 (no cross-dream id space), **SECURITY**: a reply-to-a-reply -> E12 rejected and nothing written, an empty-string `parentId` treated as top-level, deleting a top-level comment cascades its replies in one request and decrements `commentCount` by the true total removed while an unrelated top-level comment survives untouched, deleting a single reply removes only that row (parent + sibling reply survive), the DREAM OWNER's cascade delete removes replies regardless of who authored them, and `lib/comment-store.js`'s `deleteComments` (plural) unit-tested directly for multi-id-in-one-write), `test/comment-sheet-behavioral.test.js` (real Chromium against `explore.html`: signed-out "Log in to comment" with zero network call, a real XSS negative-proof — `<script>`/`<img onerror>` in both comment text AND a comment's handle render as inert escaped text and never execute — a per-instance-token-guard negative-proof (closing the sheet mid-post and reopening it for a DIFFERENT dream: the stale response must not contaminate the new sheet OR the original card's now-detached badge), optimistic post-with-reconciliation updating the card's badge via `onCountChange`, **a genuine failed-add-comment rollback test (mocked 500) asserting the optimistic entry is removed, the badge never bumps, and the error toast fires**, the empty state copy, the overflow menu's open/outside-close behavior and its Delete-only-on-your-own-comment visibility rule, a 44px tap-target check on the overflow button and Post button, an optimistic-success delete + block-hides-their-comments test, and a separate **genuine failed-delete-comment rollback test (mocked 500) asserting the comment reinserts at its ORIGINAL list position (not re-appended) and the error toast fires** — split out from a single prior test whose name claimed failure coverage its mock never actually exercised, flagged in review on this same branch; **PLUS threading**: tapping Reply scopes the composer and a successful post nests the reply under its parent both in the DOM (`.cs-thread`/`.cs-replies`) AND in the actual request payload's `parentId`, with the sheet's own title reflecting the total-tree count; cancelling a reply reverts to a plain composer with zero network calls; a reply row never renders its own Reply action (client half of the one-level-deep limit); an XSS negative-proof specifically on REPLY text/handle (same discipline as the top-level case); deleting a top-level comment removes its whole thread (replies included) from the DOM in one action with the title/badge dropping by the true total; deleting a single reply leaves its parent and sibling reply in place). `test/social-layer-v2-profile-behavioral.test.js` (u.html) and `test/ui-behavioral.test.js` updated in place for the now-real (no longer inert-placeholder) comment action on both mount points, PLUS two new tests covering `u.html`'s own independent `loadCommentPreviewFor` render path specifically (also flagged as a real gap in the same review — it has its own separate `esc()` call site from `js/comment-sheet.js`'s): a comment-count>0 case asserting only the top-2 NEWEST comments render as preview lines, a `<script>`-bearing comment renders as literal escaped text and never executes, the 3rd/oldest comment is correctly excluded, and tapping the "View all N" teaser opens the shared sheet; and a comment-count===0 case asserting no preview markup renders and `get-comments.js` is never even called. | Comment likes and notifications remain out of scope per the design doc's own "Out of scope" section (threading itself is no longer out of scope — see the paragraph above). Threading is deliberately capped at ONE level (a reply cannot itself be replied to, enforced both client- and server-side) — deeper nesting was explicitly not requested. `u.html`'s top-2 preview-line fetch is a plain best-effort `fetch()` per rendered card (not batched/cached across cards) — fine at this app's current per-profile dream-count scale, worth revisiting only if a profile's dream count grows much larger. No `test/prod-smoke/` coverage — same posture as every other row in this section without one. This is a UI-visible surface — per protocol §5 it is PREVIEW-GATED, held for founder look before merge, not auto-merged on tests-green alone. |

## Tokens, entitlements, payments

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Entitlements core (balance, daily claim, refunds, purchases, achievements) — as of tracker item `for-product-p1-urgent-fresh-signup-can-d-qhrrqy`'s follow-up (2026-08-07), the per-email balance record's five writers (`syncTokens`' init write, `claimDailyTokens`, `creditTokenPackAmountOnce`, `refundTokenAmountOnce`, `applyAchievementGrantOnce`) go through `lib/blobs-cas.js`'s REAL compare-and-swap write (`@netlify/blobs` 10.x via the separately-aliased `blobs10` package, `onlyIfNew`/`onlyIfMatch`) instead of `lib/blobs-retry.js`'s older read→mutate→write→verify mitigation — closes the "claim erasure"/signup-cascade race family (a claim landing while a delayed signup-seed write is in flight) at the root rather than narrowing the window. The three marker stores (`TOKEN_PURCHASES_STORE_NAME`/`REFUNDED_JOBS_STORE_NAME`/`ACHIEVEMENT_GRANTS_STORE_NAME`, keyed by paymentId/jobId/achievement-pair, not by email) deliberately stay on `blobs-retry.js`/`@netlify/blobs` 8.2.0 — a different key per event, so no same-account race exists there for CAS to close. | `test/entitlements-tokens.test.js`, `test/entitlements-daily-claim.test.js` (incl. a genuine end-to-end `Promise.all` regression racing a signup-seed read against a first-ever claim for the SAME brand-new email — the closest direct reproduction of the production incident this migration closes), `test/entitlements-refund.test.js`, `test/entitlements-token-purchases.test.js`, `test/entitlements-achievements.test.js`, `test/blobs-cas.test.js` (the shared CAS helper itself, in isolation: create/update paths, SKIP, a stale-read-is-atomically-rejected proof, genuine concurrent-writer races via `Promise.all`, exhaustion, `maxAttempts`/`retryDelayMs` behavior) | — |
| Daily claim (`claim-daily-tokens.js`, `get-token-status.js`) | `test/claim-daily-tokens.test.js`, `test/daily-claim-behavioral.test.js`, `test/token-daily-grant-copy-behavioral.test.js` | — |
| Daily-grant-amount copy staying in sync with `entitlements.js`'s live constants (`DAILY_CLAIM_AMOUNT`/`FIRST_CLAIM_BONUS_AMOUNT`/`TRIAL_DAILY_CLAIM_AMOUNT`/`INITIAL_GRANT`) — tracker item `recurring-bug-class-hardcoded-daily-gran-h6swgy`: this exact drift shipped FIVE separate times across two branches (style.html/result.html's old #modal-quota-body, shop.html's #shop-countdown, shop.html's #shop-cap-note twice with two different phrasings, profile.html's old countdown), each caught only by a manual review-round grep for that one known phrasing | `test/token-daily-grant-copy-behavioral.test.js` (per-instance behavioral coverage — real Chromium runs proving each live-amount copy site reads the live `dailyClaimAmount` off `getTokenStatus()`, not a hardcoded literal; note: shop.html's inline `#shop-countdown`/`#shop-cap-note`/cost-banner sites were removed in the 2026-08-10 copy trim, so shop.html's coverage is now on its auto-opened claim sheet, alongside style.html and result.html's purchase-sheet wait line), `test/daily-grant-copy-static-sweep.test.js` (additive, generic structural sweep — extracts the live constants straight from `entitlements.js`'s source text via regex, no eval, then walks every repo-root `.html` file's raw static source for digit-adjacent grant vocabulary and fails loudly with file/line/matched-text on anything that doesn't resolve to a live constant; proven to have teeth via 9 synthetic reintroductions of all 5 historical phrasings plus a literal on-disk reintroduce-and-revert of the shop.html #shop-cap-note bug). This sweep's own construction found and fixed a genuinely live SIXTH instance not covered by any per-instance test: `INITIAL_GRANT` was retuned twice in one day (220→170→150) but `profile.html`'s FAQ and `admin.html`'s owner reset-tool copy (two spots) still said "220" — corrected to 150 in the same PR | Documented known gap in the sweep test's own header comment: a stale number that happens to numerically coincide with a DIFFERENT live grant constant than the one that actually belongs at that call site is not caught (found while proving the sweep has teeth — literally replaying the old "100 every 24 hours" #shop-cap-note bug doesn't fail today because 100 now equals the live `TRIAL_DAILY_CLAIM_AMOUNT`) — a broad textual sweep with no per-element semantic awareness can't fully close this without becoming a per-instance-aware system, which is what the behavioral test file already is for the elements it knows about |
| Daily claim, live, real server-confirmed balance change | `test/prod-smoke/session.test.js` (test 3) | — |
| Auto-refund on failed generation | `test/auto-refund-behavioral.test.js` | — |
| Install bonus (`claim-install-bonus.js`) | `test/claim-install-bonus.test.js` | — |
| Dodo Payments checkout + webhook, incl. the pack ladder (pack099/199/499/999, $0.99/$2.99/$4.99/$9.99) and the pack099 starter one-time enforcement (E9) | `test/create-checkout-session-dodo.test.js`, `test/dodo-webhook.test.js`, `test/shop-pricing-copy-sweep.test.js` (repo-wide old-pricing/old-pack-id sweep) | A real end-to-end charge against the 4 new Dodo dashboard products is the only way to fully confirm the amount actually charged matches configuration — see `docs/PAYWALL_SETUP.md` |
| Out-of-tokens purchase sheet affordability guard (`PurchaseSheet.show`, founder bug 2026-08-10: the sheet appeared saying "needs 0 more" for a user who already had enough tokens). A single defensive guard at the top of `show()` — the one choke point every blocked-action call site routes through (`style.html`, `result.html` ×3, `home.html`'s E112/E412 catch) — suppresses the sheet entirely when `balance >= cost` (nothing to buy), running BEFORE `persistDraft`/mount. An optional `onAfford` proceed-callback is invoked instead when provided (wired at `home.html`'s E112/E412 catch as an honest "you have enough now — tap Create to try again" toast, deliberately not an auto-resubmit, since a server-vs-client balance disagreement had already occurred; wired at `style.html` and all three `result.html` call sites as the same `onAfford`, reusing their existing `proceedToGenerate`/`proceedWithEditDelta`/`proceedWithGenerateAgain`/turn-into-video proceed actions, added alongside the sibling fix below). Preserves the founder directive that the store STILL comes up whenever a user genuinely lacks tokens (`balance < cost` still opens it). **Sibling fix (tracker item `for-product-low-out-of-tokens-sheet-inli-uqt6or`, LOW, founder-reported 08-10):** the mid-sheet inline "Claim +N" path — a claim landing WHILE the sheet is already open that itself lifts `balance` to cover `cost` — used to re-render the same nonsensical "need 0 more" instead of closing/proceeding, since the original guard above only checked affordability at `show()`-call time. `claimInline()` (`js/purchase-sheet.js`) now re-checks affordability against the claim's own server-confirmed `data.balance` right after its existing balance/tokenStatus bookkeeping: if it now covers `cost`, the sheet closes (a non-dismiss close, same shape `handleImageFallbackTap` already uses — fires `out_of_tokens_choice: 'claim_unblocked'`, not `'dismiss'`) and invokes the SAME `onAfford` callback (reused, not a new option name) instead of falling through to `renderClaimOption()`/`renderPurchaseAmounts()`; a caller with no sensible proceed action to hand in still gets the sheet closed rather than left stuck. Still gated on the existing `myGen === currentGen` instance guard (a stale claim response must not navigate/act against a sheet reopened for a different blocked action in the meantime). A claim that does NOT bridge the gap is unchanged — the sheet stays open and `renderPurchaseAmounts()` still re-renders with the real recomputed shortfall. | `test/out-of-tokens-purchase-sheet-behavioral.test.js` (real Chromium: `balance > cost` and the `balance === cost` boundary both suppress the sheet AND fire `onAfford`, while a genuine `balance < cost` shortfall still opens it; the sibling fix: an inline claim on `style.html` that lifts a 0-token balance to cover a 10-token image cost closes the sheet and proceeds through `onAfford = proceedToGenerate` — proven by a real navigation to `home.html` and a real `generate-image` submission carrying the same persisted draft; the same claim-covers-cost path with NO `onAfford` supplied still closes the sheet rather than showing "need 0 more"; regression coverage extended onto the existing claimable-state test proving a claim that does NOT bridge the gap — 40→60 against a 100-token video cost — still leaves the sheet open with the real recomputed "40 more" shortfall, not a stale or zeroed number), `test/purchase-sheet.test.js` (pure `neededTokens` arithmetic incl. the never-negative case that produced the "0 more" copy) | — |
| Returning-buyer prefill (tracker item `for-product-repeat-purchase-friction-dod-b6pzs6`, founder decision 2026-07-28, option b): `dodo-webhook.js` stamps Dodo's own `customer_id` onto the entitlement record as `dodoCustomerId` (folded into `creditTokenPackAmountOnce`'s own atomic `blobsCas.casWrite`, not a separate write — see that function's doc comment); `create-checkout-session-dodo.js` only ever attaches the stored `customer_id` / enables `show_saved_payment_methods` after a REAL password check (`accountStore.verifyLogin`, rate-limited per-IP + per-identifier same as `create-session-transfer.js`) — any failure mode (no/wrong password, no account, rate-limited) falls back silently to today's bare-email checkout, never blocking the purchase; `shop.html`/`js/purchase-sheet.js` supply the password from `DreamStore.getCachedPassword()` when a signed-in buyer's browser has one cached locally | `test/create-checkout-session-dodo.test.js` (password-verification gate incl. rate-limit buckets), `test/dodo-webhook.test.js` (dodoCustomerId capture/redelivery/rotation/no-customer-block cases), `test/entitlements-token-purchases.test.js` (the dodoCustomerId fold-in surviving a genuinely concurrent `spendTokens` call), `test/shop-behavioral.test.js` (client sends `password` when cached, omits it otherwise) | Rescued/rebased 2026-08-08 from a stranded branch onto `entitlements.js`'s intervening CAS migration — the fold-in now rides `blobsCas.casWrite` instead of the original `blobsRetry.retryingWrite`; no real end-to-end proof against a live Dodo customer (same sandbox limitation as the row above) |
| **Dreamer Pass server-side conversion tracking** (tracker item `for-product-build-conversion-tracking-fo-k5ow3q`) — `dodo-webhook.js`'s `fireDreamerPassConversion` (sibling of the pack path's `firePurchaseConversion`) fires Meta CAPI `Subscribe` for the FIRST real Dreamer Pass charge (`handleDreamerPassPayment`, gated on `chargedAmount > 0` so a `$0` trial-start authorization never fires) and Meta CAPI `Purchase` for every later renewal (`handleSubscriptionEvent`'s `subscription.renewed` branch, so every renewal counts toward ad-attributed revenue, not just the first) — plus PostHog `purchase_completed` with `subscription: true`/`renewal: true\|false` on both. `value` is server-derived from the real Dodo object (`payment.total_amount` for the first charge, `sub.recurring_pre_tax_amount` for a renewal — never a hardcoded price constant; skips the fire entirely if neither resolves). `event_id`: the first charge reuses `payment.metadata.dreamtube_event_id` (a genuine 1:1 match with the checkout that minted it); every renewal mints a fresh id instead, since the subscription-level metadata id is static across a subscription's whole lifetime and reusing it per-renewal would make Meta's dedup silently collapse later renewals. The Dreamer Pass has been **LIVE FOR EVERYONE since 2026-08-09** (`shop.html`'s own "Dreamer Pass is LIVE FOR EVERYONE" comment, founder "flip for everyone" — corroborated by this registry's own row 152 below) and had NO conversion event of its own until this instrumentation — real customers have been able to subscribe, with that revenue going completely untracked in Meta CAPI/PostHog, since go-live. This closes that ALREADY-OPEN gap, it does not get ahead of a future one — see `docs/EVENT_TAXONOMY.md`'s "Subscribe / Purchase / purchase_completed (Dreamer Pass subscription, SERVER-SIDE)" entry | `test/dodo-webhook.test.js` (first-charge Subscribe fire incl. value-from-`total_amount` proof, `$0` trial-start firing nothing, redelivery not double-firing, renewal Purchase fire incl. value-from-`recurring_pre_tax_amount` and a fresh `event_id` per renewal — never reusing the static subscription-level metadata id, cross-event-type redelivery dedup (`subscription.renewed` + `payment.succeeded` sharing one `payment_id`), no-`payment_id` renewal firing nothing, unresolvable-amount renewal skipping the fire without guessing, and a PostHog/Meta failure never blocking the credit or the webhook's 200) | Same sandbox limitation as every other Dodo row here — no real end-to-end proof against a live Dodo subscription charge (no live `DODO_API_KEY`/webhook secret in this environment), so this closes the tracking gap in code but hasn't been exercised against a real Dodo-confirmed charge yet |
| **Dreamer Pass trial-state synthesis + `plan_changed` preservation** (money-path fix, 2026-08-10; trial-state synthesis: tracker item behind commit `9b91b0c`; `plan_changed` preservation: tracker item `for-product-low-subscription-plan-change-clww6d`) — Dodo has no native `trialing` subscription status: every Dreamer Pass trial (freetrial and trial50) reports `status:'active', trialing:false, trial_end:null`. `dodo-webhook.js`'s `handleSubscriptionEvent` SYNTHESIZES a `trialing` state on `subscription.active` (the trial-start event) from the subscription's own first-period end, which is what makes the 100/day daily-claim boost (`entitlements.isTrialActive`, gated on `status==='trialing' && now < trialEnd`) actually fire. `subscription.plan_changed`'s payload carries no reliable trial info of its own, so it can never RE-synthesize trialing — but it CAN still land while a subscriber is genuinely still inside an already-synthesized trial window (e.g. a mid-trial plan change), and before the `clww6d` fix this always wrote `status:'active', trialEnd:null`, wiping the boost early (a boost-amount-only bug — 20/day instead of 100/day for the rest of a still-legitimate trial; never a wrong charge or withheld grant, since those are separately gated by `payment_id`-keyed idempotent logic). The fix: `plan_changed` now reads the account's CURRENTLY STORED subscription state (`entitlements.getEntitlement`) and PRESERVES it via `entitlements.isTrialActive` (same "trialing AND trialEnd still in the future" gate the boost itself reads live) rather than re-deriving from the payload — a genuinely-elapsed trial still correctly transitions to `active` on its next `plan_changed`, and `notrial` (never stored as trialing) is unaffected | `test/dodo-webhook.test.js` (trial-state synthesis: `subscription.active` synthesizing trialing for freetrial/trial50 from `trial_period_days`+`next_billing_date`, the notrial product never synthesized, variant/first-period-length fallback discrimination, a redelivered `subscription.active` replaying a now-elapsed original payload not re-opening the boost, the trial50 abuse-hole guard now engaging, the real-first-billing renewal granting the 3,000 exactly once; `plan_changed` preservation: a still-future-trialEnd `plan_changed` preserving `trialing`+`trialEnd`+the 100/day boost, a genuinely-elapsed trial's `plan_changed` correctly transitioning to `active` (not indefinitely re-preserving a stale `trialing` status once its own `trialEnd` has passed, even though the stored status field itself was never flipped by an intervening event), a notrial `plan_changed` regression proof (never reads trialing), and an explicit-payload-trialing `plan_changed` staying on the pre-existing `explicitTrialing` path unaffected by the new carve-out) | Same sandbox limitation as every other Dodo row here — no real end-to-end proof against a live Dodo subscription's actual `plan_changed` delivery (no live `DODO_API_KEY`/webhook secret in this environment); the payload shapes fed to these tests mirror Dodo's documented/observed real shape (`status:'active'`, no native `trial_end`) but a real Dodo-confirmed `plan_changed` mid-trial has not been observed/replayed |
| **Dodo mode + subscription-status diagnostic** (`admin-diagnose-dodo-status.js`, branch `ship-dodo-status-diagnostic`) — READ-ONLY owner-gated tool answering two founder questions: (1) which Dodo mode is active right now (live vs test), resolved EXACTLY as `create-checkout-session-dodo.js` does it (`DODO_ENVIRONMENT \|\| 'live_mode'`) and ALWAYS returned even with no key / on a Dodo error; and (2) a subscription's true status from Dodo's API — `subscriptions.retrieve(id)` for an exact id, or `customers.list({email})` → `subscriptions.list({customer_id})` for an email lookup (the installed SDK v2.42.2 has NO email filter on `SubscriptionListParams`, only `customer_id`/`product_id`/`status`; `CustomerListParams` DOES expose `email`, so that's the resolution path), most-recent-first capped at 3. Maps down to ONLY non-sensitive status fields (subscription_id, status, derived `trialing`/`trial_end`, `trial_period_days`, next/previous billing dates, `recurring_pre_tax_amount`+currency+display, product_id, created_at) — no customer PII, no card/payment-instrument data. `trialing`/`trial_end` are DERIVED (Dodo's status enum has no `trialing` state and no `trial_end` field — computed from `trial_period_days`+`created_at`). NEVER calls any mutating Dodo method; the bearer token (`DODO_API_KEY`) never enters any response/error — only a `keyPresent` boolean, with `redactToken()` (mirroring `lib/meta-capi.js`) as belt-and-suspenders on the error path. `keyLooksLike` deliberately omitted: Dodo sets mode via an explicit `environment` arg, not a key prefix, so a prefix is not a reliable mode signal. Same real-password + `OWNER_EMAIL` gate and two-bucket rate limit as the sibling `admin-diagnose-*.js` tools; `admin.html` "Dodo — live/test mode + subscription status" card renders a big LIVE/TEST label + the mapped fields | `test/admin-diagnose-dodo-status.test.js` (SDK client injected as a recording spy via the function's `__setTestClientFactory` seam: owner-gating wrong/non-owner password → E5 without contacting Dodo, E1/E2/E3/E4 request-shape, activeMode reporting for both env states unset→live_mode and test_mode→test, retrieve happy path returning the mapped shape with derived trialing + no PII leak, non-trial trialing:false, subscriptionId-wins-over-email, email-lookup customers.list→subscriptions.list most-recent-first, empty-customer path, Dodo-API-error → 502 E8 with a SAFE message + the key redacted even when the SDK error embeds it, rate-limit both buckets, and the REGRESSION assertion that no mutating client method is ever called and the API key never appears in any response) | Cannot be exercised against real Dodo from this sandbox (no live `DODO_API_KEY`) — built/tested against a mocked SDK only; a founder with real Dodo credentials runs the `admin.html` card to get the actual live answer. Same "can't do this from the sandbox" class as the other Dodo/Resend rows here |
| Stripe checkout + webhook (legacy/dormant path) | `test/stripe-webhook.test.js` | Live/branch merge status of the Stripe vs. Dodo path should be reconciled against `dodo-merge-decision`-style tracker history before assuming both are equally "live" — this row is about test coverage, not production status |
| Shop UI (`shop.html`) — dark "Fastic-style" redesign (founder-approved mock `store-dreamtube-dark.html`, "STORE OK" 2026-08-09): welcome-offer hero (pack099), pack cards, self-hosted dream-still imagery strip (`assets/store/dream-*.webp`), smart back button (history-back → `home.html` fallback), out-of-tokens sheet's single contextual offer, purchase conversion tracking incl. the `starter` flag, AND the **Dreamer Pass** subscription hero now UN-GATED for all users (2026-08-09 founder "flip for everyone" — the earlier owner-only `PASS_OWNER_EMAIL`/`passIsOwner` UX gate is removed; hero visible + Start wired for every account) | `test/shop-behavioral.test.js` (incl. the un-gate regression: Pass hero visible to a non-owner and Start posts `plan:"dreamer_pass"`), `test/shop-purchase-conversion-behavioral.test.js`, `test/shop-checkout-confirmation-behavioral.test.js`, `test/shop-pricing-copy-sweep.test.js`, `test/out-of-tokens-purchase-sheet-behavioral.test.js`, `test/purchase-sheet.test.js` | The redesign is a VISUAL rebuild — 100% of the live Dodo wiring (PACK_INFO, pack SKUs/`data-pack`, pack099 one-time hero, Pass checkout, EmailVerifySheet prompt, every `track()` call) was ported unchanged into the new shell. The shop's old "best value" pack-card palette A/B test + `test/shop-palette-variant-behavioral.test.js` were retired 2026-08-02; `test/shop-first-purchase-callout-behavioral.test.js` was removed with the +50%-bonus callout it covered, replaced by the starter-pack coverage above |
| Owner top-up / bypass (`owner-topup-tokens.js`, `verify-owner-bypass.js`) | `test/owner-topup-tokens.test.js`, `test/owner-topup-target-behavioral.test.js`, `test/owner-bypass.test.js` | — |

## Interpretation / Chamber

| Feature/surface | Covered by | Gaps |
|---|---|---|
| `interpret-dream.js`, persona questions/readings. **As of the interpretation-retention-email build (founder-approved retention plan, 2026-08-11), a successful `mode:"reading"` ALSO records a durable server-side "this persona was read on this dream" marker** (`lib/interp-read-store.js`, keyed by the dream's server-issued `operationName` the client now passes — `js/store.js`'s `generateInterpretationReading` → `dream.sourceOperationName`), best-effort/awaited/never affecting the reading response — the signal the two retention emails need, since the pre-existing `interp_reading_shown` PostHog + localStorage signal is client-only and unreadable by a server-side sender. | `test/interpret-dream.test.js`, `test/interpreter-personas.test.js`, `test/send-interp-emails-batch.test.js` (the read-marker store's per-persona set accumulation + prefix-listing + unknown-persona rejection) | — |
| **Picker-always-first on open (founder-directed fix, tracker item `for-product-bug-founder-see-meaning-from-tecvrs`, founder repro 2026-08-11: choosing a specific persona, then reopening that dream's interpretation, silently re-showed the SAME persona's saved reading instead of the method chooser).** `js/interpret-experience.js`'s `open()` no longer branches its initial `phase` on `hasExisting` — a fresh open() ALWAYS lands on `picker`, even for a dream with one or more saved REAL-persona readings; the ✓-badged "already read" persona card (spec §3.1) remains the only, unchanged, no-network mechanism to reach a saved reading, just no longer the default landing state. One narrow, deliberate exception carried forward: a dream whose ONLY saved reading is the legacy pre-Wave-1 `classic` migration key (`js/store.js`'s `ensureInterpretationsMigrated`, no matching real persona and therefore no picker tile at all) still opens straight on `reading` — the classic fast path — since defaulting it to the picker would make that reading permanently unreachable rather than just re-orderable. `docs/INTERPRETATION_WAVE1_SPEC.md` §3.0 updated to match | `test/interp-analytics-behavioral.test.js` (revisit test rewritten: open() lands on the picker with the jung card ✓-badged and `#itp-reading-text` NOT present before any tap, tapping the badged card reaches the saved reading with zero network calls to `interpret-dream`, "Another take" still works from there; new sibling test proves a fresh never-interpreted dream is unchanged — still opens straight on picker with no badges; the pre-existing legacy-`classic`-migration test was left AS-IS and re-verified passing unmodified, since it already exercised exactly the classic-only fast path this fix preserves), `test/interp-voice-behavioral.test.js` (every "revisit with saved talmudic reading/audio" test — good saved audio, dead saved audio regenerate, the duplicate-listener regression test, the stale-caption regression test, the overlay-hidden-after-regenerate-success test, the both-tiers-dead test, and the sticky-preview-param test — updated to tap the picker's ✓-badged talmudic card first, proving the no-network revisit mechanism these tests actually depend on survived the phase-default change untouched) | The classic-only exception has no DEDICATED regression test proving the picker-default does NOT accidentally swallow it (the pre-existing legacy-migration test happens to cover the exact scenario, but was not rewritten to assert "and NOT the picker" explicitly) — low risk since that test would now fail loudly (waiting on `#itp-reading-text` that never appears) if the exception regressed, just not a maximally-explicit assertion of the negative case |
| Chamber dream strip (home embed) | `test/chamber-dream-strip-behavioral.test.js` | — |
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then a per-reading voice track + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`kokoroVoiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`). **VOICE VENDOR SWITCH, 2026-08-04 (tracker item `for-product-founder-decision-08-04-switc-cqveik`, founder decision — "much better" after listening to real samples of both):** the PRIMARY reading voice is now ElevenLabs Turbo v2.5 (`fal.run/fal-ai/elevenlabs/tts/turbo-v2.5`, voice `Brian`, `persona.voiceId`), reached through the same fal.ai infra/FAL_KEY this feature already used — measured 1257/1350/1321ms wall-clock at the exact hours Kokoro's own sync call was measured taking 18s+ under load. Captions come BUILT IN to that response (`timestamps:true` in the request) — the whisper leg is fully deleted for THIS tier. Kokoro (`am_onyx`, now `persona.kokoroVoiceId`, speed `0.9`) is UNCHANGED in mechanism but demoted to a two-tier FALLBACK (sync, then the legacy queue) — engages only if ElevenLabs sync fails/times out/throws, so a bad ElevenLabs day degrades rather than breaks the reading; kokoro's own whisper-based captions are unaffected on that fallback tier. Cost is founder-accepted by the choice (~$0.08/reading ElevenLabs vs $0.016 kokoro). `test/generate-interp-audio.test.js` extended: ElevenLabs-success-with-built-in-word-captions (asserts the exact request body incl. `timestamps:true` and no speed/stability override, and that ONLY the ElevenLabs endpoint is hit), ElevenLabs-succeeds-but-timestamps-missing/malformed degrading to sentence-level on the SAME tier (never falling back to kokoro just because captions failed — several malformed shapes covered), ElevenLabs-fails(http error)/throws(network) falling back to the kokoro sync tier with a full 3-endpoint cascade assertion, plus the pre-existing kokoro-sync-whisper-fails and both-sync-tiers-fail-to-queue tests re-verified still passing (now reached via an ElevenLabs-fails stub first, proving the fallback tiers still genuinely work, not just exist in code), plus dedicated `parseElevenLabsCaptions` unit tests for its defensive shape-parsing (accepts a `word`/`start`/`end` shape and this codebase's own whisper-chunk `{text,timestamp:[s,e]}` shape as a fallback convention, degrades to `null` on unrecognized/malformed input). **FLAGGED — NOT independently verified against a real ElevenLabs response** (no `FAL_KEY` in the build sandbox): the exact per-item field names inside the `timestamps` array (fal's own OpenAPI schema types it only as an untyped array, "for each word" — no field names published), whether timestamp values are seconds vs milliseconds (assumed seconds, matching every other timing convention already in this codebase), and whether ElevenLabs' DEFAULT pace (no speed/stability override sent, matching the founder's own approved sample) actually lands at a pace consistent with the 0.9 kokoro "feel" — a human with real listening/API access should verify both before/soon after this ships. `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff — kokoro's OWN legacy queue-fallback caption path, unaffected by the ElevenLabs switch — word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) — **NOTE: this row's "founder-preview gate (`?sagevoice=1`)" / gate-related language below predates the separate "GO WIDE" rollout (`main` commit `50cf2a3`) that removed that gate entirely; this voice-vendor-switch pass left that pre-existing drift as found, out of its own scope** | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Only `talmudic` ships voice at all this wave (scope: "sage persona first") — the other four personas have `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behave exactly like Wave 1 (silent, text-only). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Ships behind a founder-preview gate (`?sagevoice=1`) — code and assets are both ready; only the broad-rollout flip is still pending the founder's own preview walk. **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then Kokoro (`am_onyx`, speed `0.8`) voice + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`); the `?sagevoice=1` founder-preview gate this shipped behind is REMOVED as of `main` commit `50cf2a3` (founder "go wide" 2026-08-04) — voice eligibility is purely "does this persona have a voiceId," no switch to lose, live for every visitor | `test/generate-interp-audio.test.js` (validation, persona/voiceId gating, its OWN `interp-audio-ip` rate-limit scope, mock mode never bypassing a guardrail, PLUS — added 2026-08-04, tracker `cbdj3d` remaining-polish item — the sync-first happy path itself: a real one-round-trip kokoro+whisper success returning `done:true` with word-level captions and hitting `fal.run` never the queue, and the sentence-level degrade when whisper fails but kokoro still succeeds; this closed a real pre-existing coverage gap, since every prior test in this file exercised the LEGACY queue-submission path only, by relying on a stub response shape the sync leg would reject; **known remaining gap**: the whisper-SKIPPED-because-the-TTS-leg-already-ate-the-clock branch (`ttsElapsedMs > 11000`) has no dedicated test — not practically unit-testable without either a real 11+ second stub delay or mocking `Date.now()`, and out of scope for this pass since it predates it), `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff, word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Originally only `talmudic` shipped voice (scope: "sage persona first") — the other four personas had `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behaved exactly like Wave 1 (silent, text-only). **Superseded 2026-08-04** by the Chamber-makeover build (tracker `for-product-founder-spec-08-04-chamber-m-zf5ufo`, part 3 — see this file's own "Chamber makeover" row below for the full rename/tile-grid/interim-voice change): all five personas now carry the SAME `voiceId`/`introClipUrl`/`introVoiceUrl` values (jung/freud/gestalt/scientist temporarily borrow `talmudic`'s own `am_onyx` casting + intro assets verbatim, per the founder's own explicit instruction — see `js/interpreter-personas.js`'s header note), so every persona reaches a working voice stage today, not just The Sage; real per-persona casting stays a separate, later, founder-approved-per-persona effort (tracker `for-product-go-wide-follow-up-voice-the--bvqxvv`). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Live for every visitor (see the "REMOVED" note in this row's own Feature/surface column above). **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.
| **Chamber makeover — rename, tile-grid picker, interim voices for all personas** (tracker item `for-product-founder-spec-08-04-chamber-m-zf5ufo`, founder's own verbatim 3-part spec, 2026-08-04): (1) **NAME** — home.html's entry heading changed from the user-confusing "The Chamber" to "Dream Meaning" (recommended of 3 candidates — "Dream Meaning" / "What Your Dream Means" / "Interpretation" — all documented in a code comment at the rename site, home.html; result.html's `#interp-cta-btn` tooltip updated the same way for consistency); (2) **PICKER** — `js/interpret-experience.js`'s `renderPicker` replaced the horizontal swipeable `.itp-carousel`/dots with a 2-column `.itp-persona-grid` CSS grid (styled after style.html's `.style-card`, per the founder's own instruction to reuse that visual language) showing all 5 persona tiles at once, no scroll needed — the old carousel/dots CSS rules were deleted, not left dead; the 5th (odd) tile spans both grid columns generically off `personas.length % 2`; (3) **INTERIM VOICES** — `js/interpreter-personas.js`'s jung/freud/gestalt/scientist now carry the SAME `voiceId`/`introClipUrl`/`introVoiceUrl` as `talmudic` (see that file's own header note and the Speaking Sage row just above) | `test/chamber-makeover-behavioral.test.js` (new, real Chromium: home.html's hero heading is the new name not the old one, result.html's tooltip no longer says "Chamber," the picker container is a real CSS grid — not a horizontally-scrolling one — with the old carousel/dots fully gone, all 5 `.itp-persona-card` tiles render with the right `data-key`s, **all 5 tiles are simultaneously visible with no scroll needed at a real 390×844 mobile viewport** — the single most load-bearing assertion for part 2, both via Playwright's own per-element visibility check and a direct "does the last tile's bottom edge already fit within `#itp-body`'s visible bounds" geometry check, tapping a grid tile still advances the flow exactly like the old carousel cards did, and — one parameterized test per persona — each of the 4 non-Sage personas (jung/freud/gestalt/scientist) reaches a real working voice stage: the borrowed Sage intro clip mounts and can be skipped, `#itp-voice-stage` actually mounts (not `display:none`) for the reading, `interp_voice_play` fires carrying THAT persona's own key (not a hardcoded `talmudic`), and the plain Wave 1 text reading still renders alongside the voice — proving the "old silent/text-only experience" is genuinely gone for these personas, not just that the data field is set), `test/generate-interp-audio.test.js` (updated: the interim-voice change made `jung` a genuinely voice-eligible persona, so its old "rejects a persona with no voiceId" test — which used jung as its no-voiceId example — was repurposed into a positive "jung is now accepted" test, and the sibling mock-mode-validation test was moved onto a truly-nonexistent personaKey instead; the underlying `!persona \|\| !persona.voiceId` gate in `generate-interp-audio.js` is unchanged and still real defense-in-depth for any future persona added before it has voice casting, it just has no live real-persona example left to exercise that specific half of the OR today), plus the full pre-existing `test/interp-voice-behavioral.test.js`/`test/interp-analytics-behavioral.test.js`/`test/home-behavioral.test.js`/`test/interpreter-personas.test.js` suites re-run clean against these changes (all `.itp-persona-card[data-key="..."]` selectors those files already used were preserved verbatim, only the outer container/layout changed) | **Portraits are still placeholders for ALL FIVE personas, including `talmudic`/The Sage** — this build verified directly against `assets/interpreters/` that NO `.webp` portrait file exists for any persona today (only `assets/interpreters/intro/` — the Sage's intro video+voice — has real assets); every tile currently renders the pre-existing accent-gradient + initial-letter fallback (`.itp-persona-card-fallback`), which is the honest, already-graceful degrade this codebase established for a missing portrait, not a new gap this build introduced. Real portrait generation (fal.ai/flux, founder-pre-approved as a one-time negligible-cost spend) could not be executed from this build pass — no `FAL_KEY` in this sandbox (confirmed via `env`) — so all 5 image paths are wired and ready (`assets/interpreters/<key>.webp`) for whoever next has `FAL_KEY` access to actually generate and drop the files in; no code change will be needed once they exist. The interim-voice borrowing (part 3) means a non-Sage persona's intro clip will show the Sage's own visual likeness for now — the founder's own explicit, accepted tradeoff (see `js/interpreter-personas.js`'s header note), not an oversight. |
| **The interpretation offer DURING generation** (founder ask 2026-08-04, tracker item `for-product-founder-ask-08-04-offer-the--rlcai3`: "on processing while the dream generates, there is no offer to do the interpretation — there should be... entering the Chamber from processing must target the CURRENTLY-GENERATING dream... the reading can begin while the video is still rendering"). Three parts: (a) `home.html`'s forming room card gains `#d0-sage` ("✨ What does this dream mean?" — deliberately the same wording `result.html`'s completed-dream hero uses) in place of the dropped "Ready when your dream is" caption; (b) `chamberTargetDreamId()`'s priority inversion FIXED — it checked `completedDream` first, so any account with a generation in flight AND an earlier finished dream entered the Chamber on that older dream (the founder's exact report), and the pending branch was only ever reachable by an account that had never finished one; it now resolves pending-first, agreeing with `roomCardOwnedDreamId()`, which always had; (c) `InterpretExperience.notifyDreamResolved(pendingDreamId, realDreamId)` — a new public call `home.html`'s `onGenerationSettled` makes so an OPEN reading started against the synthetic `pending:<operationName>` id re-points at the real dream and swaps the finished video into the voice stage in place, reusing the pre-existing no-video path rather than adding a second one. **(d) Persistence-race fix, 2026-08-04** (independent-review finding, blocking, fixed on the same branch): (c)'s session-side swap turned out to be only half the mechanism. `js/store.js`'s `generateInterpretationReading` and `generateInterpAudio` both re-resolve `findDream(id)` at the moment their response arrives, and `findPendingDream` returned `null` the instant `pendingJob` was cleared — which `finalizeDream` does in the very same tick it creates the real dream. So a reading (or TTS track) still IN FLIGHT when the video landed had its persist-write skipped **silently**: it rendered fine in the live session (the swap deliberately tolerates the id change), and was simply gone on the next visit, with any real TTS spend behind it wasted. `generate-interp-audio.js`'s 18s `SYNC_TTS_BUDGET_MS` makes the audio call the likeliest of the two to lose this race. Fixed at the lookup rather than in each write path, so every `findDream()` caller is repaired at once: `findPendingDream` now falls through to a new `findResolvedDreamForOperation(operationName)` — the real, ownership-scoped dream whose `sourceOperationName` matches — whenever no live job matches, the same "use `sourceOperationName` to disambiguate a cleared `pendingJob`" move `isPendingJobStillCurrent` already makes for the same ambiguity. `notifyDreamResolved`'s header comment corrected in the same commit: it claimed the per-persona reading "was already persisted under the pending id," which was only ever true for writes that had already landed, never for one still in flight. Supporting: `js/store.js`'s `pendingDreamIdFor(operationName)` (the same synthetic id, computable after `pendingJob` is already cleared) and `interpret-experience.js`'s `stillTargetsDream()` staleness-guard tolerance for that one legitimate mid-session id change | `test/sage-during-generation-behavioral.test.js` (real Chromium, mobile viewport: the offer is visible/thumb-sized while forming and the old caption is gone; it steps aside in the ready state; tapping it produces a reading about the PENDING dream's text even with an older completed dream present; the Chamber pill does the same; the no-pending case still opens the completed dream; and the full swap-in — a reading mounted with no video, the video landing mid-session, the real url appearing on the SAME voice stage, the reading text not re-fetched, and the session's strip tile no longer naming a `pending:` id). "Which dream" is asserted via the `caption` actually POSTed to `interpret-dream` — the only unambiguous evidence, since an id-only assertion would pass against a stale session too. Plus, for (d), the persistence race itself: the reading response, and separately the TTS response, each held OPEN past the moment the video lands and `pendingJob` is cleared, then released — with the assertion made on the **durable record after a fresh page load**, never on what the still-open overlay is showing (the earlier tests all sequenced the race the other way, response-before-video, which is exactly why (d) shipped undetected). The reading case deliberately leaves its TTS call hanging forever: `generateInterpAudio`'s write block falls back to fabricating a `{ text, at, qa }` entry out of the text it was handed whenever `map[personaKey]` is missing, so it will otherwise resurrect a reading that was never actually saved and mask the very bug under test — that masking is real, it made a first draft of this test pass against the unfixed code. **Both (d) cases were verified as genuine regression tests: run against the pre-fix code they FAIL with exactly the reported symptom (record absent after reload while the live session rendered normally), and pass after the fix** | The swap-in path is covered with `GENERATION_MOCK_MODE`-style mocked `video-status`/TTS responses only — never against a real fal.ai generation (**this sandbox has no `FAL_KEY`**, see the note at the top of this file's own conventions). **REAL-MODE verification gate (`AGENT_POLICY.md`) — NOT satisfied, and explicitly deferred for this change rather than quietly skipped or vaguely assigned:** this sandbox has no `FAL_KEY`, no TTS credentials, no `.env`, and no Netlify CLI/deploy target (re-confirmed 2026-08-04 while fixing (d)), so a real fal.ai generation with real TTS latency cannot be run from here at all. **No real-mode pass is claimed for this row.** The honest cost of that deferral, stated rather than hand-waved: for (d) specifically it is narrow — the bug is entirely client-side (`findDream` resolution ordering against `localStorage`), and the mocked tests force the race *deterministically* by holding responses open, which exercises the ordering more reliably than real latency, which only produces it by chance. What real mode would still add is (i) confirmation that real TTS-vs-video latency actually lines up this way in production often enough to matter, and (ii) the iOS/autoplay/real-asset checks the gate exists for generally. A real-device walk of "start the reading, watch the video land under it" therefore remains a founder/Manager step — a known, accepted gap with a stated reason, not an open TODO this branch was supposed to close. The swap test asserts the real `src` lands on the SAME `<video>`, deliberately NOT that the bounce loop then advances `currentTime`: this sandbox's Chromium build has no proprietary codecs, so an mp4 never reaches `loadedmetadata` here at all (verified directly — `readyState` stays `0`, `duration` `null`), which is also why no pre-existing test in this repo asserts on real dream-video playback either; `nextBounceFrame`'s own math is unit-tested separately in `test/interp-voice-captions.test.js`. Also not covered: the same offer from `result.html` (that page has no pending-dream state of its own — it can only be reached with a real `?id=`), and the analytics events this adds (`home_day0_sage_tapped`, `interp_pending_dream_resolved`, `interp_pending_video_swapped_in`) have no dedicated assertions |
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then Kokoro (`am_onyx`, speed `0.8`) voice + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`), founder-preview gate (`?sagevoice=1`, sticky per browser — no broad on/off flip yet) | `test/generate-interp-audio.test.js` (validation, persona/voiceId gating, its OWN `interp-audio-ip` rate-limit scope, mock mode never bypassing a guardrail, PLUS — added 2026-08-04, tracker `cbdj3d` remaining-polish item — the sync-first happy path itself: a real one-round-trip kokoro+whisper success returning `done:true` with word-level captions and hitting `fal.run` never the queue, and the sentence-level degrade when whisper fails but kokoro still succeeds; this closed a real pre-existing coverage gap, since every prior test in this file exercised the LEGACY queue-submission path only, by relying on a stub response shape the sync leg would reject; **known remaining gap**: the whisper-SKIPPED-because-the-TTS-leg-already-ate-the-clock branch (`ttsElapsedMs > 11000`) has no dedicated test — not practically unit-testable without either a real 11+ second stub delay or mocking `Date.now()`, and out of scope for this pass since it predates it), `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff, word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Only `talmudic` ships voice at all this wave (scope: "sage persona first") — the other four personas have `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behave exactly like Wave 1 (silent, text-only). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Ships behind a founder-preview gate (`?sagevoice=1`) — code and assets are both ready; only the broad-rollout flip is still pending the founder's own preview walk. **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.
| **Cleanup: retired the split intro shape (`introClipUrl`/`introVoiceUrl`) + orphaned Sage assets** (tracker item `auto-cleanup-retire-the-now-unused-split-dwea7w`, dead-code removal only, no behavior change for any real persona — all five personas had already migrated to `introTalkingHeadUrl` before this landed, see the Speaking Sage / Chamber-makeover rows above) — `js/interpret-experience.js` simplified to talking-head-only intro handling: `shouldShowIntro`'s dual-field clause, `voiceStageHtml`'s split-shape `<video muted>`+`<audio>` markup branch, `startIntroPhase`'s muted-loop-backdrop branch, and the `introAudioEl`/`introTalkingHead`-ternary plumbing are all removed, leaving one talking-head-only code path; `js/interpreter-personas.js`'s header comment rewritten to describe the split shape as retired/historical rather than a currently-supported option. Orphaned assets `assets/interpreters/intro/sage-intro-reference.mp4` + `sage-intro-voice.wav` deleted (`git rm`) now that nothing references them — `test/result-play-intent-behavioral.test.js`'s unrelated "any small real mp4" fixture use was repointed to `assets/watermark-overlay-720x1280.mp4` first so its deletion wouldn't break that test | `test/interp-voice-captions.test.js` (the split-shape `shouldShowIntro` unit cases replaced with a single talking-head-only case covering both the has-intro and no-intro paths), `test/interpreter-personas.test.js` (new: every voice-eligible persona carries a non-empty `introTalkingHeadUrl` and NOT the retired `introClipUrl`/`introVoiceUrl` fields — guards against a future persona data edit silently reviving the split shape, since the runtime code that once handled it no longer exists to fail loudly on it), `test/interp-voice-behavioral.test.js`'s own header "ASSET STATUS" comment updated to describe the current per-persona talking-head casting instead of the retired split-shape handoff — its test bodies were unaffected (none exercised the split-shape branch directly, since talking-head has been the live path for every persona on this branch already) | — |

| **Scroll-lock release on page hide/bfcache-restore** (tracker item `for-product-bug-founder-video-08-07-resu-f4u8zy`, founder screen recording: result.html sometimes became entirely unscrollable — swiping down only rubber-banded into Safari's pull-to-refresh, intermittently). Root cause: `js/interpret-experience.js`'s `open()`/`close()` is the ONLY place on result.html that locks `document.body.style.overflow` (every other overlay — `.player-overlay`, `.sheet-overlay`, `.modal-overlay` — is `pointer-events:none` when closed, so a stuck `.open` class there is at worst a visibly-stuck overlay, never an invisible scroll block; confirmed by reading every such overlay's CSS/JS). `close()` already released the lock correctly on its three explicit triggers (topbar X, the post-reading Close link, the error-state Close link); the gap was every OTHER way the page can go away while the lock is held — most notably iOS Safari's native edge-swipe back gesture, which does not run this page's click handlers and can freeze the page into the back/forward cache (bfcache) with the lock still on, to be restored verbatim later with no fresh page load to ever reset it. Fixed with a `pagehide` listener that runs the SAME `close()` every explicit dismiss path already uses (so the lock, `#itp-root`'s `.open` class, and voice/audio teardown all release together, not just the CSS), plus a `pageshow{persisted:true}` listener as defense-in-depth that force-clears the lock unconditionally on any bfcache restore | `test/result-scroll-lock-behavioral.test.js` (new, real Chromium: sanity baseline that the topbar Close button already released the lock correctly; the actual regression — abandoning the overlay open with no explicit close, then dispatching `pagehide`/`pageshow{persisted:true}` (the same events a real bfcache freeze/restore fires, including via iOS Safari's edge-swipe back — the standard, portable way to test `pagehide`-driven cleanup since no engine offers a way to force a *real* bfcache cycle from automation, and this sandbox has no WebKit at all) — releases the lock and tears down the session; a `pagehide` with no active interpretation session is a verified no-op, not an accidental clear of `body.style.overflow` some OTHER unrelated feature had set. **Verified as a genuine regression test**: run against the pre-fix code, the middle test fails with exactly the reported symptom (`body.style.overflow` stays `'hidden'` forever); passes after the fix) | **Not verified against a real iOS Safari bfcache freeze/restore** — this sandbox has no WebKit engine at all (only Chromium is installed), and Chromium's own bfcache is known to be unreliable under DevTools Protocol/Playwright automation, so no test here forces a genuine bfcache cycle end-to-end; the fix and its test instead target the exact `pagehide`/`pageshow` events every engine (including iOS Safari) is documented to fire around such a cycle. A real-device walk (open the Chamber reading, edge-swipe back, edge-swipe/tap forward, confirm result.html scrolls) remains a founder/Manager real-mode check, not run from this sandbox. |
| **Ambient video/music-bed audio left playing under the Chamber** (tracker item `for-product-bug-founder-result-page-audi-zrjy48`, founder repro: on result.html, with sound ON, tapping "Dream meaning" (`#interp-cta-btn`) opened the Chamber but did not stop the ambient card video or its music bed — audio kept playing underneath, with no way to stop it). Root cause: the button's click handler called `InterpretExperience.open(dream.id)` directly with no pause step at all — every OTHER way this same `#result-video` element gets silenced (tab-visibility change, the fullscreen overlay's own close) does so by calling `video.pause()`, which cascades to `#result-music-bed` via the `'pause'`/`'play'` sync listeners this file already registers once at load (see the Generation section's music-bed row) — the interp handler was simply the one call site that never made that call. Confirmed result.html has exactly ONE `InterpretExperience.open()` call site (`#interp-cta-btn`); `home.html`'s two Chamber entry points (`#chamber-pill`, `#d0-sage`) were checked and are NOT affected — their own preview `<video>` (`#d0-video-el`) is permanently `muted`, so there is no audio to leak there. `js/interpret-experience.js` was also checked and never references `#result-video`/`#result-music-bed` at all (it owns entirely separate video/audio elements for the Chamber itself), so no hand-off/conflict state was needed — the fix is the one missing `video.pause()` call, immediately before `InterpretExperience.open()` | `test/music-bed-behavioral.test.js` (new: with the device sound pref genuinely on and both `#result-video`/`#result-music-bed` actually playing and unmuted, tapping `#interp-cta-btn` pauses both real media elements before/as the Chamber opens, asserted on their real `paused` DOM state — not just that the click handler ran — and the Chamber is confirmed to have genuinely opened, not silently no-op'd; **verified as a genuine regression test**: run against the pre-fix code it fails with exactly the reported symptom (`videoPaused: false`), passes after the fix) | Scoped to result.html only, per the tracker item's own repro and the confirmation above that home.html's entry points can't hit this bug (always-muted preview video) — not re-verified on a real device/real audio hardware (no REAL-MODE implication here: this is a pure client-side DOM/event-listener fix with no external service or mobile-specific gesture/autoplay behavior involved) |

## Retention / notifications / analytics

| Feature/surface | Covered by | Gaps |
|---|---|---|
| **"Unwatched dream" retention nudge** (founder-approved retention plan, piece 1) — a triggered email to a SIGNED-UP user whose dream video finished but who hasn't actually watched it. Trigger ("did X but NOT Y"): a verified VIDEO completion for a registered account, NOT watched within ~7 min, with a real thumbnail available. Server-side pieces: `mark-generation-completed.js`'s `maybeEnqueueUnwatchedNudge` (enqueue, alongside the video-ready push, gated to registered accounts + video + the same `pendingId`/`readyAt` pre-signup-overlap guard), `lib/unwatched-dream-nudge-store.js` (a `blobs10` CAS pending queue + a per-DREAM once-ever send guard `markNudgedOnce`), `send-unwatched-dream-nudges.js` (the `*/3 * * * *` scheduled scan: suppress if viewed, wait until the 7-min floor, thumbnail-gate, send, or drop past the give-up window), `lib/unwatched-dream-nudge-sender.js` (the guarded Resend send — embeds the user's own `storyText`/`caption` in the subject + body, the real thumbnail, and the RFC 8058 `List-Unsubscribe`/`List-Unsubscribe-Post` headers; as of 2026-08-10 it ALSO re-checks the viewed marker at the true send moment, before claiming the once-per-dream guard — belt-and-suspenders over the scan's step-1 check, closing the narrow step-1-to-send race and per the founder ask to "re-check the watched flag at send time, not just at enqueue time"; a viewed-race skip is treated as terminal by the scan's `TERMINAL_SKIPS`). Server-side VIEWED marker: `mark-result-viewed.js` + `lib/result-view-store.js`, POSTed from result.html's `openFullscreenVideo` (the ENGAGED watch, not the page render — see that endpoint's header comment for why) via `js/store.js`'s `DreamStore.markResultViewed`. **This is the SINGLE "your dream is ready to watch" email a signed-up user gets** (founder decision 2026-08-11: the separate once-ever-per-account first-dream email was retired entirely, so this nudge now covers EVERY unwatched dream, including a user's FIRST — the sender no longer has a first-dream-email overlap check). **No-overlap guarantee** (two "your dream is ready" sends could otherwise collide): the pre-signup abandonment email (`dream-webhook.js`) is excluded at enqueue (registered-account-only + `readyAt` guard); this nudge's own `markNudgedOnce` prevents two nudges for one dream. | `test/send-unwatched-dream-nudges.test.js` (the scan's full decision logic: a ready+unviewed dream sends exactly one email carrying the dream text in subject+body, the real thumbnail, and the List-Unsubscribe headers; a viewed dream suppressed; an unsubscribed recipient suppressed; a double-scan sends only once via the once-per-dream guard; a user's FIRST unwatched dream is nudged, not skipped — nothing suppresses it now the first-dream email is retired; too-soon waits; no-thumbnail waits then drops past the give-up window; no-RESEND_API_KEY defers then sends once configured), `test/unwatched-dream-nudge-enqueue.test.js` (enqueue side via `mark-generation-completed.js`: a signed-up video completion enqueues; no-account/image/no-owner/unverified do NOT; a funnel dream with `readyAt` set does NOT, without it DOES), `test/mark-result-viewed.test.js` (the viewed-marker endpoint + store: records + reads back, method/JSON/missing-field/rate-limit errors, idempotent double-mark), `test/unsubscribe-email.test.js` (the RFC 8058 one-click POST path added for this send's List-Unsubscribe-Post header — valid token suppresses + returns JSON 200, bad token 400 without suppressing) | **No live end-to-end proof against a real Resend inbox or a real Netlify scheduled-function invocation** (same sandbox limitation every other email/scheduled-function row in this registry accepts — no real `RESEND_API_KEY`/Blobs/Clockwork here). **Deliberate scope decision, flagged:** "viewed" is the FULLSCREEN watch, not merely loading result.html — because the render is also what captures the thumbnail, so marking viewed there would make "thumbnail available AND unwatched" unreachable and the feature would send zero emails (see `mark-result-viewed.js`'s header comment). A user who watches only the muted inline ambient loop (never opens fullscreen) is treated as unwatched — an accepted judgment call, bounded by the once-per-dream guard + the unsubscribe link. A user who NEVER loads result.html at all has no client-captured thumbnail, so that (real) retention segment is out of scope for this "thumbnail available" nudge and is dropped past the give-up window rather than sent thumbnail-less. The CTA links to `profile.html` (an authenticated, no-per-dream-token destination), not a deep link to the specific dream — a possible future enhancement. |
| **Interpretation retention emails — "Unread meanings" (flagship) + "No meaning yet"** (founder-approved retention plan, Phase 1, 2026-08-11) — two triggered email+push retention messages keyed PER DREAM by `operationName`. **Trigger 1 "Unread meanings":** the user READ 1-4 (inclusive) of the 5 interpretation personas on a dream (some but not all, not zero) → `lib/interp-unread-email-sender.js` (approved copy verbatim: "You read what &lt;READ_NAME&gt; saw. &lt;UNREAD_NAME&gt; saw something very different. Check it out." — persona display names from `js/interpreter-personas.js`, one read + one unread), subject "One reading you haven't seen 🌙". Re-fires per dream. **Trigger 2 "No meaning yet":** the user WATCHED the dream video (`lib/result-view-store.js` marker) but read ZERO of the 5 → `lib/interp-none-email-sender.js` (approved copy verbatim: "There's a hidden meaning in your dream '&lt;THEIR_DREAM_TEXT&gt;'. See what Jung would say.", dream text truncated), subject "There's a hidden meaning in your dream 🌙". THE SERVER-SIDE READ SIGNAL this depends on: `lib/interp-read-store.js` (one blob per `operationName::persona`, plain idempotent write — no read-modify-write set-clobber — reconstructed by prefix `list()`), written by `interpret-dream.js` on a successful reading (see the Interpretation/Chamber `interpret-dream.js` row). Both senders mirror `lib/unwatched-dream-nudge-sender.js`/`lib/winback-email-sender.js`: founder/test exclusion → email-suppression → once-per-dream `blobs10` CAS marker (`lib/interp-email-store.js`, two separate guard stores, release-on-failed-send) → Resend send via `lib/email-layout.js` shell with List-Unsubscribe headers + visible unsubscribe footer (`lib/unsubscribe-token.js`) + Idempotency-Key, FROM `dreams@dreamtube.life`, canonical origin via `lib/site-origin.js`. Each sender ALSO fires the matching Web Push (`lib/push-sender.js` new `INTERP_UNREAD`/`INTERP_NONE` types + `lib/push-dedup-store.js` per-dream dedup). CTA/link deep-links to the dream's interpretation view — `result.html?id=<dreamId>&interp=1`, a new auto-open param mirroring the existing `&play=1` (owner-gated, stripped from the URL on load). Delivery is ONE owner-gated batch endpoint `send-interp-emails-batch.js` mirroring `send-winback-batch.js` exactly: owner-gated (email===OWNER_EMAIL else 403), `previewTo` (default sends BOTH previews, optional `which:"unread"\|"none"\|"both"`), `limit` default 10 clamped to MAX 50, selection scanning accounts (`account-store.js`) × server-synced private dreams (`dream-store.js`) for eligible triggers, per-recipient CAS-guarded send, structured skip summary. Each sender's `sendPreview(event, previewEmail)` bypasses exclusion/suppression/marker with realistic sample persona names + dream text (email-only). **FOUNDER-REVIEW FIXES (2026-08-11, same branch):** (1) THUMBNAIL — both emails now ALWAYS render a media-banner `<img>` (`email-layout.js`'s new `mediaImage` helper): the recipient's OWN synced dream still (`dream.imageUrl`) where present, else a branded interpretation-themed fallback (`email-layout.js`'s `brandedFallbackImageUrl` → `/assets/chamber-sage.jpg`), never an empty slot; the none email previously rendered no image at all, and both previews now use the branded sample so the founder sees the slot. (2) CROSS-DEVICE LINK — the production CTA `result.html?id=<dreamId>&interp=1` used to dead-end to explore when opened on a device where the dream isn't in localStorage; `result.html` now hydrates the signed-in owner's server-synced private dreams (`DreamStore.reconcilePrivateDreamsFromServer`, newly exposed on the public API — GET `dream-sync`) and reloads to the original URL before falling through to explore, so the deep link resolves cross-device (preview CTAs point at `/home.html`, never explore, since a preview has no real recipient dream). (3) PER-USER CAP — `send-interp-emails-batch.js` sends AT MOST ONE interpretation email per user per run (flagship "unread" preferred over "none"), on top of the per-dream once-ever markers. **PERF + DIAGNOSTIC FIXES (2026-08-11): (4) TIME BUDGET** — the real send POST timed out (RemoteDisconnected: the per-dream `listPersonasRead` prefix-`list()` + `hasViewed` + sent-marker read × every dream × every account blew the platform execution limit). Fixed with a wall-clock budget on the selection scan (`SELECTION_BUDGET_MS`, env `INTERP_BATCH_SELECT_BUDGET_MS`, default 6s — stop scanning and send what was found) plus an independent send-loop bound (`SEND_DEADLINE_MS`, env `INTERP_BATCH_SEND_DEADLINE_MS`, default 9s); the summary reports `timeBudgetHit`/`sendBudgetHit`/`accountsScanned`. **The scan iterates the PRIVATE-DREAMS store's keys (dream-owners — the only population that can be eligible), NOT `accountStore.listAccounts` (whose N+1 get-per-account over ~253 accounts was itself eating the whole ~6s budget before any per-dream work — the real bottleneck countOnly exposed); email is resolved lazily per candidate.** An owner-gated `startOffset`/`nextOffset` cursor pages through all dream-owners across repeated calls (idempotent per-dream CAS markers make overlap harmless), so the drain completes independent of the platform timeout — proven live by paging countOnly across all 141 owners in 4 chunks with no timeout. Because every send is idempotent, repeated owner-triggered calls drain the queue in bounded chunks — the intended operating model. **(5) countOnly/dryRun diagnostic** — an owner-gated `{countOnly:true}` (or `dryRun:true`) mode returns `alreadySent:{unread,none}` (EXACT, from `interp-email-store.countSent` — two whole-store `list()`s, the source of truth for whether any send leaked) + `eligible:{unread,none}` (bounded scan) + `accountsScanned`/`timeBudgetHit`, SENDING NOTHING. **(6) DREAM TEXT in the flagship "unread" email** — it now leads with the recipient's own `storyText\|\|caption` (same helper + truncation the "none" email uses) so they know which dream, keeping the approved sentence intact. **AUTO-SEND — the "No meaning yet" ("none") email only (founder-approved 2026-08-12: "add the no interpretation email to also be automatically sent like not watched video"):** the "none" email now ALSO sends AUTOMATICALLY on a schedule, mirroring the "unwatched dream" nudge's store+scan shape — the flagship "Unread meanings" email ALSO now auto-sends on its own schedule (see the "Unread meanings auto-send" addition at the end of this cell); `send-interp-emails-batch.js` stays as the owner-gated MANUAL path for both emails (untouched). New pieces: `mark-result-viewed.js`'s `maybeEnqueueInterpNoneCandidate` ENQUEUES a candidate on the user's WATCH (best-effort, video-only, registered-account-only, identity resolved via `lib/job-owners.js`'s submission-time binding), into `lib/interp-none-queue-store.js` (a `blobs10` onlyIfNew CAS pending queue `{operationName,username,email,watchedAt}` — the pending half only; the once-per-dream send guard is the EXISTING shared `lib/interp-email-store.js` `markNoneSentOnce`, so the scheduler and the manual batch share ONE guard and can never double-send). `send-interp-none-nudges.js` (`*/3 * * * *` scheduled scan, registered in `netlify.toml`): for each candidate — if read-count ≥1 self-suppress+dequeue; if inside the ~10-min `DELAY_MS` floor (env `INTERP_NONE_NUDGE_DELAY_MS`) wait; if past `EXPIRE_AFTER_MS` (env `INTERP_NONE_NUDGE_EXPIRE_MS`, default 3d) drop; else resolve the dream by `sourceOperationName` and call `lib/interp-none-email-sender.js`'s `sendIfEligible` (the same guarded sender the batch uses, terminal vs transient skip handling like the unwatched scan), bounded to `MAX_PER_RUN` (env `INTERP_NONE_NUDGE_MAX_PER_RUN`, default 50) per invocation. **AUTO-SEND — the flagship "Unread meanings" ("unread") email too (founder-approved 2026-08-12, same directive):** the "unread" email now ALSO sends AUTOMATICALLY on a schedule, the direct sibling of the "none" auto-send above and reusing the SAME guarded sender (`lib/interp-unread-email-sender.js`'s `sendIfEligible`) the manual batch uses — so both the scheduler and the manual batch share the EXISTING once-per-dream guard (`lib/interp-email-store.js`'s `markUnreadSentOnce`) and can never double-send (whichever claims the CAS first wins, the other no-ops `already_sent` — confirmed BOTH directions). New pieces: `interpret-dream.js`'s `maybeEnqueueInterpUnreadCandidate` ENQUEUES a candidate on the user's FIRST reading (best-effort, video-only, registered-account-only, identity resolved via `lib/job-owners.js`'s submission-time binding — the enqueue fires right after the read-marker write on a successful `mode:"reading"`), into `lib/interp-unread-queue-store.js` (a `blobs10` onlyIfNew CAS pending queue `{operationName,username,email,firstReadAt}` — the pending half only; the once-per-dream send guard is the shared existing store). `send-interp-unread-nudges.js` (`*/3 * * * *` scheduled scan, registered in `netlify.toml`): for each candidate — re-check read count via `lib/interp-read-store.js`: count 5 (read all) self-suppress+dequeue, count 0 (marker vanished) drop, count 1-4 proceed; if inside the ~NEXT-DAY `DELAY_MS` floor (env `INTERP_UNREAD_NUDGE_DELAY_MS`, default ~20h — the plan's "next day" timing) wait; if past `EXPIRE_AFTER_MS` (env `INTERP_UNREAD_NUDGE_EXPIRE_MS`, default 4d — a few days past the delay) drop; else resolve the dream by `sourceOperationName`, pick a read persona (`readSet[0]`) + the first unread persona to name in the approved copy, and call `sendIfEligible` (terminal vs transient skip handling like the "none" scan — no `not_watched` here since the "unread" trigger is read-based, not watch-based), bounded to `MAX_PER_RUN` (env `INTERP_UNREAD_NUDGE_MAX_PER_RUN`, default 50) per invocation. | `test/send-interp-none-nudges.test.js` (the auto-send scan's full decision logic: watched+read-0+past-delay sends exactly one "none" email with the user's dream text + Jung hook + List-Unsubscribe header then dequeues; too-soon waits; read≥1 dequeued without sending; a dream already sent by the MANUAL batch is skipped+dequeued via the shared guard AND the reverse — scheduler-first then batch is a no-op; unsubscribed + founder/test-excluded suppressed; expired dropped; empty no-op; no-RESEND_API_KEY defers then sends once configured; the per-run cap bounds sends), `test/interp-none-nudge-enqueue.test.js` (enqueue-on-watch via `mark-result-viewed.js`: a signed-up video watch enqueues; no-account/image/no-owner do NOT; a re-watch does NOT reset the watchedAt clock), `test/send-interp-unread-nudges.test.js` (the flagship "unread" auto-send scan's full decision logic: read-1-4+past-delay sends exactly one "unread" email naming a read + an unread persona + the user's dream text + List-Unsubscribe header then dequeues; too-soon waits; read-all-5 dequeued without sending (self-suppress); read-0 dropped; a dream already sent by the MANUAL batch is skipped+dequeued via the shared guard AND the reverse — scheduler-first then batch is a no-op; unsubscribed + founder/test-excluded suppressed; expired dropped; empty no-op; no-RESEND_API_KEY defers then sends once configured; the per-run cap bounds sends), `test/interp-unread-nudge-enqueue.test.js` (enqueue-on-first-reading via `interpret-dream.js`'s `maybeEnqueueInterpUnreadCandidate`: a signed-up video reading enqueues; no-account/image/no-owner do NOT; a second reading does NOT reset the firstReadAt clock; AND a full `mode:"reading"` call through the real handler with a stubbed LLM enqueues end-to-end), `test/send-interp-emails-batch.test.js` (the read-store's per-persona set + prefix-listing + unknown-persona rejection; the "unread" trigger 1-4 selection + exact approved copy with a read + an unread persona named + the `&interp=1` deep link + one-click unsubscribe header; read-all-5 NOT eligible; the "none" trigger watched+zero-read selection + the user's own dream text + Jung hook; zero-read-but-not-watched skipped; per-dream dedup across two runs for BOTH triggers + at the send choke point directly; the hard cap; suppression + founder/test exclusion skipped; the matching push firing with the right `type` for a subscribed user under VAPID; owner-gating 403 / method 405 / missing OWNER_EMAIL 500 / invalid json+limit+which 400 / the 50 ceiling; `previewTo` sending BOTH by default and `which:"unread"` sending only one) | **REAL-MODE verification gate — NOT satisfied from this sandbox** (same limitation every other Resend/push/Blobs row here accepts: no real `RESEND_API_KEY`, VAPID keys, or Netlify Blobs backend). Proven at the LOGIC level (selection/cap/dedup/exclusion/copy/owner-gating + the exact Resend request built, incl. the media-banner image + the per-user cap), NOT a real delivered email or push. The cross-device link recovery IS proven in a real browser: `test/result-crossdevice-deeplink-behavioral.test.js` (a signed-in owner opening `result.html?id=…&interp=1` for a NOT-local dream hydrates it from a mocked `dream-sync` and opens the Chamber, not explore; a genuinely-missing dream still falls through to explore) — a systematic-debugging Phase-4 regression test (fails against the pre-fix synchronous local-only redirect). The founder/Manager fires the preview against production and reviews it, and runs the real 10-recipient batch himself — those are the going-live steps. The server-side read signal is only recorded from a REAL reading generation (piggybacked on `interpret-dream.js`), so it can't be exercised against a real dream from here; the `js/store.js` client wiring that passes `operationName` and the `result.html` `&interp=1` auto-open are covered by syntax + the existing `test/interp-analytics-behavioral.test.js` Chromium suite passing against the changed files, not by a real reading-to-marker round trip. |
| **Resend send-log diagnostic** (`admin-diagnose-resend-sends.js`, tracker item `for-product-urgent-email-dedup-integrity-iys5fb`, follow-up to `admin-diagnose-account-duplicates.js` once the founder confirmed the 08-01/08-02 multi-send accounts — moreecemiller/dtsexton18/demiaharchie49 — are genuinely distinct accounts, each owning its own distinct email, not shared-inbox duplicates): owner-gated, read-only, pages Resend's real `GET /emails` list-sent-emails endpoint (Bearer `RESEND_API_KEY`, `limit`/`after` cursor pagination, no server-side recipient filter) newest-first, stopping once it pages past a caller-supplied `sinceISO` or hits a `PAGE_CAP` (50 pages/5000 emails) safety bound (`truncated:true` reported honestly rather than silently returning a partial scan as complete), then counts/reports each requested recipient's actual Resend-confirmed send count + timestamps/subjects in `[sinceISO, untilISO]` — settles once run against real Resend data whether iys5fb's multi-send accounts are (A) an event-taxonomy over-count (PostHog telemetry over-firing for one real send) or (B) a genuine CAS-dedup bug in `lib/first-dream-email-store.js`'s `markSentOnce` not holding against real Netlify Blobs. `created_at` parsing is hardened against Resend's actual documented response format — raw Postgres `timestamptz` text (`"2026-04-03 22:13:42.674981+00"`: space separator, bare 2-digit offset), NOT strict ISO-8601 as every mock fixture had assumed pre-2026-08-08 — via an explicit normalizer (`parseResendTimestamp`) tried before falling back to the raw string, so both shapes parse correctly; a `created_at` that's still genuinely unparseable after that fails loudly as a new documented `E11 unparseable_timestamp` error rather than being silently skipped (which would previously make a real duplicate-send bug look identical to "confirmed clean" — the exact failure mode this tool exists to catch). A new `E12 resend_order_violation` also fails loudly if a fetched page's items are ever not in the newest-first order the pagination stop-at-`sinceISO` safety logic depends on. **One-tap `admin.html` runner added (tracker item `for-product-add-a-one-tap-admin-html-run-s58738`, branch `build-admin-resend-dedup-runner`):** a "Resend send-log diagnostic — email dedup check" card (owner username/password, a recipient-emails textarea accepting comma/newline-separated addresses, since/until fields) — general-purpose rather than hardcoded to the 3 `iys5fb` accounts, since this page has no way to resolve a username to its real email address without touching production account data; `sinceISO` defaults to `2026-08-01T00:00:00Z` (when the suspicious events started), `untilISO` defaults to the moment the page loads. Renders exactly what the API returns per recipient (count + each send's timestamp/subject) plus a scan-coverage footer (`totalEmailsScanned`/`pagesFetched`/truncation warning). | `test/admin-diagnose-resend-sends.test.js` (auth/rate-limit/request-shape parity with sibling admin-diagnose-*.js tools; the actual scenario — multiple genuine sends to one recipient correctly counted with timestamps/subjects returned; multi-recipient filtering; case-insensitive recipient matching; a send outside `[sinceISO, untilISO]` excluded even on an otherwise in-range page; pagination correctly stopping once it pages past `sinceISO` without fetching further pages, proven via an intentionally-unreachable page 3; the `PAGE_CAP` safety bound triggering `truncated:true` with an honestly-partial `totalEmailsScanned`; stopping cleanly on Resend's own `has_more:false` with no truncation; a real Resend API error and a network-level fetch failure both propagated honestly as 502 E10, never swallowed into an empty/partial result; E9 when `RESEND_API_KEY` is unconfigured, never calling `fetch`; auth failure never calling Resend; Resend's REAL documented `created_at` format — the literal `"2026-04-03 22:13:42.674981+00"` sample from Resend's own docs, not a clean ISO-8601 mock — parsed to the exact correct millisecond, cross-checked against an independently-computed `Date.UTC(...)` value via a 1ms-wide match window; a plain ISO-8601 `created_at` still works too; a genuinely-unparseable `created_at` fails loudly with 502 `E11` rather than silently reporting `count:0`; a page whose items violate the newest-first ordering assumption fails loudly with 502 `E12`), **`test/admin-resend-dedup-card-behavioral.test.js`** (new, real Chromium against `admin.html`'s owner gate: since/until fields pre-filled with the sensible default window on load; a comma-AND-newline-separated recipient list is parsed correctly into the request body alongside the date window; a successful response renders per-recipient counts, every send's timestamp+subject, and the scan-coverage footer; `truncated:true` surfaces the truncation warning; a wrong password (E5) shows a plain-English error and never reveals the result panel; a dynamic-detail error code (E10) surfaces its real detail text via the same "strip only the leading code prefix" house pattern the Dodo/reset cards already use; empty-recipients and over-20-recipients are both blocked client-side with zero requests sent) | **REAL-MODE verification gate — NOT satisfied, cannot be from this sandbox:** no real `RESEND_API_KEY` exists here (same "can't do this from the sandbox" class as this registry's other Resend-dependent rows) — this tool is built and tested against a mocked Resend API only; the actual founder-run diagnosis (fetching real Resend send-log evidence for the three named accounts, now via the one-tap card rather than a raw POST) is a step only someone with real Resend dashboard/API access can execute, same posture as `admin-diagnose-account-duplicates.js`'s own gate (`for-ron-run-the-duplicate-account-check--ypmr7r`) before it. No pagination beyond `PAGE_CAP` (50 pages) is ever attempted regardless of how wide a date range is requested — an intentional cost/hang guard, not an oversight; a founder investigation spanning more than ~5000 total Resend sends in the account's history would need to narrow the date window rather than widen the cap. The card cannot resolve the 3 `iys5fb` usernames (moreecemiller/dtsexton18/demiaharchie49) to their real email addresses itself — the founder pastes those in himself before running it. **NOTE (2026-08-11):** the first-dream email this diagnostic was built to investigate has since been retired entirely (`lib/first-dream-email-store.js`/`-sender.js` deleted), so branch (B) — a CAS-dedup bug in that store — is now moot; the tool itself is general-purpose (any recipient/date window against Resend's send log) and is retained. |
| **Unsubscribe + email suppression + redesigned template** (tracker item `for-product-email-redesign-unsubscribe-l-16ysmp`, founder ask: "make the email content look better + add unsubscribe", founder-approved and shipped): `unsubscribe-email.js` (GET endpoint, verifies `lib/unsubscribe-token.js`'s per-email HMAC token, records the unsubscribe via `lib/email-suppression-store.js`), scoped to the two retention/marketing senders in scope today — `lib/unwatched-dream-nudge-sender.js`'s `sendIfEligible` and `dream-webhook.js`'s `sendReadyEmail` — both now check `isSuppressed` before sending and carry a real unsubscribe link; both templates rebuilt on the shared `lib/email-layout.js` night-aesthetic shell (logo header, dark card, unsubscribe+address footer), whose footer mailing address is the founder's own real address ("10 Dgania, Herzliya, Israel", CAN-SPAM-required), rendered deliberately small/faint (his own explicit ask) via a dedicated `ADDRESS_COLOR` a step fainter than the unsubscribe line's own text color. **Update (2026-08-10, "unwatched dream" nudge round):** the VISIBLE unsubscribe LINE itself is now also small/faint (founder "small faint font" ask) via `email-layout.js`'s dedicated `UNSUBSCRIBE_FONT_SIZE` (11px) + `UNSUBSCRIBE_LINK_COLOR` (a step down from the brighter `textMuted` it used before) — applies to every email using the shared shell; and `unsubscribe-email.js` now ALSO accepts the RFC 8058 one-click POST (not just GET), so the `List-Unsubscribe-Post` header the newer retention sends carry actually resolves. | `test/unsubscribe-token.test.js` (deterministic signing, cross-email/tampered-token rejection, secret-rotation changes the token, graceful no-env-var fallback — confirms `UNSUBSCRIBE_TOKEN_SECRET`'s fallback to `RESEND_API_KEY`/a hardcoded dev constant works correctly with no dedicated env var ever set), `test/email-suppression-store.test.js` (suppress/isSuppressed, idempotent double-suppress, normalization, unrelated emails unaffected), `test/unsubscribe-email.test.js` (valid token unsubscribes and is idempotent, invalid/tampered/cross-email token is rejected WITHOUT suppressing, missing-params/wrong-method errors), `test/send-unwatched-dream-nudges.test.js` (a suppressed recipient is actually skipped end-to-end with no Resend call; the nudge carries the logo + List-Unsubscribe headers + a real unsubscribe link), `test/dream-webhook.test.js` (same suppression-skip proof for the second in-scope sender, redesigned template asserts logo + unsubscribe link present, rest of the webhook flow still completes when the email itself is skipped) | Explicitly OUT of scope, by the tracker item's own framing (transactional, not marketing — must never be suppressible or carry an unsubscribe link): `request-password-reset.js`, `lib/verification-email-sender.js` (used by `register-account-passwordless.js`/`resend-verification-code.js`), `submit-support-message.js` — all left on their prior plain (non-redesigned) template, unchanged. `UNSUBSCRIBE_TOKEN_SECRET` is still not a dedicated env var in the real Netlify environment — confirmed as an accepted, working default (not a blocker): the fallback chain (`RESEND_API_KEY`, then a hardcoded dev constant) still produces a real, unguessable per-email token either way, see `lib/unsubscribe-token.js`'s own header comment. No live end-to-end proof against a real Resend inbox (same sandbox limitation every other email-sending row in this registry already accepts). |
| Retention-email real first-frame thumbnail (`upload-dream-thumbnail.js`, `js/store.js`'s `DreamStore.saveThumbnailBestEffort`, result.html's capture IIFE, consumed by `lib/unwatched-dream-nudge-sender.js`'s `buildHtml`/`absoluteImageUrl` + `send-unwatched-dream-nudges.js`'s scan) — tracker item `for-product-dream-ready-email-real-first-qr9fbj`. The client captures a real video first-frame still and syncs it as the dream's `imageUrl`; the "unwatched dream" nudge correlates it by `sourceOperationName` and renders it as the email's real `<img>` (a dream whose thumbnail never syncs is dropped from the nudge, never emailed thumbnail-less — see that scan's header comment). **NOTE (2026-08-11):** the earlier THUMBNAIL-GATED-SEND scaffolding for the separate first-dream email (`lib/first-dream-email-pending-store.js`, `send-pending-first-dream-emails.js`, `lib/first-dream-email-sender.js`) was removed when the founder retired that email — the nudge's own thumbnail gate is now the only such path. **ROOT-CAUSE FIX (2026-08-07, tracker item `for-product-p1-regression-evidence-found-7lbwkx`, founder-confirmed regression: he had never once received a real thumbnail):** the capture IIFE's canvas draw used the VISIBLE, playback `#result-video` element, which never set `crossOrigin` — and `dream.videoUrl` is routinely cross-origin (video-file.mjs 302-redirects to fal's own CDN past an 18MB threshold, the common case for a real veo3.1 720p/8s/audio-on clip). Drawing a cross-origin media element with no `crossOrigin` attribute ALWAYS taints the canvas and throws on `toDataURL()`, regardless of whether the remote server sends CORS headers — the old code's catch block silently swallowed this on essentially every real dream. Fixed by capturing from a SEPARATE, detached `<video>` element (created fresh per attempt, `crossOrigin='anonymous'` set before `.src`, never inserted visibly, bounded 6s timeout, always cleaned up) instead of the real playback element — `#result-video` itself is never touched (no `crossOrigin`, no src changes), so playback can never regress even if fal's CDN turns out not to send CORS headers. | `test/upload-dream-thumbnail.test.js` (endpoint validation, authToken identity, per-account key namespacing, idempotency, and proving the write is served back out unchanged by `image-file.mjs`), `test/send-unwatched-dream-nudges.test.js` + `test/email-public-origin.test.js` (a synced real thumbnail renders as the real `<img>` in the nudge, resolved relative→absolute against the canonical origin even on a header-less scheduled invocation, never a broken/empty-host src — the documented blank-square regression guard), `test/funnel-real-chain-behavioral.test.js` (the real-browser-driven, real-handler end-to-end chain seeds the synced thumbnail, forces the nudge past the 7-min floor, and runs the scan directly before asserting the real Resend send — proves the deferred send still genuinely fires off the REAL chain), **`test/result-thumbnail-crossorigin-capture-behavioral.test.js`** (new, 2026-08-07, real Chromium + two real standalone cross-origin HTTP servers on `127.0.0.1` serving a real decodable video file — not mocks: (1) no `Access-Control-Allow-Origin` on the remote video → capture stays a silent no-op, `upload-dream-thumbnail` never called, no uncaught error escapes the page — proves the fix is no worse than before; (2) `Access-Control-Allow-Origin: *` present → capture succeeds, `upload-dream-thumbnail` called once with a real `data:image/jpeg` payload — proves the fix actually works when a CDN cooperates; (3) in BOTH cases, `#result-video` never gets a `crossOrigin` attribute and still decodes/plays a real frame — the hard regression guard; (4) `dream.imageUrl` already set → capture never runs, existing guard preserved; also proves the detached capture `<video>` is fully removed from the DOM afterward, never left leaking) | Correlating a pending operationName to its thumbnail relies on `lib/dream-store.js`'s already-synced private-dream record (`sourceOperationName`/`imageUrl`) — if the client never reaches result.html at all (tab closed, no sync ever lands), or the dream was already published (out of `dream-store.js`'s scope by design), the nudge finds no thumbnail and drops the dream rather than emailing it thumbnail-less; this is the intended honest degrade, not a bug. No live end-to-end proof against a real deployed Netlify scheduled-function invocation (same sandbox limitation `test/send-daily-claim-pushes.test.js`, this repo's first scheduled function, already accepts). Capture is also only wired on result.html, not explore.html's separate resume-completion path (`fireFirstVideoCreatedIfEligible`) — that path has no dedicated full-size `<video>` element to capture a frame from, so a dream completed that way has no thumbnail for the nudge until it's later opened on result.html. **Still genuinely unknown from this sandbox (safe either way): whether fal's actual production CDN (`fal.media`) sends `Access-Control-Allow-Origin` on its redirected video responses** — no network access from here to check. If it does, real thumbnails should appear in the nudge; if it doesn't, the dream is dropped from the nudge (never a bare send), provably no worse than before. |
| FirstVideoCreated conversion event + durable markers (`mark-generation-completed.js`, `consume-generation-marker.js`) | `test/first-video-created-behavioral.test.js`, `test/first-video-created-explore-resume-behavioral.test.js`, `test/generation-completion-marker.test.js` | — |
| Web push (daily-claim-available, dedup) | `test/send-daily-claim-pushes.test.js`, `test/push-dedup-store.test.js`, `test/push-subscription-store.test.js` | — |
| Morning Capture Ritual — WhatsApp channel (opt-in field, `save-whatsapp-number.js`, `send-whatsapp-morning-capture.js`'s per-day dedup + PILOT-ONLY GATE, `admin-submit-whatsapp-template.js`'s one-off Meta template submission) — tracker item `for-product-build-whatsapp-morning-captu-skez3n` | `test/account-store.test.js` (whatsappNumber field), `test/save-whatsapp-number.test.js`, `test/send-whatsapp-morning-capture.test.js` (send/dedup mechanics, plus its own section 3: `isPilotOnly`/`isSendEligible`, `scanAndSend`'s `pilotSkipped` counting, proving a pilot-skip never consumes the per-day dedup slot so a later go-live flip still reaches that account same-day), `test/whatsapp-morning-capture-opt-in-behavioral.test.js`, `test/admin-submit-whatsapp-template.test.js` (the exact Meta Message-Templates-create request shape, owner-email gating, missing-config reporting, Graph API/network error surfacing) | Cannot exercise a real Meta send or a real template submission — no live `WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_WABA_ID`/approved template in this sandbox, same honest-degrade limitation `test/send-daily-claim-pushes.test.js` already accepts for real push delivery; the real template submission and the first real pilot send stay manual steps for Manager/Ron (see `admin-submit-whatsapp-template.js`'s own header comment). No email/push sibling channel exists yet for this same ritual (this is currently the only channel) — see `send-whatsapp-morning-capture.js`'s own header comment. The pilot roster is `lib/test-identity.js`'s existing founder-alias list, not a dedicated new one — see `send-whatsapp-morning-capture.js`'s PILOT-ONLY GATE header comment for why. |
| Meta CAPI / conversion tracking (`track-conversion.js`) | `test/meta-capi.test.js`, `test/meta-capi-behavioral.test.js`, `test/funnel-meta-attribution-cookies-behavioral.test.js` | — |
| PostHog identity/test-traffic tagging | `test/posthog-identity-merge-behavioral.test.js`, `test/posthog-test-traffic-tagging-behavioral.test.js`, `test/server-analytics-containment.test.js` | — |
| Support/feedback inbox (`submit-support-message.js`, `get-support-messages.js`) | `test/support-feedback.test.js`, `test/support-feedback-behavioral.test.js` | Whether `get-support-messages.js` (the admin-read side) has its OWN direct handler-level test, vs. only being exercised indirectly through the behavioral submit flow, should be spot-checked next time this row is touched |

## Owner/admin tooling & coordination

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Paywall toggle | `test/admin-paywall-toggle.test.js` | — |
| WhatsApp morning-capture template submission (`admin-submit-whatsapp-template.js`, owner-gated, never runs automatically) — tracker item `for-product-build-whatsapp-morning-captu-skez3n` | `test/admin-submit-whatsapp-template.test.js` (exact Meta Message-Templates-create payload shape/category, GET preview vs POST auth split, owner-email gating, missing-config reporting, Graph API/network error surfacing) | No real Meta submission exercised (see the Morning Capture Ritual row above) — the real call is a manual step for Manager/Ron. |
| tracker.html (`get/add/update/delete-tracker-item.js`) | `test/tracker.test.js`, `test/tracker-behavioral.test.js`, `test/tracker-comments-behavioral.test.js`, `test/tracker-reviewed-behavioral.test.js`, `test/tracker-speak-behavioral.test.js`, `test/tracker-waiting-for-behavioral.test.js` | — |
| Media library page + backfill sweep (`media-library-x7q4.html`, `admin-backfill-media-rehost.js`, `admin-media-library-data.js`, `admin-recovery-sweep.html`) — tracker items `for-product-owner-media-library-page-fou-1fwxaw`, `for-product-media-library-founder-feedba-wnr3iw` (Published/Private filter + desktop-scroll fix), `for-product-media-library-clicking-a-vid-bnlcus` (card-tap lightbox), `for-product-media-library-founder-08-03--6h7fmv` (dream-text-per-card), `for-product-close-the-recovery-backfill--9chzb4` (**`admin-recovery-sweep.html`**, added 2026-08-08: a second, dedicated one-tap front end onto the same `admin-backfill-media-rehost.js` endpoint — mirrors `audit-x7q4.html`'s auth/UX shape rather than `media-library-x7q4.html`'s, since it only ever runs against the single hardcoded owner account, and its whole job is driving the endpoint's cursor loop to `done:true` and showing one final accumulated recovered/already-durable/lost/skipped summary, rather than `media-library-x7q4.html`'s own "Run backfill sweep" button, which surfaces each page's own numbers in a scrolling log with no final total. Both remain safe to run interchangeably since the sweep itself is fully idempotent.), **`for-product-media-library-stamp-durable--u4oju3`** (founder 08-11: "still doesn't have a timestamp for videos and isn't sorting by new ones" — root-caused and fixed: `media-library-x7q4.html`'s own render/sort code was already correct (`ml-sort-order`, newest-first-by-`createdAt`, unknown sinks to the end); the REAL bug was upstream, on the WRITE side, in two places `admin-media-library-data.js`'s own read-side code could never see or fix on its own — (1) `js/store.js`'s dream object has carried a real `createdAt` since `finalizeDream` stamped it, but BOTH `js/store.js`'s own `attemptPrivateDreamSync` POST-body builder AND `dream-sync.js`'s server-side `DREAM_FIELDS` whitelist silently dropped it before it ever reached the durable server-side private-dream store — every synced private dream had no `createdAt` at all, server-side, regardless of when it was actually made; (2) the shared feed record (`publish-dream.js`) never had a `createdAt` field in its shape at all, only `publishedAt` (which can be well after actual generation). Fixed at both write paths, forward-only, never fabricated for a genuinely un-derivable record: `js/store.js` now forwards `dream.createdAt` in both the private-dream-sync and publish-to-feed payloads (validated as a real finite number, else `null`); `dream-sync.js` now accepts and stores it (same validation, handled outside the plain `DREAM_FIELDS` pass-through since a bad value would corrupt the media library's own sort); `publish-dream.js` now stamps a durable `createdAt` on every feed record's first creation (client value if sent, else `Date.now()` — same fallback `publishedAt` already used), preserves it immutably across every later republish/edit (exactly like `publishedAt`), and opportunistically backfills a pre-fix legacy record (no `createdAt` of its own yet) from the client's real value on its NEXT republish, falling back to that record's own `publishedAt` if the client doesn't send one either. `admin-media-library-data.js`'s feed read now prefers the real `createdAt` over `publishedAt`, falling back to `publishedAt` only for a record this fix genuinely can't repair. Also fixed a THIRD, independently-discovered gap in the same investigation: `saveClaimedDream` (`claim-dream.html`'s abandoned-dream re-engagement landing page) stamped no `createdAt` at all, client or server — now stamps `Date.now()` at claim time (the earliest real signal available, the original generation moment isn't recoverable there). No backfill attempted for an ALREADY-synced pre-fix private dream with no `createdAt` on file server-side — no reliable signal exists to derive one from (an existing `updatedAt` can post-date actual creation by an edit), so per the tracker item's own explicit scope it's left correctly sinking to "unknown time," same as any other genuinely un-derivable legacy record; it self-heals automatically the next time that same browser re-syncs that dream (any future upsert now carries the real local `createdAt` forward).) | `test/admin-backfill-media-rehost.test.js` (auth, idempotency, pagination across the accounts→feed phases, per-field re-host outcomes), `test/admin-media-library-data.test.js` (auth, item shape, status classification matching `lib/media-status.js` directly, both/neither-media-field edge cases, and the `storyText` field: a private dream's distinct `storyText` returned verbatim over its `caption`, a pre-split private dream with no distinct `storyText` falling back to `caption`, a published feed item — which never carries a separate `storyText` on its own wire shape — using its `caption`, and a dream with no text on file at all returning `null` rather than an empty string; **createdAt fix**: a published feed record WITH a real stamped `createdAt` uses it over a later `publishedAt`, a private dream synced with no `createdAt` AND no `updatedAt` (genuinely un-derivable) returns `null` rather than a fabricated value, a private dream synced WITH a real `createdAt` (post-fix) carries it through end to end; **READ-SIDE FALLBACK follow-up (founder 08-11): a HISTORICAL private dream with no `createdAt` but a real `updatedAt` now falls back to `updatedAt`** for its displayed/sortable timestamp — `collectPrivateItems` uses `createdAt || updatedAt || null`, mirroring the feed side's own `publishedAt` fallback — so an old private video gets a real timestamp instead of blank/last; a record WITH `createdAt` still prefers it over `updatedAt`; **VIDEO-CARD-ONLY fix (founder 2026-08-11): a VIDEO dream emits exactly ONE card (its video), carrying its auto-captured first-frame still (`dream.imageUrl`) as the card's `thumbnailUrl`/`<video poster>` — it no longer also emits a separate "image" card for that still (which is not a user-created image); a genuine user-created IMAGE dream (`mediaType:'image'`, flux) still emits its image card — discriminated by `record.mediaType` (falling back to "has videoUrl" for a legacy record), asserted both ways in `admin-media-library-data.test.js`; the `<video>` in `media-library-x7q4.html` now carries `poster=thumbnailUrl` so it's not a black rectangle**), `test/media-library-page.test.js` (Playwright: gate → unlock → grid render → status/account/published filters including all three combined, a 1280×800 desktop-viewport test that seeds 60 items to force real overflow and proves the grid's internally-scrolling region actually reaches content past the fold, the card-tap lightbox — opens a real unmuted controls-enabled `<video>` or a bigger `<img>` using the item's real url rather than the in-grid muted/lazy thumbnail, and closes via its own close button, Escape, or tap-outside-the-media, while a tap ON the media itself does not close it — and per-card dream text: a card whose item carries `storyText` renders it in a `.interp-text` element with the exact text, a card with no `storyText` on file renders no description element at all), `test/admin-recovery-sweep-page.test.js` (Playwright: a full multi-page sweep across both the accounts and feed phases accumulates totals ACROSS every page rather than showing just the last page's numbers, confirmed against real per-page request bodies including the literal `'feed'` cursor handoff; a wrong password shows a plain-English error and never reveals the results panel; a failure mid-sweep on the second page is shown honestly with no false-success panel and no partial totals presented as final), **createdAt write-path fix**: `test/dream-sync.test.js` (a real `createdAt` round-trips through the private-dream sync — the actual root-cause fix; a missing/malformed value is dropped, never fabricated or stored as a bad value; re-upserting updates it in place), `test/publish-dream.test.js` (a client-supplied `createdAt` is stamped on first publish; omitted/malformed falls back to `Date.now()`; preserved immutably across a republish/edit even when the client sends a different value; a pre-fix legacy record with no `createdAt` of its own opportunistically backfills from the client on its next republish, or falls back to its own `publishedAt` if the client sends nothing), `test/dream-sync-client-behavioral.test.js` (real Chromium: `saveClaimedDream` stamps and forwards a real `createdAt`; publishing a private dream with a real local `createdAt` forwards it to `publish-dream.js`'s shared feed record) | Both admin endpoints are unpaginated on the FEED side and the data-listing endpoint is unpaginated on the accounts side too (documented in each file's own header comment as an accepted "fine at current scale" tradeoff, same posture `send-daily-claim-pushes.js` already established) — would need real pagination if the account base grows large. The Published/Private filter's underlying data (private items, `isPublished` flag) was already correct in both the backend and the per-card badge before this change — verified by reading `admin-media-library-data.js`/`lib/dream-store.js` before building; only the dedicated filter control was missing, so no backend change was needed for that part. The desktop-scroll bug's root cause: `#app` (no `.scroll-shell`/`.scroll-area`, the pattern `tracker.html` already uses for the same long-content-owner-page shape) gets a fixed height + `overflow:hidden` once the desktop phone-frame breakpoint (`@media min-width:560px` in `css/styles.css`) applies, with nothing to scroll content past that height — invisible on mobile, where `#app` is plain block flow and the whole document scrolls normally instead. The lightbox's tap-outside-to-close deliberately reuses `js/sheet-dismiss.js`'s `SheetDismiss.wire()` for its same-spot re-tap guard rather than a bespoke listener (see `media-library-x7q4.html`'s own inline comment); Escape-to-close is a small standalone `keydown` listener since that module doesn't cover it. The founder's own phrasing asked for the dream text "rendered the same way" as "the interpretation text" — verified before building that NO existing interpretation-text (or any dream-text) element had ever been rendered on this page at all, so this reuses `css/styles.css`'s own `.interp-text` class verbatim (the one place this codebase actually renders something called "interpretation text," `js/interpret-experience.js`'s reading card) rather than inventing new typography; the field itself is `storyText` (falling back to `caption` for a pre-split dream), never `promptText` (the engineered generation prompt, not what the user typed) — see `admin-media-library-data.js`'s own header comment for the full fallback reasoning. **createdAt fix's own accepted gap**: an already-synced pre-fix private dream that is never touched/re-edited again by its owning account stays permanently "unknown time" — there is no batch/admin backfill path for it (no reliable server-side signal to derive a real creation time from), matching the tracker item's own explicit "don't force it" scope. |

| Instagram/TikTok post pack MVP — Phase 1, manual-export only, no posting APIs (`assemble-instagram-tiktok-postpack.js` nightly scheduled job, `lib/postpack-selector.js`, `lib/postpack-caption.js`, `lib/postpack-store.js`, `admin-postpack-data.js`, `postpack-h4mv.html`) — tracker item `for-product-instagram-tiktok-auto-postin-cahr76`, Manager's SPEC v1: reuses the existing `channelLicenseGrantedAt`/`channelLicenseRevokedAt`/`okToFeatureOnChannels` consent fields (tracker item `for-product-terms-republish-license-per--fhpcxk`) already carried on every shared-feed record, and reuses `lib/push-dedup-store.js`'s existing `markSentOnce`/`hasSent` (not a new store) for both the max-1-per-day-per-channel guard (`'postpack-daily:'+channel+':'+utcDate`) and the durable, permanent never-repeat-a-dream-across-all-history guard (`'postpack-dream:'+dreamId`, global across BOTH channels — once a dream is used for either channel it is never reused for the other). Video downloads now route through the watermark-on-download mechanism (see that row below) instead of a plain `<a href download>` — see `postpack-h4mv.html`'s own `handleVideoDownload`/`cardHTML` comments; image downloads are untouched (that ruling only concerns video) | `test/postpack-selector.test.js` (pure eligibility rules: requires a real `channelLicenseGrantedAt`, excludes a revoked or per-dream-opted-out dream, excludes ANY dream with a moderation report on file regardless of reason, requires real media, newest-`publishedAt`-first ordering incl. a missing-`publishedAt` record sorting last), `test/postpack-caption.test.js` (caption includes the dream's own text/style/@-credited handle without a double `@`, hashtags are a deterministic per-style set — all four real app styles plus an unrecognized-style/generic fallback — deduped and capped), `test/assemble-instagram-tiktok-postpack.test.js` (end-to-end against a seeded fake feed: builds one item per channel with the newest-first + cross-channel-distinctness behavior the dedup markers actually produce, output item shape incl. video-vs-image `mediaType`/`mediaUrl` passthrough, each individual selection-policy rule skipping the right dream, max-1-per-day-per-channel via a same-day re-run building nothing new and persisting no duplicates, and the GLOBAL never-repeat marker excluding an already-used dream even with fresh unclaimed daily markers), `test/admin-postpack-data.test.js` (owner-gate/rate-limit shape duplicated from `admin-media-library-data.test.js`, newest-`builtAt`-first ordering, empty-store handling), `test/postpack-page.test.js` (Playwright: login-redirect, wrong-password error, correct-password grid render with channel badges, channel filter, empty state, the per-card caption+hashtags readonly textarea matching the built item exactly, the copy button's "Copied!" confirmation, an image card's plain download link/real media url, a video card rendering Download as a button rather than an anchor, and that button resolving+downloading the WATERMARKED twin via `download-video.js` keyed by the item's `dreamId` — never fetching the raw clean video url) | Phase 2 (actually posting to Instagram/TikTok via their real APIs) is explicitly out of scope for this MVP and unbuilt by design — see the tracker item's own "DO NOT build Phase 2" instruction; this only assembles a pack for the founder to manually copy/download/post. No real Meta/TikTok developer account or credential exists anywhere in this build. Both the nightly job and the owner data endpoint are unpaginated full-store scans, same accepted "fine at current scale" tradeoff as `admin-media-library-data.js`/`send-daily-claim-pushes.js`. |

| **Watermark-on-download** (`lib/watermark-video.js`, `lib/fal-queue.js`, `download-video.js`, `download-video-status.js`, `watermarked-video-file.mjs`, `js/watermark-download.js`, `assets/watermark-overlay-720x1280.mp4`) — tracker item `for-product-founder-ruling-08-07-every-u-g81h7v`, founder's ruling: "when a user downloads a video, the file must carry the logo watermark," generation and in-app playback stay completely clean; a watermarked TWIN is produced lazily server-side on first download request, cached forever, and reused for every later download of that dream (retroactive for every existing video) as well as by the IG/TikTok postpack export flow (see that row above). Investigated twice before landing on this build (see the tracker item's own comment history): `fal-ai/ffmpeg-api/compose` (the mechanism the ruling's own text originally named) is a pure clip SEQUENCER with no position/opacity/alpha fields at all, genuinely unable to place a corner overlay; `fal-ai/workflow-utilities/blend-video` CAN via ffmpeg's `blend` filter's arithmetic `screen` blend-mode trick (verified against its real API schema, not marketing copy) against a single, small, STATIC overlay asset (committed to the repo, not generated per-video) — `blend_mode:"screen"` makes a solid-black top layer a no-op everywhere except a translucent logo badge; `shortest:false` (explicit, overriding fal's own `true` default) plus ffmpeg's own `repeatlast` framesync default holds that overlay's one real frame for the WHOLE base video's length regardless of the dream's own duration, so one ~1-second overlay asset works for any dream length — this exact ffmpeg `blend` mechanic (not just the formula on paper) was verified directly against a real ffmpeg binary in this build's own sandbox (`imageio-ffmpeg`'s bundled static build): a 1s single-frame overlay screen-blended against a synthetic 4s base clip with `shortest=0` produced a full 4s output with the logo still visible in the very last frame. Twins are cached via `lib/media-rehost.js`'s EXISTING best-effort Blobs pattern (a new `'video-watermarked'` mediaType added to its store/file-function maps, its own `dreamtube-videos-watermarked` store, never touching the clean-original `dreamtube-videos` store), keyed by the dream's own `id` (not a fal requestId) — the same id `js/share-sheet.js`'s "Save to device" and `postpack-h4mv.html`'s postpack export both already key off, which is what makes "IG/TikTok reuses the same twins" true for free. The real fal.ai `blend-video` call is a genuine queue job, so `download-video.js` (submit-or-check-cache) / `download-video-status.js` (poll-and-finalize) follow the SAME submit-then-poll idiom `generate-video.js`/`video-status.js` already established, rather than blocking a single Netlify Function invocation. On any failure (fal down, job errors, timeout) the client shows a real error and NEVER silently falls back to fetching the clean, unwatermarked `dream.videoUrl` — that would violate the ruling this feature exists to satisfy | `test/watermark-video.test.js` (`lib/watermark-video.js` in isolation: cached-twin lookup incl. the oversized/sourceUrl-fallback case, the exact blend-video request body shape incl. `shortest:false`/`blend_mode:"screen"`/the `X-Fal-Object-Lifecycle-Preference` no-expiry header, pending-job recording/staleness, and `pollAndFinalize`'s full state machine — still-running, fal-side failure clears the marker, `COMPLETED` with no video url in the response, a successful re-host into the twin store, and a re-host-fetch failure still resolving `ready` via fal's own url), `test/download-video.test.js` and `test/download-video-status.test.js` (the two functions' own HTTP surface: method/param validation, missing-`FAL_KEY` handling, cache-hit short-circuit with zero fal calls, in-flight-job de-duplication so an overlapping request never submits a second fal job, and a full submit→poll→poll→ready flow whose SECOND submit call for the same dream then hits the cache), `test/media-rehost.test.js` (the new `'video-watermarked'` mediaType is its own store, distinct from `'video'`, and a write there never touches `dreamtube-videos`), `test/media-file-functions.test.js` (`watermarked-video-file.mjs`'s own streaming/redirect behavior, mirroring `video-file.mjs`/`image-file.mjs`'s existing coverage), `test/watermark-download-client.test.js` (`js/watermark-download.js`'s `resolveVideoUrl` polling/timeout/cancellation logic in isolation, fast, no browser), `test/share-sheet-behavioral.test.js` (real Chromium: every pre-existing "Save to device" video test now routes through a mocked `download-video.js` first — regression-proven unchanged behavior downstream — plus two NEW scenarios: a `processing`→poll→`ready` flow that shares the WATERMARKED url while proving the raw clean url is never fetched, and a watermark-job failure showing a real error and never falling back to the raw video), `test/postpack-page.test.js` (see that row above) | **REAL-MODE gate: NOT exercised against the real fal.ai `blend-video` endpoint** — no `FAL_KEY` in this build sandbox (disclosed, same recurring limitation as this codebase's other fal-adjacent work). The `screen`-blend-mode compositing trick and the `shortest:false`/`repeatlast` duration mechanic were independently verified against a REAL ffmpeg binary (not simulated in JS) since fal's `blend-video` is a documented thin wrapper around ffmpeg's own `blend` filter, but the actual fal-hosted call itself — request/response shape, real timing, real output quality against this app's real Veo-generated H.264 media — has not been exercised for real. Someone with `FAL_KEY` access should run one real download (cheapest path: any existing short dream video, `founder downloads a video from his profile` is this ruling's own stated close-check) before calling this fully closed. |

## Cross-cutting / infrastructure

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Rate limiting (`lib/rate-limit.js`) | Exercised indirectly inside several endpoint tests (e.g. `test/check-email.test.js`, `test/claim-daily-tokens.test.js`) | No single dedicated `rate-limit.test.js` isolating the shared helper itself |
| Netlify Blobs retry helper (`lib/blobs-retry.js`, read→mutate→write→verify mitigation, `@netlify/blobs` 8.2.0 — still used by marker/dedup stores keyed by an opaque id, not by email/account: token-purchase/refund/achievement-grant markers, tracker-store, pending-dreams, support-store, feed-comment-count) | `test/blobs-retry.test.js` | — |
| Netlify Blobs CAS helper (`lib/blobs-cas.js`, REAL compare-and-swap via the separately-aliased `blobs10` package/`@netlify/blobs` 10.x, `onlyIfNew`/`onlyIfMatch` — used by entitlements.js's per-email balance record writers and like-dream.js's feed-index write; see this doc's own Tokens/Likes rows) | `test/blobs-cas.test.js` | — |
| Shared bottom-sheet dismissal (`js/sheet-dismiss.js` — tap-outside, drag-to-dismiss, wired into all 13 `.sheet-overlay` call sites app-wide) | `test/sheet-dismiss-behavioral.test.js` (per-instance five-point suite across profile.html/result.html/create.html/wizard.html/js/purchase-sheet.js, plus a dedicated rapid-re-tap-after-open regression — tracker item `for-product-urgent-founder-posthog-recor-75ob70`: a tap-outside click landing within the sheet's own opening transition window used to self-dismiss it immediately, reading as "the sheet never opens" on a real PostHog recording) | — |
| Web push helper | covered via `test/push-dedup-store.test.js`/`test/push-subscription-store.test.js` and `test/helpers/mock-web-push.js` | — |
| **"Notify me" push ask — shown on every logged-in home visit** (founder ask 2026-08-11: push was almost never prompted, so almost nobody subscribed; enable the ask for everyone going forward). `home.html`'s `refreshNotifyCard` now calls `js/push-subscribe.js`'s `maybeShowAsk` on any normal logged-in home visit into `#notify-card-slot`, no longer gated behind a generation being in flight (`if(!pendingJob) return` removed). The real gating in `push-subscribe.js` (`shouldShowAsk`: `isSupported`, not an FB/IG in-app webview via `detectInAppWebviewHost`, `Notification.permission==='default'`, not `getPushAskDismissed`; iOS-Safari-tab keeps the "add to home screen" `shouldShowIOSFallback`). `refreshNotifyCard` guards against re-rendering an already-present `.push-ask-card` so repeated `refreshHomeState` calls don't re-fire shown-telemetry or nag; the one-ever dismissed marker still holds. End-to-end path: ask → `Notification.requestPermission` → `subscribe(username)` → `save-push-subscription.js`. **PUSH-GATING FIX (founder-approved 2026-08-12): the native prompt is now INSTALLED-ONLY.** `shouldShowAsk` additionally requires `isInstalledStandalone()` (delegates to `PwaInstall.isStandalone()` — `matchMedia('(display-mode: standalone)')` OR iOS `navigator.standalone`, fail-closed if `PwaInstall` absent), so the real "Notify me" soft-ask (whose "Notify me" tap is the ONLY thing that fires `Notification.requestPermission`) renders ONLY in the installed/standalone app AND where push is genuinely supported — NEVER in a plain browser tab or an in-app webview (on iOS a "deny" in a context that can't subscribe permanently burns the origin's permission; this stops spending that one shot where it can't succeed). The founder's install-first flow is preserved: NON-installed users keep the install enticement untouched — the iOS-tab `shouldShowIOSFallback` (`push_fallback_shown`) and `js/install-nudge.js`'s A2HS nudge. The existing card is already a SOFT-ASK (native fires only on the "Notify me" tap, so a decline on our own card never burns the native permission and they can be re-asked); a new `push_softask_accepted` event marks that tap (the accept step), alongside the unchanged `push_prompt_shown`/`push_prompt_granted`/`push_prompt_denied`/`push_fallback_shown`. | Module-level persistence covered by `test/push-subscription-store.test.js`/`test/push-dedup-store.test.js` (persistence + dedup halves). **The installed-only gating IS covered by a real Chromium behavioral suite:** `test/push-install-gating-behavioral.test.js` (2026-08-12 fix — a non-standalone capable tab renders NO "Notify me" ask and never requests native permission, `isInstalledStandalone()` false; a forced-standalone capable launch renders the soft-ask and tapping it reaches the native prompt firing `push_softask_accepted`→`push_prompt_granted`, `isInstalledStandalone()` true; a non-installed iOS Safari tab still shows the `push_fallback_shown` install enticement), plus `test/pwa-stage0-behavioral.test.js` and `test/a2hs-install-nudge-journey-behavioral.test.js` (their real-"Notify me"-ask tests now force standalone to match the installed-only gate; the FB/IG-webview-hidden, iOS-tab-fallback, dismissed-once, and no-fallback-when-standalone cases unchanged). **Past-user limitation (stated, not a test gap):** a push can only reach a user who already has a SAVED subscription; because the ask rarely fired before this change, there are likely ~0 existing subscriptions, so "push to past users" is effectively impossible until they return to home.html and subscribe. The exact count of existing subscriptions is not knowable from code alone (it lives in the `dreamtube-push-subscriptions` Blobs store, not the repo). | — |
| **Win-back email — owner-gated, hard-capped batch sender** (founder-approved retention piece): a one-time "come back" email to a small batch of LAPSED past users. `send-winback-batch.js` (owner-gated POST `{ email, limit }`, `limit` default 10 / clamped to a 50 absolute ceiling, selects at most `limit` eligible accounts and sends each once so it can NEVER exceed `limit`), `lib/winback-email-sender.js` (the single send choke point — reuses the nudge sender's Resend core, `lib/email-layout.js` shell, `lib/unsubscribe-token.js` one-click footer, `lib/site-origin.js` canonical links; excludes founder/test addresses, honors `isSuppressed`, both checked BEFORE the guard; approved verbatim copy: subject "Your next dream is waiting 🌙", 2 lines + a "Make a new dream →" button to `/create.html`, "Sweet dreams, DreamTube"; best-effort `winback_email_sent` PostHog), `lib/winback-email-store.js` (a per-ACCOUNT once-ever `blobs10` `onlyIfNew` CAS marker claimed before send, released on send failure, so no user ever receives it twice across repeated batch runs), and `account-store.js`'s new `listAccounts` (whole-store `list({prefix:'u:'})` enumeration). **Lapsed definition:** an account whose `updatedAt` is ≥ `WINBACK_LAPSED_DAYS` (default 30) days old (no dedicated last-seen/createdAt field exists; `updatedAt` is set at signup and advanced only by rare account mutations, so it reads as "signed up / last touched over N days ago" — a missing/invalid `updatedAt` is treated as unknown age and skipped). | `test/send-winback-batch.test.js` (the hard cap is never exceeded even with more eligible users than `limit`, plus the 50-ceiling clamp; suppressed/unsubscribed skipped; the once-ever CAS marker prevents a second send across two batch runs AND directly at the sender choke point; a failed send releases the claim so a later run can retry; founder + `@example.com`/`test.` addresses excluded; recently-active/unknown-age/no-email accounts skipped; the exact approved subject/copy + `/create.html` link + List-Unsubscribe headers; `winback_email_sent` fires; owner-gating — non-owner → 403, plus 405/500/400/invalid-limit guards) | **No live end-to-end proof against a real Resend inbox** (same sandbox limitation every other email row here accepts — no real `RESEND_API_KEY`/Blobs). This endpoint is MANUAL/owner-triggered (not scheduled): the founder reviews, deploys, and triggers each batch himself. The "lapsed" signal is a documented proxy (`updatedAt`), not a true last-activity timestamp — a lapsed user who recently did a password reset/email verify/WhatsApp opt-in has a fresh `updatedAt` and is conservatively treated as not-lapsed. |
| **Live, real-origin, end-to-end smoke coverage** (as opposed to mocked-backend coverage) | `test/prod-smoke/*.test.js` (this file's own registry entry) | Intentionally narrow — see `test/prod-smoke/README.md`. Does NOT cover: payments/checkout, admin/owner tooling, transcription, interpretation/Chamber, push notifications, or most retention email paths. Those stay covered only by the mocked `test/*.test.js` suite. |
| **Reliability net: recurring both-domain health checks** — a scheduled Netlify Function (`both-domain-smoke.js`, every 4h) hits both `dreamtube1.netlify.app` and `dreamtube.life` for every key page + a curated set of core function endpoints (`get-feed`, `get-token-status`, `get-tracker-items`, `check-email`), asserting a real 200 + content marker, and writes results to `lib/smoke-status-store.js` (read back via the public `get-smoke-status.js`) — tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 1 | `test/both-domain-smoke.test.js` (checkPage/checkFunction/runDomainChecks/scanAndCheck pass/fail logic, with a fully faked fetch — no real network calls), `test/smoke-status-store.test.js`, `test/get-smoke-status.test.js` | Deliberately does NOT auto-file a tracker item on a real failure (this build has no owner-email credential to call `add-tracker-item.js` with) — writes to the status store only; whether to add automatic tracker-filing later is an open decision for Manager/Ron, not attempted here. Never actually run against a live Netlify deploy from this sandbox (no way to trigger a real scheduled invocation here) — `runDomainChecks`/`checkPage`/`checkFunction` WERE run directly (not via the scheduled `exports.handler` wrapper) against real production for both domains as part of this build's own verification, and passed cleanly on both. |
| **Reliability net: nightly state-seeded journeys, both domains** — `test/prod-smoke/session.test.js` (Journey 2 signup → claim → generate → edit → publish/unpublish → nav walk, now also asserting shop.html renders a real balance + pack cards), `test/prod-smoke/journey1-funnel-render.test.js` (Journey 1 entry), `test/prod-smoke/media-serving.test.js` (real media playback) run nightly against BOTH domains via `npm run smoke:prod:dreamtube1`/`npm run smoke:prod:life`, fired by a scheduled Routine (this environment's own external cron mechanism, NOT a Netlify Function — see `test/prod-smoke/README.md`'s "Nightly scheduling" section for why a real Playwright/Chromium run cannot execute inside a Netlify Function's own runtime), reporting its result to `report-smoke-status.js` — tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 2 | The three prod-smoke files listed (already covered, and cited, in their own rows above) + `test/report-smoke-status.test.js` (the reporting endpoint's own auth/validation logic) | `SMOKE_STATUS_REPORT_TOKEN` (the shared secret `report-smoke-status.js` requires) is not yet set in Netlify — until a human sets it, the nightly run's own status report soft-fails (503 `not_configured`, logged, never a hard error) rather than actually landing in the shared store; the Routine itself still runs the real tests and reports pass/fail in its own session regardless. See this build's own report for the one-line step to close this gap. |
| **Domain routing matrix** (which domain serves what, and why both are legitimately live at once) — `docs/DOMAIN_ROUTING_MATRIX.md`, tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 3 | `test/prod-smoke/domain-routing-matrix.test.js` (run against real production as part of this build — see that file's own header comment; all 5 assertions passed live against both real domains) | — |
| **Canonical-domain links** (founder standing rule 2026-08-08: "if we don't need a Netlify URL, never link to any Netlify URL" — every user-facing outbound link resolves to `https://dreamtube.life`): `lib/site-origin.js`'s `emailOrigin` no longer consults the request's Host/X-Forwarded-Host at all (env `URL` → canonical fallback), and every emailed/shared link builder now routes through it — `lib/dream-share-token.js`/`lib/pending-dream-token.js` `buildUrl`, `request-password-reset.js`, `lib/verification-email-sender.js`, `share-dream.js`/`share-profile.js` (og:url + redirect Location). Client side: `home.html`'s copy-link/open-in-Chrome webview escape swaps the live Netlify alias for the canonical host (guarded to exactly `dreamtube1.netlify.app`, deploy previews untouched; safe because the copied URL carries the origin-agnostic session-transfer `?bt=` token), plus the Make-it-yours copy text and `js/install-nudge.js`'s toolbar-mock fallback host. | `test/email-public-origin.test.js` (resolver contract rewritten to canonical semantics: alias/deploy-preview request hosts IGNORED, garbage/CRLF/non-public hosts still rejected, header-less scheduled sends canonical, plus a new no-alias-anywhere-in-email assertion), `test/share-dream.test.js`/`test/share-profile.test.js` (alias-in → canonical-out for og:url, meta-refresh, Location; CRLF/XSS defenses re-proven under the new resolver), `test/send-unwatched-dream-nudges.test.js` (the retention nudge — the sole scheduled retention email — emits canonical links on a header-less send), `test/unsubscribe-token.test.js`, `test/dream-webhook.test.js`, `test/media-file-functions.test.js` (expectations flipped to canonical while inbound alias hosts kept, proving alias requests emit canonical links) | Deliberately NOT canonicalized (each is a mechanism, not a user-facing link): Dodo checkout selfOrigin (mid-browser round trips must return to the origin holding the visitor's localStorage session), `start-pending-generation.js`'s fal webhook URL (machine-to-machine), `netlify.toml`'s `/go/*` proxy target (that IS the origin fetch), `both-domain-smoke.js`/prod-smoke tooling (probes both domains by design), tracker API base in agent docs (internal tooling). Cross-origin UX consequence verified, not built around: a user who browsed on the Netlify alias and clicks a canonical email link lands on dreamtube.life without that origin's localStorage — covered for verification links (`verify-email-link.js` mints a `?bt=` session-transfer token) and claim/share/unsubscribe/reset links (none require an existing signed-in session); worst case is a normal passwordless re-login. |

## Test tiering (critical vs. full)

Added per tracker item `for-product-test-suite-pruning-tiering-f-qminmq`
(founder pushback 2026-08-07: "are you sure we need 350 tests?" — the
suite carries real maintenance tax, and a redesign like Layout-B touching
several UI-facing test files at once felt disproportionate to the real
protection those touch-points bought). Two tiers, not two suites — the
critical tier is a **subset** of the same `test/*.test.js` files, not a
separate/duplicated set of tests:

- **`npm test` / `npm run test:full`** — unchanged, `node --test
  test/*.test.js`, every file (255 as of this pass, ~15-30 min). This is
  still the ground truth and the one that must pass before anything
  genuinely sensitive merges — nothing about tiering lowers that bar.
  Run this nightly (or before merging anything touching money/data/auth,
  regardless of tier) — it's the safety net that catches everything the
  fast tier intentionally skips.
- **`npm run test:critical`** (`scripts/run-critical-tests.js`,
  driven by `test/critical-tests-exclude-list.js`) — a fast subset (180
  of 255 files as of this pass) meant to run on every build/PR before
  the full sweep gets a turn. Fails loudly if the exclude list ever
  references a deleted/renamed file, so it can't silently go stale.

**EXCLUDE-LIST, NOT INCLUDE-LIST.** This started as an include-list
(default excluded, add a filename once someone recognized it as
sensitive) and was rebuilt as an exclude-list the same day after three
independent review rounds each found real money/data/security-relevant
test files silently missing — including one case where a file was added
for an explicit `SECURITY:`-labeled test while its own sibling file in
the SAME feature/PR, with an equally explicit `SECURITY:` test, was
missed in the same pass. An include-list's failure mode is silent and
one-directional: every oversight drops real coverage from the fast tier
with zero signal anything is wrong. An exclude-list inverts that: the
default is IN, so a newly added test file is critical-tier automatically
from the moment it exists — an oversight here just means a cosmetic file
runs a few extra seconds in the fast tier, never a missed real concern.

**What's in the critical tier, by construction:** every `test/*.test.js`
file EXCEPT the ones named in `test/critical-tests-exclude-list.js`.
That non-negotiable floor — money/data/auth, documented race/concurrency
bug families (signup, login, sessions, tokens/entitlements, generation,
payments, persistence, rate limiting, Blobs CAS/dedup, refunds), real
fal.ai/paid-vendor calls with their own rate limits, and documented past
production incidents — was never in scope to prune or loosen regardless
of tier, and now can't be silently dropped from the fast tier either.

**What's excluded, and why that's fine** (see
`test/critical-tests-exclude-list.js`'s own header comment for the
authoritative, itemized version — 8 named categories: admin one-off
tools with no live traffic; `tracker.html`'s own internal-tool test
family; secondary channels parked below the primary surface, like
WhatsApp capture and postpack/social-share assembly; analytics/telemetry
plumbing where a bug produces a wrong metric, not a wrong charge or a
leak; a specific founder-approved redesign's own copy/visual-pin
regression suite where the underlying behavior has separate real
coverage elsewhere; pure UI/interaction-polish suites with no money/
data/auth stake even on total failure; internal build/deploy/reliability
tooling with no user-facing surface; and narrow generation-variant UI
tests that explicitly disclaim any real fal.ai cost in their own
header). None of that coverage is deleted or weakened — `npm test` /
`test:full` still runs every one of those files, every time. Tiering
only changes what's fast enough to gate a routine PR.

**How a new test file gets classified:** by default, do nothing — it's
already critical-tier the moment it exists. Only add it to
`critical-tests-exclude-list.js` if it clearly and *only* matches one of
the 8 named categories, and you can point to where its real underlying
behavior (if any) is covered elsewhere. If it touches money, real user
data, auth, a race/concurrency/CAS/atomic-write concern, a real paid-
vendor call with its own rate limit, or a documented past production
incident — leave it in, full stop, even if it also carries some
cosmetic assertions.

**What this pass actually changed in the test files themselves** (the
pruning/loosening half of the same tracker item) — a small, deliberately
conservative set, since a careful read of this suite found it was
already mostly using range/regex assertions (`>=44px` tap targets,
`match`/`doesNotMatch` regex on copy) rather than the brittle
exact-pixel/exact-string anti-pattern the founder's concern was aimed
at. What did get touched:
- **Loosened** (kept the real behavioral assertion, dropped an
  incidental exact-value pin that had zero behavioral consequence):
  `test/avatar-describe-behavioral.test.js` (a sheet-stays-open check
  now asserts the `.open` class token via `isVisible()` instead of the
  full `class` attribute string — the job is "still open", not "class
  attribute is byte-identical"), `test/home-behavioral.test.js` (the
  ritual module's locked-state class check now asserts the `warm` class
  token is absent, via `doesNotMatch`, instead of pinning the entire
  class attribute to exactly `'ritual'`), `test/profile-night-restyle-
  behavioral.test.js` (the night-theme `theme-color` meta check now
  compares against `home.html`'s own live value instead of a hardcoded
  hex pinned redundantly in two files — a future retune of the exact
  shade no longer requires touching this test, only actually shipping a
  mismatch would).
- **Fixed** (genuinely broken on `main` at the start of this pass, found
  by running the full suite — root-caused per `systematic-debugging`,
  not just patched to pass): `test/home-behavioral.test.js`'s Build-it
  step-1 assertion pinned "step 1 of 5" text that the Layout-B redesign
  replaced with `#build-dots`, and now checks the real still-current
  properties instead (Subject step content visible, dot 1 of 5 marked
  current); `test/ui-behavioral.test.js`'s gibberish-detection test
  waited on `#char-count` text containing "character"/"N characters",
  stale since the most recent commit made that counter word-based ("N /
  50 words"); `test/social-layer-v2-profile-behavioral.test.js` and (a
  second instance of an already-known bug class, same fix pattern
  `ui-behavioral.test.js` already carries elsewhere in this file) a
  44px tap-target check failed on a sub-pixel float rounding value
  (43.99998...px) — given the same documented epsilon rather than a
  hard `>=44`; `test/sound-nudge-behavioral.test.js` and `test/logo-
  purple-retouch-asset-sweep.test.js` — a test fixture's image URL had
  reused the real, since-retired logo asset's old filename, tripping the
  repo-wide old-asset sweep; repointed at a generic fixture filename.
- **Deliberately left alone** despite dense copy/CSS assertions on
  inspection, because each earns its keep: `test/shop-behavioral.test.js`/
  `test/shop-pricing-copy-sweep.test.js` (pack pricing/token amounts —
  real money facts, explicitly in scope to protect, not prune), `test/
  home-mky-copy-behavioral.test.js` (regression coverage for a real
  founder-flagged confusion bug — "install" read as different from "add
  to home screen" — not incidental copy), `test/comment-sheet-
  behavioral.test.js`'s exact comment-count text (`'Comments · N'`) —
  the count is the real data under test, not incidental styling, `test/
  interp-voice-behavioral.test.js`'s exact-copy `waitForFunction` gates
  — used as a state-transition signal (loading → ready), not as the
  actual assertion under test. No test file was deleted outright — no
  file was found that was purely dead weight with zero real behavior
  left to protect; see the "Fixed"/"Loosened" bullets above for what
  actually changed.
- No `test:critical` exclusion touches any file in the KEEP list above
  (money/data/auth/race) — that set is exactly what the exclude-list
  approach above guarantees stays in the fast tier by default.

## Last reconciled

2026-08-10, alongside tracker item `recurring-bug-class-hardcoded-daily-gran-h6swgy`:
added `test/daily-grant-copy-static-sweep.test.js`, a generic repo-wide
static-HTML sweep for hardcoded daily-token-grant-amount copy (see the
"Tokens, entitlements, payments" row above for the full writeup). Also
fixed a genuinely live stale-copy instance the sweep found while being
built: `profile.html`'s FAQ and `admin.html`'s owner reset-tool copy
(two spots) still said the old "220" `INITIAL_GRANT` value after it was
retuned twice the same day (220→170→150) — corrected to 150, which in
turn required updating one pre-existing test
(`test/faq-section-behavioral.test.js`) that had incidentally hardcoded
the old "220 free" string as an unrelated accordion-interaction
assertion.

2026-08-08, alongside tracker item `for-product-close-the-recovery-backfill--9chzb4`:
extended the "Media library page + backfill sweep" row to cover the new
`admin-recovery-sweep.html` (a dedicated one-tap runner for the existing
`admin-backfill-media-rehost.js` sweep, mirroring `audit-x7q4.html`'s
auth/UX shape) and its new test file, `test/admin-recovery-sweep-page.test.js`.

2026-08-04, alongside tracker item `for-product-founder-08-04-evening-music--jfjco0`
(mood-keyed music beds): added the mood-keyed-bed row above covering the
`dream.mood` persistence fix (the wizard's Mood answer used to reach the
generated prompt text and nothing else), the two-tier `urlForDream`
selection layer, and `bed-audition-x7q4.html`. The row's Gaps column
discloses the real, un-worked-around blocker: no `FAL_KEY` in the build
sandbox, so the 12 candidate tracks don't exist and selection tier 1 is
inert — shipped as a tested layer that provably changes nothing a user
hears today, and switches on with files only.

Corrected later the same day, after review found a real bug on the same
branch: that row's coverage column had claimed the mood-survives-cross-
device-sync path was proven, citing `test/dream-sync.test.js`. It wasn't
— that test drives the server handler with a hand-built payload and never
exercises the real client, which was in fact dropping `mood` from its
outgoing sync body entirely (`js/store.js`'s `attemptPrivateDreamSync`
builds that body from an explicit hand-maintained field list). Server
allowlist correct + local state correct + wire silently missing the field
is exactly the shape a handler-only test cannot see. The column now says
which half each test actually covers, and the client-side wire contract
has its own coverage. **General lesson worth carrying:** for any field
that has to cross a client→server boundary, coverage of the server
handler and coverage of local state can both be green while the field
never leaves the browser — assert on the outgoing request body itself.

2026-08-04, alongside tracker item `for-product-surface-deploys-day-as-a-vis-xld1vk`
("Surface deploys/day as a visible number"): added the race-free
deploy-log write/count design above (`lib/deploy-log-store.js`,
`get-deploy-count.js`, wired into `scripts/generate-version.js`'s
existing per-build step and folded into `admin.html`'s existing
release-visibility line rather than a new page). See the new
Cross-cutting/infrastructure row above for the honestly-disclosed gap
(build-time write needs a founder-provided Netlify PAT env var; not yet
exercised against a real Netlify deploy from this sandbox).

2026-08-03, alongside tracker item
`for-product-reliability-net-spec-v1-smok-x1o5zc` ("SPEC v1: smoke +
seeded journeys now; nightly real-generation probe HELD for founder spend
approval"): added the reliability-net buildout's three no-spend pieces —
`both-domain-smoke.js` (scheduled, both domains, key pages + core
function endpoints), a nightly external-Routine-driven run of
`test/prod-smoke/*.test.js` against both domains (see that directory's
own README "Nightly scheduling" section for why this couldn't be a
Netlify Function instead), and `docs/DOMAIN_ROUTING_MATRIX.md` +
`test/prod-smoke/domain-routing-matrix.test.js`. Also extended
`test/prod-smoke/session.test.js`'s existing nav-walk with a shop.html
render check. See the three new Cross-cutting/infrastructure rows above.
The nightly real-generation probe (real fal.ai spend) remains explicitly
NOT built — see this build's own report for the Board-vs-task-text
discrepancy on whether that spend has actually been approved.

2026-08-02, alongside tracker item `for-product-wizard-characters-step-is-si-
paxp07` (founder repro): fixed `wizard.html`'s Subject step (Step 1) from a
single-active-subject model to real independent multi-select toggles
(`staged.subjectCharacterIds` + `subjectOtherKind` composing together), and
extended `js/wizard-chips.js`'s `assembleCaption`/`buildDeterministicStory`
with a new, backward-compatible `subjects`-array input to carry multiple
selections all the way into the assembled generation prompt — see the
Onboarding funnel row above for the full writeup and its test coverage.
Also fixed a real, pre-existing bug found while touching this exact code
path: the in-place-login (already-registered-email) branch's post-flush
`draft.characterIds` id remap used to run BEFORE the `draftPatch` setDraft
call that immediately overwrote it back to the stale pre-flush local id(s)
— reordered to match the normal-signup branch's own (already-correct)
ordering.

2026-08-02, SECURITY FIX (round-2 independent review of tracker item
`for-product-build-passwordless-signup-fo-at2fko`): closed a real account-
takeover bug — a bare POST of an already-registered email to
`register-account-passwordless.js` used to mint a fully usable session with
zero ownership proof. Fixed via a new `login-with-email-code.js` endpoint
(the real access-control gate — a genuine login, mailed-code-as-credential)
and `verify-email-link.js` now also minting a `lib/session-transfer-
token.js` token on success. See the new SECURITY FIX row above for the full
writeup and its test coverage.

2026-08-02, alongside tracker item `for-product-build-passwordless-signup-fo-
at2fko` (founder-decided passwordless-signup hybrid): added the passwordless
signup path (`register-account-passwordless.js`, mock-only —
`SIGNUP_PASSWORDLESS_LIVE` is `false`), deferred email verification
(`verify-email-code.js`, `resend-verification-code.js`,
`verify-email-link.js`'s implicit-verification-via-link-click,
`js/email-verify-sheet.js`'s "next visit" prompt), the gate list
(`create-checkout-session-dodo.js`'s E10, `publish-dream.js`'s E8 — see
`lib/account-store.js`'s GATE LIST header comment), and Resend hard-bounce
handling (`resend-bounce-webhook.js`) — see the Auth & accounts rows above.

2026-08-02, alongside tracker item `for-product-urgent-founder-posthog-recor-
75ob70`: fixed `js/sheet-dismiss.js`'s tap-outside-to-close listener
self-dismissing a sheet within ~50ms of its own open (a rapid re-tap at the
trigger button's coordinates landing on the just-opened overlay backdrop,
before the `.sheet` panel had visibly slid into view) — a same-position
re-tap guard (not a flat time window, which regressed
`test/out-of-tokens-purchase-sheet-behavioral.test.js`'s several
genuinely-intentional immediate tap-outside-to-dismiss cases; see
`js/sheet-dismiss.js`'s own comment for why position, not time, is the
right signal) now ignores a tap-outside only when it lands within ~48px of
the most recent click that happened somewhere else on the page. Also added
the Cross-cutting/infrastructure row above for this shared module (which
had no dedicated registry row despite already having its own
`test/sheet-dismiss-behavioral.test.js` file).

2026-08-02, alongside tracker item `for-product-dream-ready-email-real-first-
qr9fbj`: added the retention email's real first-frame thumbnail (new
`upload-dream-thumbnail.js` endpoint, `lib/first-dream-email-sender.js`'s
`buildHtml` real-`<img>`/flat-banner branch and `absoluteImageUrl` helper,
`js/store.js`'s `DreamStore.saveThumbnailBestEffort`, result.html's
client-side canvas capture IIFE) — see the Retention row above.

2026-08-02, alongside tracker item `for-product-build-whatsapp-morning-captu-
skez3n`: added the Morning Capture Ritual's WhatsApp channel (`whatsappNumber`
account field, `save-whatsapp-number.js`'s profile.html Settings opt-in,
`send-whatsapp-morning-capture.js`'s daily scheduled sender + per-day dedup
via `lib/push-dedup-store.js`) — see the Retention row above.

2026-08-02, alongside tracker item
`for-product-owner-media-library-page-fou-1fwxaw`: added own-storage media
re-hosting (video-status.js/image-status.js/dream-webhook.js completion
paths, `lib/media-rehost.js`, `lib/media-status.js`, `image-file.mjs`), the
owner-only backfill sweep (`admin-backfill-media-rehost.js`), and the owner
media library page + its data endpoint (`media-library-x7q4.html`,
`admin-media-library-data.js`) — see the rows above.

2026-08-01, alongside tracker item
`for-product-build-founder-directed-produ-miyfp4` (founder-directed,
HIGH priority) — built `test/prod-smoke/` and this file from scratch.
Whoever next touches a feature area above should update that row's
"Covered by"/"Gaps" columns in the SAME merge (see `AGENT_POLICY.md`'s
build/review definition-of-done).

2026-08-02, alongside tracker item
`for-product-add-a-prod-smoke-assertion-f-bqt2sh` (a direct follow-up to
`for-product-urgent-reopen-video-repair-p-cyp8np`'s module-load-crash
incident): added `test/prod-smoke/media-serving.test.js` — a real,
currently-deployed-origin check that `video-file.mjs`/`image-file.mjs`
actually serve stored media (200/302 + correct content-type) against a
known-good production video fixture, plus a clean-404 check. Image
coverage is opt-in via `MEDIA_SMOKE_IMAGE_KEY` and currently skips (no
verified real image key was available to hardcode) — see that Own-storage
media re-host row above and the test file's own header comment.

2026-08-03, alongside tracker item
`for-product-media-library-founder-08-03--6h7fmv` (founder's last open
note on the media library page: "also show the text they wrote"): added
the per-card `storyText` field to `admin-media-library-data.js`'s
`itemsForRecord` (falling back to `caption` for a pre-split dream, never
`promptText`) and its rendering in `media-library-x7q4.html`'s `cardHTML`,
reusing `css/styles.css`'s `.interp-text` class verbatim — see the Media
library row above.

2026-08-03, alongside tracker item
`for-product-build-whatsapp-morning-captu-skez3n` (the template-submission
+ sender-wiring follow-up round, per Manager's explicit GO): added
`admin-submit-whatsapp-template.js` (a deliberately-not-automatic one-off
tool that submits the founder-drafted morning-capture template to Meta's
Graph API's Message Templates endpoint, MARKETING category), and retrofitted
`send-whatsapp-morning-capture.js`'s `scanAndSend` with a PILOT-ONLY GATE
(`isPilotOnly`/`isSendEligible`, gated on the `WHATSAPP_MORNING_CAPTURE_GO_LIVE`
env var, defaulting closed) so the real sender cannot reach any real end user
until Manager/Ron explicitly flips it — reusing `lib/test-identity.js`'s
existing founder-alias allowlist rather than a new one. See the Morning
Capture Ritual row above.

2026-08-03/04, alongside tracker item
`for-product-email-redesign-unsubscribe-l-16ysmp` (founder ask: "make the
email content look better + add unsubscribe" — branch
`email-redesign-unsubscribe-16ysmp`): added the unsubscribe mechanism
(`unsubscribe-email.js`, `lib/unsubscribe-token.js`'s stateless per-email
HMAC token, `lib/email-suppression-store.js`) and wired it into the two
retention/marketing senders in scope (`lib/first-dream-email-sender.js`,
`dream-webhook.js`'s `sendReadyEmail`), plus rebuilt both templates on a
new shared night-aesthetic shell (`lib/email-layout.js`). Founder-approved
follow-up (08-04): the first-dream retention send now gates on the
captured first-frame thumbnail — send immediately once it's available,
or send anyway with the existing no-thumbnail template variant once 3
minutes have elapsed since the trigger (the no-thumbnail template is a
real fallback, not dead code); footer now carries the real CAN-SPAM
mailing address (10 Dgania, Herzliya, Israel), styled small/faint. See
the Retention row above for the full writeup and its test coverage.

2026-08-07, alongside the same tracker item's closing round (branch
`mood-music-candidates`): the mood-bed audition ran its full course in one
day — the 12 fal-ai/stable-audio candidate tracks were generated and
committed, the founder picked one winner per mood (peaceful-A, joyful-B,
dreamy-A, mysterious-B, tense-B, epic-B), the winners were promoted to
`assets/music-beds/moods/<mood>.wav`, `js/music-bed.js`'s `MOOD_FILES`
went live with all six (selection tier 1 now actually fires), and the six
losing candidates plus `bed-audition-x7q4.html` itself were deleted per
the item's own no-dead-assets instruction. The mood-keyed-beds row above
was rewritten to the live state: its coverage column now describes the
live-tier-1 + assets-integrity tests that replaced the audition-page
picking-flow tests (removed with the page), and its gaps column narrowed
to the one remaining REAL-MODE post-deploy phone check.

2026-08-09, tracker item `follow-up-home-html-checkout-return-has--hm8na5`
(branch `fix-home-checkout-return-confirmation`): fixed two real weaknesses
in `home.html`'s `handleCheckoutReturn` (the post-Dodo-checkout token-credit
confirmation that auto-resumes a generation blocked by out-of-tokens),
found relative to the already-fixed reference pattern in `shop.html`'s own
`startCreditConfirmation` (tracker item
`for-product-webhook-p0-reframed-by-found-peytt8`):
1. **Threshold bug (the serious one)** — the confirmation threshold used
   to be just `pendingPurchase.cost` (the blocked action's own cost, e.g.
   100), so ANY unrelated balance bump clearing that much smaller number
   (a same-time daily claim, a stale read) could fire the auto-resume
   before the real purchase webhook (300+ tokens) had actually landed.
   Rebuilt to mirror shop.html's two-baseline scheme:
   `Math.min(balanceBefore, arrivalBalance) + packTokens`, never
   fabricating a baseline when neither is available (falls back to
   `draftMediaTypeCost()` off the real draft rather than shop.html's flat
   `1`, since a marker-less return here still has a real action to
   resume). `js/purchase-sheet.js`'s pending-purchase marker (built in
   `wireBuyButton`) now also carries `balanceBefore` — the sheet's own
   `opts.balance` at the moment it opened.
2. **Timeout UX** — `onTimeout` used to be a single 2.2s toast with no way
   back to it. Replaced with a small, persistent, on-page card
   (`#checkout-confirm-card`, new markup/CSS in `home.html`) offering a
   real "Check again" retry, never silently vanishing; a later real credit
   (page revisit, or the retry itself) still resolves normally. Ported
   shop.html's `creditConfirmAttempt`/`isCurrent()` monotonic-attempt
   guard and its baseline-retry-then-poll two-phase shape (one shared
   `window.__TEST_POLL_OVERRIDES__` budget across both phases, same as
   shop.html) so a superseded attempt (dismiss/pagehide/retry) can never
   resume generation or repaint the card on behalf of a stale attempt.
   `home.html`'s own auto-resume sequence
   (`fireBlockedActionResumed`/`beginOptimisticFreshGeneration`/
   `onGenerationSettled(startFreshGenerationFromDraft())`/
   `pollForPendingJobToAppear`) and the pre-existing `pagehide`
   poll-cancellation guard are unchanged, just re-scoped to cover both
   phases. Grepped the repo for other checkout-return/token-credit-poll
   call sites while fixing this — `home.html` and `shop.html` remain the
   only two (`processing.html` no longer exists; nothing else calls
   `PurchaseSheet.pollForCredit` directly) — no third drifted copy found.
   `test/out-of-tokens-purchase-sheet-behavioral.test.js`'s checkout-return
   tests were updated to assert the fixed threshold (adding
   `balanceBefore` to seeded markers and adjusting mocked balances to the
   real `baseline + packTokens` target) plus new regression coverage: an
   unrelated bump clearing the OLD threshold but not the new one, the
   "Check again" retry actually resuming once the real credit lands, and
   a true no-baseline-ever (marker-less + persistently failing balance
   read) case resolving into the honest card rather than a hang or a
   fabricated confirmation.

2026-08-10, alongside tracker item `delete-account-js-never-cleans-up-
block--luhb4f` (found auditing a different, already-closed item): a
deleted account's `block-store.js` record (who it had blocked) was never
cleaned up by `delete-account.js` at all — not a sub-marker gap like the
achievement-marker bug this was found alongside, a whole per-account
record simply never wired into the deletion flow. Fixed with a new
`block-store.js#deleteBlocksForUsername` export (mirrors
`dreamStore.deleteAllPrivateDreams`'s own never-throws, safe-to-run-last
shape) called as delete-account.js's new step 7. `test/delete-account.
test.js` gained a real regression test proving the actual hazard this
closes: a brand-new account re-registering a FREED username (this
codebase has no username-reuse cooldown) no longer inherits the deleted
former holder's block list — stashed the fix and confirmed the new test
fails without it before restoring.

2026-08-10, tracker item `for-product-track-avg-video-generation-t-2ci8ue`
(founder ask, instrumentation half only — see the "Generation (video/
image)" row above for the full writeup): added `duration_ms` to
`video_created`, and two new events, `generation_slow` (3-minute threshold
alert) and `generation_failed` (the true fail/vanish rate, covering every
terminal failure shape including the E304 "vanish"/dream-sync-unconfirmed
shape). New `test/generation-instrumentation-behavioral.test.js`. No
wiring bug found in the `tokens_refunded` pipeline on close read — see
that row's own "Investigation finding" for the full reasoning.
