# Test Registry

A living map of **feature/surface → what actually covers it → known
gaps**, seeded honestly from the real feature set as of 2026-08-01 (not
an aspirational list) — see `docs/JOURNEY_MANIFEST.md`, `home.html`, and
`netlify/functions/*.js` for the source of truth this was built against.
Gaps are listed on purpose, not papered over.

**Discipline rule (see `AGENT_POLICY.md`'s build/review sections):** a
feature merge must update this file's relevant row(s) — new coverage
added, or what's covered redefined — as part of that same merge. `review`
checks this happened.

Two kinds of automated coverage exist in this repo, and most rows below
cite one or both:

- **`test/*.test.js`** (`npm test`) — mocked backend, runs against a local
  static server. Proves the app's own client/server logic is correct in
  isolation. This is the vast majority of this repo's test coverage.
- **`test/prod-smoke/*.test.js`** (`npm run smoke:prod`) — real backend,
  runs against a real origin (default production). Proves the currently
  *deployed* site actually works end to end for a real new user. Deliberately
  thin — a handful of end-to-end flows, not exhaustive per-feature coverage
  (see that directory's own `README.md`).

A row with only `test/*.test.js` coverage and no prod-smoke row is
**normal, not a gap** — prod-smoke is intentionally narrow. A row with
**no coverage at all** is a real gap, called out as such.

## Auth & accounts

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Signup (`register-account.js`, `js/store.js#signup`) | `test/signup-screen-behavioral.test.js`, `test/funnel-real-chain-behavioral.test.js`, `test/facebook-login-signup-behavioral.test.js`, `test/account-store.test.js`, `test/wizard-ui-behavioral.test.js` | — |
| Screen 13 layout — vertical distribution across the real viewport (`.fn-signup-stage`/`.fn-stage-spacer`, `fnStageHtml`) + the Facebook "or" divider (`facebookOrDividerHtml`, `#fn-fb-or-divider`) between the Facebook button and the email field (tracker item `for-product-signup-screen-layout-fixes-f-3hclmj`, founder mobile walk 2026-08-03 — "SPREAD IT OUT" + "ADD AN 'or' DIVIDER"; **founder-approved and merged 2026-08-04**, with one follow-up fix from his preview verdict: the button and divider are now two separate `.fn-stage-group`s instead of one concatenated group, so the same `.fn-stage-spacer` flex mechanism gives the divider an equal, auto-sized gap above and below it — genuinely centered in the whitespace between the Facebook button and the email field, not glued to the button). Covers all three screen-13 arms this item named: `unified` and `reveal` (both get the divider, since both render the Facebook button) and the passwordless wall's email step AND its own "code" step (the "passwordless-code wall" the item calls out directly) — passwordless gets the spacing fix only, no divider, since it never renders the Facebook button at all. Superseded the earlier Direction Y "no divider" decision recorded in `start.html`'s own LAYOUT NOTE comment (updated in place, not left stale) | `test/signup-screen-layout-spacing-behavioral.test.js` (`.fn-signup-stage` present with real, non-collapsed distribution spacers and the visible content spanning a majority of the viewport on all 3 arms' email steps plus the passwordless code step; the divider present-and-correctly-positioned on `unified`/`reveal`, absent on passwordless), `test/facebook-login-signup-behavioral.test.js` (existing "feature flag ON" test asserts the divider exists and is correctly positioned; sibling test confirms it disappears along with the button when the flag is off) | — |
| Screen 13 'reveal' challenger — forming veil, "Send me my dream" copy, save-framing reassurance, graceful preview degradation (tracker item `for-product-build-now-email-wall-fix-1-n-6qvebd`, mock-only — `SIGNUP_REVEAL_CHALLENGER_LIVE` is `false`) | `test/signup-reveal-variant-behavioral.test.js` | Not yet live in the real A/B (founder must approve the mock — `reveal-variant-mock-x7q4.html` — first); no PostHog experiment-readout coverage since nothing is actually splitting real traffic yet |
| Forming veil's animated nebula/aurora background — replaces the old static flat-gradient `.fn-forming-frame` fill with a CSS-only, style-tinted (`fnstyle-cartoon`/`cinematic`/`anime`/`realistic`/`default` via `formingTintClass`), continuously-drifting `.fn-form-nebula` layer (tracker item `for-product-forming-veil-replace-the-fla-nwbe60`, founder feedback: "something more interesting and moving there than just a square color"; **founder-approved (the animated nebula "After" version) and merged 2026-08-04**, along with his verbatim reassurance-copy change on the same screen — the non-record-mode line above the email field now reads "No need to wait — we will send your dream once it's ready to your email:", replacing the old "Your dream is saving to your email... it'll be waiting..." copy) | `test/forming-veil-nebula-behavioral.test.js` (`formingTintClass` pure-logic coverage incl. fail-closed default, the new `.fn-form-nebula` structure replacing the flat background with no video/GIF asset requested, four distinct per-style resolved colors + default fallback with no style chosen, real computed-style change over real wall-clock time proving the keyframes actually run — not just declared — `prefers-reduced-motion` disabling it, and that the pre-existing best-effort real-still-frame swap-in via `tryShowFormingPreview`/`video-status.js` is unaffected), `test/signup-reveal-variant-behavioral.test.js` (`.fn-forming-frame`/`.fn-form-caption`/`.fn-forming-still` coverage still passes against the new markup; updated to assert the new reassurance copy verbatim) | Only applies to the reveal arm today (`formingFrameHtml` isn't called anywhere else) — `home.html`'s separate `.day0card` forming veil is a different, unrelated implementation and out of scope here |
| Passwordless signup — email-only entry, no password ever (`register-account-passwordless.js`, `js/store.js#signupPasswordless`, `start.html`'s `renderScreen13Passwordless`, tracker item `for-product-build-passwordless-signup-fo-at2fko`, founder-decided hybrid, mock-only — `SIGNUP_PASSWORDLESS_LIVE` is `false`) | `test/passwordless-signup.test.js` (create-vs-resolve identity resolution, offline-fallback local commit shape, rate limiting), `test/signup-passwordless-behavioral.test.js` (real Chromium run: no password field ever rendered, email-only submit, reaches screen 14, CompleteRegistration fires exactly once at the same depth via the real `track-conversion.js` call — see `docs/EVENT_TAXONOMY.md` — local state has `password:null`/`emailVerified:false`, the deferred-sheet session marker is pre-set, and undeliverable-email validation) | Not yet live for real traffic (feature-flagged off, same posture as the 'reveal' challenger above — no mock-review artifact exists for this arm yet, unlike 'reveal's `reveal-variant-mock-x7q4.html`, since the founder's own spec (his exact words, tracker item body) already fully specified the UI shape rather than leaving it to a design pass); no `test/prod-smoke/` coverage (payments/signup-variant paths are out of that suite's intentionally narrow scope, same as every other signup arm) |
| **SECURITY FIX, 2026-08-02** (round-2 independent review finding, real, fixed same day): the RESOLVE branch (an already-registered email POSTed to `register-account-passwordless.js`) used to mint a fully usable `authToken` with ZERO proof the caller controlled that inbox — a one-request account takeover of any existing account, given only its email address. Fixed: that branch now sends a fresh code/link and returns NO authToken/username at all (`pendingVerification:true`); a real session is only ever granted via the NEW `login-with-email-code.js` endpoint (a genuine login, mailed-code-as-credential) or a clicked `verify-email-link.js` link (which now also mints a `lib/session-transfer-token.js` token, same mechanism `facebook-oauth-callback.js` already uses once IT has proven identity, redirecting to `/home.html` — the one page that already consumes `?bt=` — instead of the old default `/profile.html`, which never did). `start.html`'s passwordless arm gained a new inline code-entry step (`renderCodeStep`) for this branch. See `netlify/functions/register-account-passwordless.js`'s own header comment for the full incident writeup. | `test/passwordless-signup.test.js` (the exact attacker scenario: a caller who is not the account's owner gets no authToken/username, the victim's own original token is completely undisturbed; `login-with-email-code.js`'s full error surface incl. no-enumeration between "wrong code" and "no such account"; `verify-email-link.js`'s `bt` token is real and independently consumable via `verify-session-transfer.js`), `test/signup-passwordless-behavioral.test.js` (real Chromium: an already-registered email shows the code step and leaves the browser genuinely signed OUT, a correct code completes login WITHOUT firing CompleteRegistration, a wrong code shows an inline error and never advances) | — |
| Deferred email verification — 6-digit code (`verify-email-code.js`, `resend-verification-code.js`, `login-with-email-code.js` for the security-fix login-via-code path above), implicit verification via clicked email link (`verify-email-link.js`), the "next visit" deferred-prompt sheet (`js/email-verify-sheet.js`, wired on `home.html`), `lib/email-verification-store.js` | `test/passwordless-signup.test.js` (code correctness incl. attempt-limiting, authToken-only identity for `verify-email-code.js` — never a client-claimed username, link-token single-use, open-redirect guard on `verify-email-link.js`'s `redirect` param, resend no-op-if-already-verified), `test/email-verify-sheet-behavioral.test.js` (real Chromium run against home.html: the sheet auto-opens for an unverified account on a fresh visit, a correct code marks it verified and shows the confirmation view, a wrong code shows an inline error without closing, "Resend code" calls the real endpoint, a verified/password-based account is never shown the prompt, and the session-already-offered marker correctly suppresses a repeat show) | The "before publish"/"before purchase" DEFERRED-PROMPT triggers (as opposed to the server-side GATE, which both enforce — see the gate-list row below) are not wired to interactively open `js/email-verify-sheet.js` in this pass — only the "next visit" trigger is; publish is fire-and-forget client-side (see `js/store.js`'s `syncPublishedDreamToFeed`) so has no reaction point today. `shop.html`'s E10 branch DOES open the sheet reactively (see that file's `purchasePack`), but has no dedicated behavioral/Playwright test of its own yet — only the server-side E10 rejection is covered above. |
| Gate list — accounts with `emailVerified:false` are blocked from purchasing (`create-checkout-session-dodo.js`, E10) and publishing to the shared feed (`publish-dream.js`, E8); every pre-existing account-creation path (`register-account.js`, `facebook-oauth-callback.js`) is unaffected/still defaults to verified — see `lib/account-store.js`'s own GATE LIST header comment for the authoritative, exhaustive list | `test/passwordless-signup.test.js` | — |
| Bounce handling — Resend `email.bounced` webhook, hard (Permanent) bounces only, flags `emailBounced` (`resend-bounce-webhook.js`, `lib/resend-webhook-verify.js`) | `test/resend-bounce-webhook.test.js` (real, locally-computed Svix/HMAC-SHA256 signatures — the actual algorithm, not a stubbed-out check) | **Cannot be exercised end-to-end against real Resend** — no live Resend account/webhook subscription in this sandbox; someone with Resend dashboard access still needs to register this endpoint's URL as a webhook (subscribed to `email.bounced`) and set `RESEND_WEBHOOK_SECRET` in Netlify before it ever receives a real delivery — same "can't do this from the sandbox" class as Dodo/Netlify-env-var setup elsewhere in this registry. `emailBounced` is also currently informational only — nothing in this codebase yet refuses an action off it. |
| Login (`account-login.js`) | `test/account-store.test.js`, `test/rename-migration-login-behavioral.test.js`, `test/facebook-login-signup-behavioral.test.js`, `test/password-reset-account.test.js` | — |
| Facebook Login / OAuth (`facebook-oauth-callback.js`, `facebook-complete-signup.js`) | `test/facebook-oauth-callback.test.js`, `test/facebook-login-signup-behavioral.test.js`, `test/account-store-facebook.test.js` | — |
| Password reset (`request-password-reset.js`, `verify-password-reset.js`) | `test/password-reset-account.test.js` | — |
| Account deletion (`delete-account.js`) | `test/delete-account.test.js`, `test/delete-account-behavioral.test.js` | `test/prod-smoke/session.test.js` (real delete, used for its own probe-account cleanup — proves the endpoint works against real prod, not a dedicated feature test) |
| Auth tokens (`lib/account-auth-token.js`) | `test/account-auth-token.test.js` | — |
| Cross-device session transfer (`create-session-transfer.js`, `verify-session-transfer.js`) | `test/session-transfer.test.js`, `test/session-transfer-behavioral.test.js` | — |
| Account rename/consolidation (one-off admin tools) | `test/admin-diagnose-rename-conflict.test.js`, `test/consolidate-accounts-client-migration-behavioral.test.js`, `test/admin-restore-founder-email.test.js` | These are one-off hardcoded tools per `AGENT_POLICY.md`'s "remove one-off admin panels after use" policy — coverage is expected to be retired alongside the tool itself, not maintained indefinitely. |

## Onboarding funnel (`docs/JOURNEY_MANIFEST.md`)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Journey sequence itself (both journeys' documented screen order) | `test/journey-manifest.test.js` (source-level, pins `SCREEN_RENDERERS`) | — |
| Journey 1 entry render, live on a real deployed origin | `test/prod-smoke/journey1-funnel-render.test.js` | Only checks screen 13 renders — doesn't complete a second full signup (see that file's own header comment for why) |
| Journey 1 full flow (`start.html`) | `test/funnel-signup-navigation-token-guard-behavioral.test.js`, `test/funnel-meta-attribution-cookies-behavioral.test.js`, `test/facebook-login-signup-behavioral.test.js` | — |
| Journey 2 full flow (`wizard.html`, organic) | `test/wizard-ui-behavioral.test.js`, `test/wizard-chips.test.js`, `test/wizard-action-chip-curation-behavioral.test.js`, `test/wizard-photo-upload-sheet-token-behavioral.test.js`, `test/record-mode-behavioral.test.js` | — |
| Subject step (Step 1, "Who's the dream about?") multi-select — tracker item `for-product-wizard-characters-step-is-si-paxp07`, founder repro 2026-08-02: selecting a character then adding another used to UNCHECK the first (single-active-subject model treated as one-choice). Fixed to real independent toggles: any number of staged characters (`staged.subjectCharacterIds`) compose with at most one of the four "other" chips (`subjectOtherKind`, still single-select among just those four) — see `wizard.html`'s own design note above `renderSubject` for the full reasoning and `js/wizard-chips.js`'s doc comment for the new `subjects`-array input `assembleCaption`/`buildDeterministicStory` both accept (create.html's own separate single-select "Build it" subject step is unaffected — still uses the old singular fields, still covered above) | `test/wizard-chips.test.js` (unit-level `subjects`-array coverage: single-entry array matches the singular path byte-for-byte, the founder's own 3-subject worked example, a photo character combined with described characters — correct phrase/id split preserved per-entry, 2-subject "and" joining, empty-array fallback, `buildDeterministicStory`'s equivalent "with X, Y and Z" joining incl. the "me" + one-other "with" upgrade), `test/wizard-ui-behavioral.test.js` (3 tests: selecting Me + "Someone I know" + "A stranger" all stay selected together — the literal founder example; deselecting one of several via a toggle-off tap then re-selecting it via a DIRECT `#subject-chip-row` chip tap, independent of the character-sheet path; the full end-to-end trace — all three selections reaching the real `start-pending-generation.js` POST body's `caption`/`characters`/`characterIdsForGeneration`, not just DOM selected-state), `test/sheet-dismiss-behavioral.test.js` (section 7c — the same founder scenario exercised through a NORMALLY-opened character sheet, regression coverage for the interaction with the 75ob70 sheet-dismiss fix, since this exact step was that fix's own originally-reported instance) | — |
| **Layout-B wizard redesign** (tracker item `for-product-founder-picked-layout-b-flo--lks7mj`, founder "go live" 2026-08-07 after seven mock rounds on the since-deleted `mock-wizard-b-x7q4.html`) — `wizard.html`'s full pill-row/emoji-chip/progress-dot skin plus its two founder-directed interaction changes: (1) single-select steps (Setting's place pick, Action, Mood, Style — Style per mock round 4) AUTO-advance ~260ms after a tap, with "+ Something else" write-ins, Day/Night, POV, and the "+N more" expander all exempt, and a Continue tap inside the window advancing exactly once; (2) the Subject step's Me row stages+selects immediately AND — since **founder round 8** ("opens nicely for Someone i know but not for Me and it should including the option to upload image") — opens the character sheet in self mode (no name field, describe + photo upload; Cancel exits cleanly with Me still selected and no details, per the 08-04 details-optional ruling, tracker items `for-growth-founder-ruling-08-04-me-someo-w8k8xr`/`for-product-paired-with-growth-funnel-pr-22o77k`; the round-7 inline Me detail input was removed with it — "Someone I know" rows keep theirs; typed/sheet details flush onto the real Me character at signup, never re-asked; a detail-less Me never blocks and never creates a half-empty store character). **Round 8 also**: (a) the editable recap is its OWN step before the wall again (`renderRecap`, "Here's your dream — make it yours", pencil affordance, "Looks good →"; the wall dropped its folded-in copy) — it fires the sanctioned `wizard_recap_viewed {}` instead of a renumbered `wizard_step_viewed` (see `wizard.html`'s `STEP_ANALYTICS`: Subject..FreeText stay 1-6, the wall stays 7, byte-stable); (b) fresh entries (`?entry=index`/direct — never FB return legs or `?resume=1`) meet the mock's screen-1 entry chooser (build/write/speak) as a pre-step outside `SCREEN_RENDERERS`, firing `wizard_entry_mode_chosen {mode, surface:'wizard'}` only — Build → normal flow, Write → jumps to the free-text step with the typed dream going through VERBATIM as the caption (create.html Write-it's contract, no chip-default 'flying' in a written prompt), Speak → `create.html?record=1` via its login gate (no new pre-signup recording surface; hidden in FB/IG webviews). `create.html`'s Build/Write/Record chooser wears the same pill grammar (flash-then-advance on real taps; Record stays synchronous for the getUserMedia gesture). `SCREEN_RENDERERS` re-pinned with `renderRecap` by `test/journey-manifest.test.js` + `docs/JOURNEY_MANIFEST.md` (updated same-commit per that test's contract). One sanctioned analytics ADDITION rode this branch (founder ask 2026-08-07, via Manager): `wizard_entry_mode_chosen` `{ mode: 'build'|'write'|'record', surface: 'create' }`, fired once per committed tap in each of create.html's three chooser handlers (deep-link paths reusing those handlers fire it too; the one direct `startRecordingUI()` path that bypasses `#choice-record`'s handler is deliberately untracked) — measures the chooser %-split and per-mode signup rate | `test/wizard-ui-behavioral.test.js` (the dedicated Layout-B + round-8 sections: auto-advance + no-double-advance; Me tap opens the self sheet — no name field, photo option present, old inline input gone — with the sheet description traced to caption + store flush; Cancel-keeps-Me-selected end-to-end; the recap STEP's editability — pencil affordance, hand-edit beating the late LLM rewrite, surviving Back-from-the-wall, riding the submission as storyText; the entry chooser — shown fresh with the chooser dot active and zero `wizard_step_viewed`, Build firing `wizard_entry_mode_chosen {surface:'wizard'}` once then Subject's usual step:1, Write-mode verbatim-caption trace, Speak routing + IG-webview hiding, and no chooser on `?resume=1`/FB return legs; plus every pre-existing wall/flow test re-walked under the new grammar), `test/wizard-action-chip-curation-behavioral.test.js` (curation re-proven under auto-advance, incl. the write-in row NOT auto-advancing), `test/split-prompttext-storytext-behavioral.test.js`/`test/funnel-real-chain-behavioral.test.js`/`test/sheet-dismiss-behavioral.test.js`/`test/wizard-photo-upload-sheet-token-behavioral.test.js` (updated walks: sheet now opens via the Me row's pencil), `test/home-behavioral.test.js`/`test/create-record-inapp-webview-guard-behavioral.test.js` (chooser routing unchanged under the pill restyle), `test/wizard-ui-behavioral.test.js` (entry-chooser analytics: `wizard_entry_mode_chosen` fires exactly once per Build/Write/Record tap with the right `mode`/`surface`, via the PostHog stub-queue read) | Real-mode mobile walkthrough on the deployed branch preview still pending before founder-ready (AGENT_POLICY REAL-MODE gate); visual verification so far is real-Chromium mobile-viewport screenshots against a local serve |
| Entry-routing guard: a logged-out visitor with stale local account history (a browser that already held a real account, even after logout — `DreamStore.hasLocalAccountHistory()`) never lands mid-funnel/wizard, always the welcome/choice screen (`index.html`) instead — tracker item `for-product-life-origin-generate-handoff-founder-repro-8yc4wm` | `test/life-origin-generate-handoff-behavioral.test.js`, `test/route-organic-to-wizard-behavioral.test.js` (genuinely-first-time-visitor control case) | — |
| Record-intent preservation across the login round-trip: a logged-out `create.html?record=1` deep-link (Speak/record-it entry) must survive the sign-in guard's redirect and land back in the actual recording flow, not the plain Build/Write/Record choice screen — tracker item `for-product-speak-path-from-the-wizard-e-ycos08` | `test/record-mode-behavioral.test.js` (2 tests: the full `?record=1` round-trip via `login.html`'s own `?next=`, and the plain-path control case) | — |
| Journey 2 full flow, live, real backend, real probe account | `test/prod-smoke/session.test.js` (test 1) | — |
| Generate-during-signup real chain (client → real server handlers) | `test/funnel-real-chain-behavioral.test.js` | — |
| Email deliverability/availability check (`check-email.js`) | `test/check-email.test.js`, `test/email-domain-check.test.js` | — |
| Pending-generation adoption (`start-pending-generation.js`, `claim-pending-generation.js`, `pending-dreams.js`) | `test/pending-dreams.test.js`, `test/funnel-real-chain-behavioral.test.js`, `test/claim-and-verify-pending.test.js` | — |
| A2HS / install nudge | `test/a2hs-install-nudge-journey-behavioral.test.js`, `test/install-first-door-behavioral.test.js` | — |
| A2HS small nudge card (`js/install-nudge.js`) — "install" vs "add to your home screen" CTA-copy unification, matching its own head's phrasing | `test/home-hero-whisper-mky-copy-behavioral.test.js` | — |
| Service worker update lifecycle + stale-page prevention (`sw.js`, `js/pwa.js`, `scripts/generate-version.js`'s CACHE_VERSION auto-stamping) — tracker item `for-product-urgent-founder-gets-stale-pa-6dn54z`, four real founder-device staleness incidents across 2026-07-29/08-02/08-03/08-04. Three rounds: (1) `registration.update()`/`controllerchange` wiring + the 30-minute-hidden `visibilitychange` reload fallback (iOS Safari tab-freeze case); (2) CACHE_VERSION now auto-stamped with a fresh, deploy-unique string on every real build (reusing `version.json`'s own commit-based version string, not a second scheme) so sw.js's bytes reliably differ every deploy — a manual bump left most routine content-only deploys byte-identical, so the browser's own update-check had nothing to find; also forces `cache:'no-store'` on the outbound network-first fetch (bypassing the browser's separate HTTP disk-cache layer, not just this file's own Cache Storage usage) and fixed a `response.ok`→`response.status===200` bug (a 206 Partial Content response would throw on `cache.put`); (3) a SECOND, SW-lifecycle-INDEPENDENT safety net (`js/pwa.js`'s `initVersionFallback()`/`checkVersionFallback()`) — rounds 1-2 above are confirmed live and correct server-side on both production origins, but only ever reach a device with an already-registered OLD service worker if the browser's own `registration.update()` byte-check succeeds at least once, which is specifically unreliable in Safari/WKWebView (the founder's real device context); this round polls `/version.json` directly on page load and on the same `hiddenStaleThresholdMs()`-gated `visibilitychange` pattern round 1 already uses (no new timing scheme), comparing against a `localStorage`-recorded last-seen version, and reuses the shared `reloadWhenSafe()` guard (one-shot, typing/recording-safe) on a genuine mismatch — never touches the service worker at all, so it keeps working even if that whole layer is broken | `test/pwa-sw-update-behavioral.test.js` (real Chromium: update/controllerchange/visibilitychange-reload lifecycle, plus a genuine on-disk sw.js byte-change end-to-end via a symlinked temp deploy root — proves the real mechanism, not a synthetic event), `test/sw-fetch-handler.test.js` (a `vm`-sandboxed load of the real, unmodified sw.js source proving the exact fetch options/caching decisions its handler issues — Playwright cannot intercept SW-internal fetches once a worker controls the page, so this is the only way to inspect them without a real browser), `test/generate-version.test.js` (`stampServiceWorkerCacheVersion` unit tests incl. against the real committed sw.js, plus an end-to-end run of the real build script proving version.json and sw.js always agree and differ across two simulated deploys), `test/pwa-version-fallback-behavioral.test.js` (round 3, real Chromium: first-ever check for a browser records a baseline with no reload; a genuine on-disk `version.json` byte change — same symlinked-temp-deploy-root technique as the sw.js test — triggers exactly one reload once the tab resurfaces past the shared stale threshold; the typing guard is shared and respected; a `route.abort()`-forced fetch failure never throws, never fabricates a recorded version, and never reloads; a multi-tab `storage`-event fix — review finding: `LAST_KNOWN_VERSION_KEY` is one shared-per-origin key, so without this a sibling tab whose own check fired later would see the first tab's write and silently skip its OWN reload — proven with two pages sharing one browser context, where the second page never runs its own version check at all and can only reload via the sibling's localStorage write) | No real WebKit/Safari engine exists anywhere in this repo's test tooling (Chromium only) — every test here proves the MECHANISM works, not that a real iOS Safari/in-app-webview client behaves identically; this is an accepted, structural limitation (see each file's own header comment), not a gap to close in this pass. This is a repeat founder-reported device bug, so per this codebase's standing QA rule it stays `waitingFor:ron` for final on-device confirmation even after merging. |

## Home (`home.html`, 2026-08-01 module set)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Overall page / round-4 rebuild | `test/home-behavioral.test.js`, `test/home-round4-behavioral.test.js` | — |
| Day-0/active dream room card (`#day0-card`) | `test/home-day0-dream-card-behavioral.test.js` (incl. the forming caption's new copy — the old "Ready when your dream is" line is gone), `test/edit-regeneration-forming-state-behavioral.test.js`, `test/sage-during-generation-behavioral.test.js` (the forming state's own interpretation offer `#d0-sage` — see the Interpretation/Chamber section's row for the full picture) | — |
| Ritual module's daily claim button + bonus-tokens countdown (`#ritual-claim`/`#ritual-countdown`) — the countdown ("Next N free tokens in Xh Ym") re-added inside the ritual module itself (tracker item `for-product-bonus-tokens-countdown-vanis-qm96xc`, founder feedback: it had quietly dropped out of an earlier "Home round N" rebuild), lives INSIDE `#ritual` rather than as a separate page element, hides while claimable (the claim button takes its spot), and ticks live via a 60s interval that re-fetches once `nextClaimAt` actually passes (same pattern as `shop.html`'s `#shop-countdown`) | `test/home-behavioral.test.js` | — |
| `?generate=1` fresh-in-app-generation handoff — the room card/Tonight-hero must show forming IMMEDIATELY on a brand-new (non-edit) submission, not only once the real pendingJob lands seconds later (worst on a first-ever-on-this-browser-origin account, where nothing else on Home hints anything is happening) — tracker item `for-product-life-origin-generate-handoff-founder-repro-8yc4wm` | `test/life-origin-generate-handoff-behavioral.test.js` | — |
| Ritual module's streak CONSTELLATION (`#ritual-constel`'s `<svg class="constel">`, lit stars + connecting lines) — tracker item `for-product-regression-founder-screensho-17vxr2`, founder-reported missing on his real device despite the code being byte-identical to every approved mock. Real root cause a source-level diff never caught: the `<svg>` was missing `width="84" height="46"` attributes every mock includes, and the one CSS rule referencing `.constel` never actually applied to it (it targets a direct flex child of `.ritual-head`, but the real flex child is the wrapping `#ritual-constel` span, not the svg nested inside it) — so the svg's computed size collapsed to 0×0 in every branch, regardless of streak/week-count data | `test/home-behavioral.test.js` (asserts a REAL non-zero rendered bounding box, not just DOM presence, across all four `renderRitualModule()` branches plus the founder's exact reported state — 5 nights/0-of-3-this-week/claimable) | — |
| Tonight hero "empty sky" — moon + cycling whisper only, no streak-sky/constellation stars (dropped per founder amendment, tracker item `for-product-build-ship-today-founder-app-zn9zyy`; the ritual module's own constellation is unaffected, still covered below) — plus the whisper's own no-overlap-at-any-viewport guarantee | `test/home-hero-whisper-mky-copy-behavioral.test.js` | — |
| Tip of the Day, Get Inspired, Consider Publishing, Make It Yours, ritual/streak module — as a set, live on a real deployed origin | `test/prod-smoke/session.test.js` (test 2) | Per-module deep behavioral coverage (copy variants, edge/empty states, exact interaction details) is NOT this suite's job — check for a dedicated `test/*.test.js` file per module before assuming full coverage; several of these modules (Get Inspired, ritual/streak beyond the daily claim itself) currently have **no dedicated `test/*.test.js` file** of their own — home-behavioral/home-round4-behavioral cover the page broadly, not module-by-module |
| Make It Yours — "install" vs "add to your home screen" copy unification (reward line, done-state, inline install-row CTA + its supporting text, webview-fallback note) — tracker item `for-product-build-ship-today-founder-app-zn9zyy` | `test/home-hero-whisper-mky-copy-behavioral.test.js` | — |
| Bottom dock (`js/bottom-nav.js`, "Bar A") | `test/barA-nav-rollout-behavioral.test.js` | — |
| Webview escape nudge | `test/home-webview-escape-behavioral.test.js` | — |
| Tonight hero's primary pill (`#tonight-pill`) now lands on `create.html`'s EXISTING Build/Write/Record choice screen instead of deep-linking straight past it into the chip wizard (tracker item `for-product-founder-ask-08-05-tell-your--vmfvsk`, founder preview catch: "when I clicked the Tell your dream button I was redirected to wizard without the choice first or without the suggestion to record or write instead"). Fix: the pill's click handler navigates to a bare `create.html` (was `create.html?build=1`) — `create.html`'s existing, already-tested default landing (`#create-select`, unparameterized) is unconditionally the 3-choice screen with no signed-in/returning-user bypass, so this is a pure reuse of already-shipped UI, not new surface area. The quiet Write/Speak/No-recall row underneath the pill is intentionally left in place (not redundant: "No recall" has no equivalent on the choice screen, and Write/Speak remain a faster one-tap shortcut via their own still-live `?write=1`/`?record=1` deep-links). `create.html`'s `?build=1` deep-link handling itself is left in place too, unremoved — no longer called from anywhere in this app today, but kept as an established, already-tested convention (mirrors `?write=1`/`?record=1`) available to any future entry point that deliberately wants to skip the choice screen | `test/home-behavioral.test.js` (the pill navigates to a bare `create.html` with no query string, lands showing all three real choice cards with the wizard NOT yet visible, and "Build it" from there still reaches the chip wizard's real step 1; a second test confirms "Write it"/"Record it" from the pill's choice screen still reach their correct existing destinations, `#create-write`/`#create-record`) | Held for founder preview before merge (changed-screen rule) — not yet merged to `main` |

## Generation (video/image)

| Feature/surface | Covered by | Gaps |
|---|---|---|
| `generate-video.js` (prompt build, guardrails, model rotation/config, tokens, Turnstile) | `test/generate-video-build-prompt.test.js`, `test/generate-video-model-rotation.test.js`, `test/generate-video-model-config.test.js`, `test/generate-video-tokens.test.js`, `test/generate-video-turnstile.test.js`, `test/generate-video-audio-toggle.test.js`, `test/generate-video-image-to-video.test.js` | — |
| **Style-integrity guardrail** — `buildPrompt`'s new `styleIntegrityClause(style)`, appended as the prompt's second clause (right after the caption, before characters/camera/scenery/the style modifier itself) — tracker item `for-product-founder-ask-08-04-style-must-ezz8uf`, founder repro: caption "I am running back and forth to bring some important things for my family" + style Anime produced a video where the dreamer was rendered AS a flying dragon, because nothing in the prompt told the model the style choice governs rendering only, not the subject's species/identity/actions. Fix asserts "the dreamer remains a real human being... unless the dream itself explicitly describes a transformation" — style-agnostic (applies to every style, not just Anime/Cartoon) and deliberately doesn't override genuine transformation content the dream text itself describes. Shared by both generation entry points, since `start-pending-generation.js`'s pre-signup path calls this same exported `buildPrompt`, not a separate copy | `test/generate-video-build-prompt.test.js` (exact founder-repro caption + Anime produces the guardrail clause and preserves clause ordering ahead of the style modifier; guardrail present for every style incl. an unrecognized one; guardrail coexists correctly with the pre-existing self-photo reference-image pointer line/description) | **Real generation+eyeball verification NOT done** — no `FAL_KEY` in the build sandbox; someone with fal.ai access must run one real generation with the founder's exact caption + Anime and confirm the dreamer stays human before calling this fully resolved (see tracker item for the "post before/after prompt text" ask, answered in that item's comments) |
| Audio/dialogue generation — `generate_audio` unconditionally `false` for EVERY video generation path (`generate-video.js`'s plain/reference-to-video/image-to-video/PixVerse-rotation calls, and `start-pending-generation.js`'s own independent pre-signup computation), overriding whatever `audioOn` the client sends — tracker item `for-product-turn-off-audio-dialogue-gene-ooeyoj`, founder directive 2026-08-02 (cost, and to stop Veo's native audio from lip-syncing invented dialogue the user never wrote). Supersedes the 2026-07-28→07-29 history of a client-honored toggle + a retired owner/IL force-off (`resolveGenerationProfile`'s `forceAudioOff` field removed as dead weight; `profile` label survives for cost-attribution logging only). **Still in effect, unconditionally, untouched by the music-bed row below** — the generated video FILE itself is, and will remain, permanently silent; the `audioOn`/`musicStyle` payload plumbing (`js/store.js`, `home.html`/`result.html`'s regenerate/turn-into-video paths) is left intact and inert, a trivial flip back on if ever reversed. style.html's own Audio & music toggle, previously shown disabled here (`.toggle-switch.disabled`), briefly became real (music-bed row below) and has since been removed entirely — see that row, not this one, for the current state | `test/generate-video-audio-toggle.test.js` (every call shape — plain, reference-to-video, image-to-video-adjacent, PixVerse rotation — always `generate_audio:false` regardless of `audioOn`/`musicStyle`/`OWNER_EMAIL`/geo/condensing; `resolveGenerationProfile` no longer returns a `forceAudioOff` key), `test/start-pending-generation-audio-force.test.js` (the same unconditional-off contract on this file's own independent `generateAudio` computation, incl. its self-photo reference-to-video branch), `test/audio-toggle-behavioral.test.js` (real Chromium: every generation still only ever sends the permanently-inert `audioOn:false`/`musicStyle:null`/no `musicBedOn` at all to `generate-video.js`; `regenerateDream`'s pre-existing `audioOn:true` client payload is confirmed to still be sent — deliberately inert plumbing, not a behavior change) | — |
| **Music-bed playback (CLIENT-SIDE ONLY, not server-side audio generation)** — tracker item `for-product-build-founder-approved-08-03-jlkjy9`. Shipped in two stages the same day: (1) style.html's Audio & music toggle went real, per-dream, default ON (Option B); (2) hours later, **founder simplification: "NO TOGGLE. Since the music beds are free, music is simply ALWAYS ON — every dream video plays with its style-matched bed, no user choice."** The toggle, its markup (`#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub`), and the `musicBedOn` field it wrote onto a draft/dream are now removed entirely — no disabled/placeholder UI left behind (matches the precedent this same tracker item already set when it removed the old `#music-style-row` chip picker outright rather than disabling it). **Current, permanent state:** a reusable, one-time-generated, style-matched AMBIENT MUSIC BED (`assets/music-beds/{cartoon,cinematic,anime,realistic}.wav`, served as plain static files — no build step, no new endpoint) plays for **every** dream that has a real video and a known style, full stop — `js/music-bed.js`'s `eligible()` no longer reads `musicBedOn` in any form, so a dream generated/published during either earlier stage (carrying a stale `musicBedOn:false`/`true`, or predating the feature entirely with no such field) is treated identically to a brand-new one — no lingering opt-out from old data. Never anything model-generated — the row above's `generate_audio:false` fix is completely untouched. Playback is a synced `<audio>` element (`js/music-bed.js`) that starts/stops with the video's own play/pause state and shares the video's own device-level mute preference (`DreamStore.getSoundPref`/`setSoundPref`) — wired into result.html's player and explore.html's feed (home.html's own ambient preview thumbnail and the admin-only media-library lightbox stay deliberately scoped OUT, see `js/music-bed.js`'s and this row's own detail below); **mute stays available in the player exactly as always.** **Implementation-choice tradeoff, stated explicitly (per the tracker item's own ask):** client-side playback, not muxed into the video file at generation — ships without a new ffmpeg/vendor dependency, but means a downloaded or externally-shared video file carries no audio at all; the bed only ever plays inside DreamTube's own player UI. Root-caused via systematic-debugging: a freshly created, `preload="none"` `<audio>` element's first `.play()` call, made synchronously inside an `IntersectionObserver` callback, intermittently (~1 run in 3 in headless Chromium) silently never started (no error/event at all) — fixed with a bounded, event-driven retry (the video's own recurring `timeupdate`, never a blind timeout) in both result.html and explore.html. **Known gap, scoped out deliberately:** `watch.html` (the retention-email "watch it" link) and `claim-dream.html` (the pre-signup claim moment) read a dream through server-side projection endpoints (`get-shared-dream.js`, the claim flow's own endpoint) that don't carry `style` at all — extending those would mean threading a new field through `dream-sync.js`'s private-dream schema and those endpoints' own response projections, a materially larger, separate change from the client-only surfaces this pass covers; both stay silent (unchanged from before this feature) until a follow-up does that work. **Update 2026-08-04:** the four style beds described here are now selection TIER 2 — the permanent fallback reached via `js/music-bed.js`'s new `urlForDream` when a dream has no usable mood. Nothing in this row's behavior changed (see the mood-keyed row directly below, whose zero-regression coverage proves exactly that); `urlForStyle` is unchanged and still directly tested. | `test/music-bed-behavioral.test.js` (all 4 bed assets resolve as real static files at their production path; `js/music-bed.js`'s `urlForStyle`/`eligible` contract — a real videoUrl + known style is the ONLY requirement now, confirmed identical across a stale `musicBedOn:true`, a stale `musicBedOn:false`, and the field missing entirely; an unknown style or missing video still fails closed to no bed; result.html and explore.html both confirmed, via real Chromium, to play/pause/mute a bed in sync with the video for every eligible dream regardless of any historical `musicBedOn` value), `test/audio-toggle-behavioral.test.js` (confirms the toggle's markup — `#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub` — is fully absent from the DOM on both media types, not just hidden; `musicBedOn` confirmed never sent to `generate-video.js` and never present on a finished/regenerated dream record) | `watch.html`/`claim-dream.html` playback (see detail column — real, scoped-out gap, not silently missed); home.html's own ambient preview thumbnail and the admin-only media-library lightbox are deliberate silent-by-design exclusions, not gaps (see `js/music-bed.js`/explore.html's own comments for why) |
| **Mood-keyed music beds (selection tier 1) + `dream.mood` persistence + the audition page** — tracker item `for-product-founder-08-04-evening-music--jfjco0`, FINAL-FINAL scope (founder insight 2026-08-04: a dream's VISUAL style is a weak signal for what it should sound like; the dream-builder wizard's MOOD step is the right one). Three parts. **(1) The persistence bug, fixed.** The wizard's Mood step (step 4 of 5 — peaceful/joyful/dreamy/mysterious/tense/epic, `js/wizard-chips.js`'s `MOOD_CHIPS`) reached `assembleCaption`/`buildDeterministicStory` — i.e. the generated prompt/story TEXT — and was then dropped on the floor at `create.html`'s `finishBuildWizard` `setDraft` and `wizard.html`'s `draftPatch`, so it never survived onto the dream record and nothing downstream could key off it. Now threaded end to end: wizard → `state.draft.mood` → `home.html`'s `startFreshGenerationFromDraft` → `startGeneration` → `savePendingJob` → `finalizeDream` → `dream.mood`, plus the pre-signup route that never touches `generateVideo` at all (`adoptPendingGeneration(..., mood)` → `resumePendingJob`'s re-stamp), plus into the shared feed record (`publish-dream.js`, validated against a fixed `KNOWN_MOODS` list — this value lands in a world-readable blob and is read back into an asset path, so an unbounded string is rejected). A regenerate/edit PRESERVES the dream's existing mood (those flows have no mood picker; dropping it mid-edit would be an unrequested behavior change). **SKIP vs. Continue is a real distinction** — both wizards pre-highlight the default chip, so `moodKey` alone can't tell them apart; a new `moodSkipped`/`buildMoodSkipped` flag feeds the shared `WizardChips.persistedMood(moodKey, skipped)`, which returns null for skipped / '+ Something else' free text / anything unrecognized. **Prompt and story assembly are deliberately untouched** — a skip still contributes the default mood language it always did, so what actually gets generated is byte-identical to before this field existed. **(2) The selection tier** (`js/music-bed.js`'s new `urlForMood`/`urlForDream`, now called by result.html and explore.html in place of `urlForStyle`): the dream's own mood first, the four visual-style beds as the **permanent** fallback (not a migration step — they are what a skipped/free-text/legacy mood resolves to, forever). **(3) `bed-audition-x7q4.html`** — owner-page convention (unguessable name + `noindex,nofollow`, no password since it calls no backend), one row per mood, a reference player for the four current beds, two candidate slots, one-winner-per-mood picking persisted across reloads, and a copyable picks summary. **TIER 1 IS LIVE (2026-08-07, same-day full cycle):** the 12 candidate tracks (2 per mood, fal-ai/stable-audio 45s seamless loops — generated at 47s, last 2s crossfaded into the first 2s — 44.1kHz/16-bit stereo, gain-matched to the four style beds) were generated and committed, the founder auditioned all 12 on `bed-audition-x7q4.html` and picked one winner per mood (**peaceful-A, joyful-B, dreamy-A, mysterious-B, tense-B, epic-B**), each winner was promoted in place to `assets/music-beds/moods/<mood>.wav`, `MOOD_FILES` was filled with all six, and — per the tracker item's own no-dead-assets instruction — the six losing candidates AND the audition page itself were deleted. A dream with a real chosen mood now hears its mood bed on every playback surface; a skipped/free-text/legacy mood still falls back to its visual-style bed byte-identically to before (tier 2, permanent). The four style beds are untouched at `assets/music-beds/<style>.wav` | `test/mood-music-bed-behavioral.test.js` (the zero-regression proof across all 30 mood × style combinations with `MOOD_FILES` empty, plus a guard that it IS still empty so committing a real track forces this row to be revisited; tier 1 winning over the visual style once a track exists, proven by populating `MOOD_FILES` at runtime as a stand-in — a partially-delivered 2-of-6 state, not just all-or-nothing; skipped/free-text/unknown/absent mood and the both-tiers-fail case all falling back or failing closed; the three copies of the mood key list — `MOOD_CHIPS`, `music-bed.js`'s `MOOD_KEYS`, `publish-dream.js`'s `KNOWN_MOODS` — asserted identical since they can't import one another; real Chromium walks of create.html's "Build it" Mood step for chip/Skip/free-text/back-then-answer, asserting both the persisted `draft.mood` AND that the generated prompt text is unchanged; mood surviving a full mocked-transport generation onto `dream.mood` and then driving result.html's actual `#result-music-bed` src; the no-mood path producing exactly today's record and today's bed; a RESUMED adopted pending job keeping its mood — the pre-signup funnel's only route; regenerate preserving mood; explore.html feed cards keyed off a record's mood with legacy no-mood records unaffected; and the six LIVE mood beds: tier 1 resolving every real mood × every style to that mood's own committed winner bed, the no-mood/free-text/legacy path still resolving byte-identically to `urlForStyle` (the zero-regression guarantee, scoped to the dreams it still applies to), a future-mood-key-without-a-track guard falling back rather than going silent, and an assets-integrity test — all six winner WAVs valid RIFF/WAVE and full-length on disk, served 200 at their production URLs, one genuinely decoding and playing in real Chromium, plus explicit assertions that the candidates directory and the audition page are GONE. The audition-page picking-flow tests were removed with the page itself when the audition concluded — the flow they covered no longer exists), **the cross-device sync path, which takes BOTH halves and originally had only one** — `test/dream-sync.test.js` covers the SERVER half only (`mood` surviving the private-dream upsert/GET round trip once it reaches the handler — it had to be added to `dream-sync.js`'s `DREAM_FIELDS` allowlist; plus the no-mood dream syncing exactly as before), but it calls the handler directly with a payload hand-built by its own `dreamPayload()` helper, so it proves nothing about whether the real client ever puts `mood` on the wire. **It didn't** — review on this branch caught that `js/store.js`'s `attemptPrivateDreamSync` builds its upsert body from an explicit hand-maintained field list that omitted `mood`, so the value was correct in local state, correct in the server allowlist, and silently dropped in transit; a dream restored on a new device would still have come back moodless. Fixed, and the wire contract itself is now covered in `test/mood-music-bed-behavioral.test.js` (a real generation's mood asserted present in the actual outgoing dream-sync POST body, and the no-mood path asserted to send an explicit `mood: null` rather than an absent key) — both verified to fail against the unfixed client and pass with it, `test/wizard-chips.test.js` (`persistedMood` unit coverage: all six real keys, the Skip-vs-Continue-on-the-default ambiguity, free text, fail-closed on unknown, and an explicit assertion that it changes neither `assembleCaption` nor `buildDeterministicStory`) | **Real-mode/mobile verification of the live mood beds on the deployed site is still pending** (`AGENT_POLICY.md`'s REAL-MODE gate) — everything above is verified against a local serve in real Chromium at mobile viewport; one post-deploy listen on a real phone (autoplay gesture path included) should confirm before this is called fully done. No `dream.mood` backfill for dreams created before this shipped (forward-only, same as `createdAt`/`storyText`/`modelUsed`) — they fall back to their style bed, which is what they already had. `claim-dream.html`'s `saveClaimedDream` path carries no mood (the abandoned-dream re-engagement link resolves server-side through `verify-pending-claim.js`, which has no mood field) — falls back to the style bed, same as `watch.html`/`claim-dream.html`'s pre-existing scoped-out gap in the row above. Free-text moods are NOT keyword-mapped (explicit scope cut in the tracker item); the typed text still reaches `promptText`, so a later pass has something to work from |
| **One-time sound-discoverability nudge** — tracker item `for-product-p1-founder-repro-08-04-12-40-6icx89`. Root cause: `DreamStore.getSoundPref()` (`js/store.js`) defaults every video/bed to MUTED until a user has explicitly tapped a mute/unmute control — harmless for this app's entire history because every video was permanently silent (row above), so nobody ever had a reason to tap unmute. The music-bed row above shipped the first real audio this app has ever had, inheriting that same mute-by-default with zero affordance telling anyone sound now exists — the founder's own real-device repro (a brand-new dream, total silence) confirmed this affects virtually the entire installed base, not an iOS-specific autoplay-policy issue. Fix (deliberately NOT flipping the default to unmuted — real browser autoplay policy would just fight an unmuted autoplay attempt with no gesture, trading one silent-failure mode for a dishonest one): a toast (`🎵 Tap for sound`) + a bounded pulse (`.sound-nudge-pulse`, `css/styles.css`, reusing the existing `pulse` keyframes already used by `.rec-dot` rather than inventing new motion) on the EXISTING mute icon — no new modal/banner, since the mute button itself already works and already shows the honest current state. Fires exactly ONCE per device (not per page/per video), gated on two new device-level `js/store.js` flags: `hasEverSetSoundPref()` (has `dreamtube_sound_on` ever been explicitly written — distinct from `getSoundPref()`'s own boolean, which can't tell "explicitly muted" apart from "never touched") and `hasSeenSoundNudge()`/`markSoundNudgeSeen()` (`dreamtube_sound_nudge_seen`, marked the instant the nudge is shown, not on any later tap). Wired into the only two surfaces that actually render a playable bed-eligible video: result.html's `render()` (fires whenever a video dream with a bed is shown) and explore.html's `videoObserver` callback (fires the moment a bed-eligible card actually becomes the playing one). home.html/start.html/style.html were checked (`MusicBed`/music-bed grep) and confirmed to only reference the feature in comments — no playable bed-eligible video renders there, so no nudge wiring needed | `test/sound-nudge-behavioral.test.js` (fires exactly once on a fresh device for a bed-eligible video on both result.html and explore.html; does not re-fire on a second visit/video, including across pages, once already seen on this device; does not fire once sound has been explicitly touched via a real mute-button tap or a pre-existing `dreamtube_sound_on` value even without a matching tap; never fires for a dream with no eligible bed, and confirms the "seen" flag stays false in that case so a later eligible dream can still nudge) | — |
| **Music-bed playback (CLIENT-SIDE ONLY, not server-side audio generation)** — tracker item `for-product-build-founder-approved-08-03-jlkjy9`. Shipped in two stages the same day: (1) style.html's Audio & music toggle went real, per-dream, default ON (Option B); (2) hours later, **founder simplification: "NO TOGGLE. Since the music beds are free, music is simply ALWAYS ON — every dream video plays with its style-matched bed, no user choice."** The toggle, its markup (`#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub`), and the `musicBedOn` field it wrote onto a draft/dream are now removed entirely — no disabled/placeholder UI left behind (matches the precedent this same tracker item already set when it removed the old `#music-style-row` chip picker outright rather than disabling it). **Current, permanent state:** a reusable, one-time-generated, style-matched AMBIENT MUSIC BED (`assets/music-beds/{cartoon,cinematic,anime,realistic}.wav`, served as plain static files — no build step, no new endpoint) plays for **every** dream that has a real video and a known style, full stop — `js/music-bed.js`'s `eligible()` no longer reads `musicBedOn` in any form, so a dream generated/published during either earlier stage (carrying a stale `musicBedOn:false`/`true`, or predating the feature entirely with no such field) is treated identically to a brand-new one — no lingering opt-out from old data. Never anything model-generated — the row above's `generate_audio:false` fix is completely untouched. Playback is a synced `<audio>` element (`js/music-bed.js`) that starts/stops with the video's own play/pause state and shares the video's own device-level mute preference (`DreamStore.getSoundPref`/`setSoundPref`) — wired into result.html's player and explore.html's feed (home.html's own ambient preview thumbnail and the admin-only media-library lightbox stay deliberately scoped OUT, see `js/music-bed.js`'s and this row's own detail below); **mute stays available in the player exactly as always.** **Implementation-choice tradeoff, stated explicitly (per the tracker item's own ask):** client-side playback, not muxed into the video file at generation — ships without a new ffmpeg/vendor dependency, but means a downloaded or externally-shared video file carries no audio at all; the bed only ever plays inside DreamTube's own player UI. Root-caused via systematic-debugging: a freshly created, `preload="none"` `<audio>` element's first `.play()` call, made synchronously inside an `IntersectionObserver` callback, intermittently (~1 run in 3 in headless Chromium) silently never started (no error/event at all) — fixed with a bounded, event-driven retry (the video's own recurring `timeupdate`, never a blind timeout) in both result.html and explore.html. **Known gap, scoped out deliberately:** `watch.html` (the retention-email "watch it" link) and `claim-dream.html` (the pre-signup claim moment) read a dream through server-side projection endpoints (`get-shared-dream.js`, the claim flow's own endpoint) that don't carry `style` at all — extending those would mean threading a new field through `dream-sync.js`'s private-dream schema and those endpoints' own response projections, a materially larger, separate change from the client-only surfaces this pass covers; both stay silent (unchanged from before this feature) until a follow-up does that work. | `test/music-bed-behavioral.test.js` (all 4 bed assets resolve as real static files at their production path; `js/music-bed.js`'s `urlForStyle`/`eligible` contract — a real videoUrl + known style is the ONLY requirement now, confirmed identical across a stale `musicBedOn:true`, a stale `musicBedOn:false`, and the field missing entirely; an unknown style or missing video still fails closed to no bed; result.html and explore.html both confirmed, via real Chromium, to play/pause/mute a bed in sync with the video for every eligible dream regardless of any historical `musicBedOn` value), `test/audio-toggle-behavioral.test.js` (confirms the toggle's markup — `#audio-toggle-row`/`#audio-toggle`/`#audio-toggle-sub` — is fully absent from the DOM on both media types, not just hidden; `musicBedOn` confirmed never sent to `generate-video.js` and never present on a finished/regenerated dream record) | `watch.html`/`claim-dream.html` playback (see detail column — real, scoped-out gap, not silently missed); home.html's own ambient preview thumbnail and the admin-only media-library lightbox are deliberate silent-by-design exclusions, not gaps (see `js/music-bed.js`/explore.html's own comments for why) |
| `GENERATION_MOCK_MODE` / `GENERATION_TEST_DURATION` themselves | `test/generate-video-mock.test.js`, `test/video-status-mock.test.js` | — |
| `generate-image.js` / `image-status.js` | `test/generate-image.test.js`, `test/image-status.test.js`, `test/image-status-refund.test.js`, `test/image-generation-style-toggle-behavioral.test.js`, `test/image-generation-turn-into-video-behavioral.test.js`, `test/image-status-rehost.test.js` | — |
| `video-status.js` (polling, refunds, model-agnostic) | `test/video-status-mock.test.js`, `test/video-status-model-agnostic.test.js`, `test/video-status-refund.test.js`, `test/video-status-rehost.test.js` | — |
| **Error-status normalization for a fal-side status-check transport hiccup** (tracker item `for-product-netlify-cost-deploys-are-97--9vbpd4`, investigating an observed 17.6% request error rate): `video-status.js`'s E203/E204 and `image-status.js`'s E503/E504 (a non-JSON or non-OK response from fal's own STATUS-check endpoint — polled every 10s for every in-flight generation) used to propagate fal's raw upstream HTTP status as this endpoint's own response status; now normalized to 200 with the error in the JSON body, matching the pattern this same file already used one step later for E206/E207 (`video-status.js`) and E506/E507 (`image-status.js`) — a fal-side transport hiccup on our own upstream call was never a failure of this endpoint, and `js/store.js`'s `pollUntilDone` only ever reads the parsed body's `error` field, never `res.status`, so this is a pure metrics fix with zero client-visible behavior change | `test/video-status-refund.test.js` (new E203/E204 statusCode:200 assertions), `test/image-status.test.js` (new E503/E504 statusCode:200 assertions) | Root cause of the observed 17.6% figure is NOT confirmed — this sandbox has no access to real Netlify Observability/production traffic logs, so this fix addresses a real, code-confirmed inconsistency (verified against this same file's own already-normalized E206/E207/E506/E507 pattern) and a plausible contributor given call frequency, not a proven fix for that specific number. |
| Own-storage media re-host on completion (`lib/media-rehost.js`, `lib/media-status.js`, `video-file.mjs`, `image-file.mjs`), including the streaming-function `MAX_STREAMABLE_BYTES` size gate + oversized-redirect defense-in-depth, and the `force`-bypass option repair tooling uses — tracker items `for-product-owner-media-library-page-fou-1fwxaw`, `for-product-urgent-founder-repro-on-drea-uq3a36` (SIZE fix), `for-product-urgent-reopen-video-repair-p-cyp8np` (`force` option) | `test/media-rehost.test.js` (including the SIZE-gate and `force`-option cases), `test/media-status.test.js`, `test/media-file-functions.test.js` (including each file's own oversized-redirect defense-in-depth check), `test/video-status-rehost.test.js`, `test/image-status-rehost.test.js`, `test/dream-webhook.test.js` (the funnel webhook's own separate re-host path), `test/prod-smoke/media-serving.test.js` (real, currently-deployed `video-file.mjs`/`image-file.mjs` actually serving a real stored file — 200/302 + correct content-type — against a known-good production fixture key; the module-load-crash class of regression `test/media-file-functions.test.js`'s mocked suite structurally cannot see, per that file's own header comment — tracker item `for-product-add-a-prod-smoke-assertion-f-bqt2sh`) | Best-effort by design (see `lib/media-rehost.js`'s header comment) — a re-host failure is expected to leave the fal URL in place, covered explicitly, not a gap. No live `FAL_KEY`/real fal media in this sandbox to confirm actual byte-for-byte fidelity against a real fal response — mocked fetch responses only, same limitation this codebase's other fal-adjacent tests already accept. The new prod-smoke image-serving check is opt-in via `MEDIA_SMOKE_IMAGE_KEY` and currently skips — no verified real image key was available to hardcode; see that test file's own header comment. |
| Legacy broken-metadata repair sweep for own-storage media re-hosted BEFORE the SIZE fix shipped (`admin-repair-oversized-media.js`) — tracker item `for-product-urgent-reopen-video-repair-p-cyp8np` | `test/admin-repair-oversized-media.test.js` (auth, pagination across accounts→feed same as the backfill sweep, the cheap metadata-only-backfill tier vs. the real fal-refetch tier, `fal_result_gone`/`missing_or_invalid_source_operation_name`/`missing_fal_key`/`blobMissing` failure paths all reported not crashed, `dryRun` performing zero writes/fal calls, `targetDreamIds`/`targetKeys` filtering) | Never executed against real production data or a live `FAL_KEY` from this sandbox (no live `FAL_KEY`, no live Netlify deploy target, no real owner password available here) — every fal call and Blobs read/write is mocked. A human with live access (Manager or Ron) must run it against production and verify the specific records per the founder's own stated DONE bar before this tracker item can close; see that item's own notes for the exact request shape. |
| Edit-delta mechanism (`realign-dream-prompt.js`, the edit sheet) | `test/edit-mechanism-behavioral.test.js`, `test/realign-dream-prompt.test.js` | — |
| Edit/regen forming-state + stale-completion race guard | `test/edit-regeneration-forming-state-behavioral.test.js` | — |
| Edit/regen forming veil + reload persistence, live on a real deployed origin (the i2yzqo regression class) | `test/prod-smoke/session.test.js` (test 5) | — |
| Avatar generation (`generate-avatar.js`) | `test/generate-avatar.test.js`, `test/avatar-describe-behavioral.test.js` | — |
| Record-it voice capture + transcription (`transcribe-audio.js`, `transcribe-status.js`, real `MediaRecorder`/`getUserMedia`) | — | **Real gap.** `test/transcript-text-review-behavioral.test.js` covers reviewing/editing ALREADY-transcribed text, not the actual recording/transcription call chain. No test exercises `transcribe-audio.js`/`transcribe-status.js` directly. |
| Model cost categorization/reconciliation (`fal-cost-categorize.js`, `reconcile-fal-cost.js`) | `test/fal-cost-categorize.test.js`, `test/reconcile-fal-cost.test.js` | — |

## Dreams: storage, publish/unpublish, feed, social

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Local dream store / edit history | `test/dream-store.test.js` | — |
| Server-side private dream persistence (`dream-sync.js`) | `test/dream-sync.test.js`, `test/dream-sync-client-behavioral.test.js` | — |
| Post-generation sync confirmation + retry (`js/store.js#syncPrivateDreamBestEffort`/`retryUnconfirmedPrivateDreamSyncs`) — tracker item `for-product-p0-data-loss-founder-repro-0-6bzvv1`: a completed, token-charged generation could silently never reach the server-side private-dream store at all (present locally at best, absent on any other device/browser), with no retry and no way to tell later whether the write had landed. Fixed with a local `syncConfirmed` flag + retry-with-backoff (genuine transient/5xx failures) + a load-time sweep that retries any not-yet-confirmed dream the next time ANY page loads with a real `authToken` on file — no fresh login required, unlike `reconcilePrivateDreamsFromServer`'s own login-only catch-up push | `test/private-dream-sync-navigation-race.test.js` (a real transient server-side 5xx retried to success; a dream generated while `state.user.authToken` is null — a real, documented `attemptLocalLogin` fallback shape, not a contrived state — never even attempts a sync, then reaches the server automatically on the next ordinary page load once a real token is on file, with no explicit login/logout) | The write-side mirror of `nsbbg5`'s navigation-abort race (an in-flight `dream-sync` POST outrun by same-tab SPA navigation or the tab closing) was investigated directly but could NOT be reliably reproduced as a genuine server-side loss in this harness — Netlify Functions generally complete server-side once a request is dispatched, regardless of client fate — so it is not confirmed as a live mechanism, only defended against as a matter of course by the same retry/confirmation fix. See the build session's own report for the full investigation. |
| Admin diagnose/repair for a generation that was charged but never got a surviving dream record (`admin-diagnose-lost-generations.js`, `admin-repair-lost-generations.js`) — tracker item `for-product-p0-data-loss-founder-repro-0-6bzvv1`, cross-references `lib/job-owners.js` (who submitted a job) against `lib/dream-store.js` (who has a finished dream record) by operationName, per-account or swept across all accounts since a timestamp; repair re-queries fal's own result endpoint and writes a real recovered dream record (placeholder caption unless a human supplies `captionOverride`/`styleOverride` — job-owners.js never records the original caption/style) | `test/admin-diagnose-lost-generations.test.js`, `test/admin-repair-lost-generations.test.js` | Cannot be run against real production from this sandbox (no live `FAL_KEY`/owner password) — built and tested against mocked fixtures only; whoever has real production access must actually run it. `lib/job-owners.js` has no secondary index by account or date, so both tools page over its full key list — fine at this app's current scale, would need real pagination discipline (already built in, via `cursor`/`nextCursor`) at a much larger scale. |
| `audit-x7q4.html` — owner-only one-tap browser runner for `admin-diagnose-lost-generations.js`'s `mode:'sweep'` (built because the endpoint needs the founder's REAL password, which no agent sandbox holds — see that file's own header comment). **BUG FIX (08-03, same tracker item, follow-up sweep result 37/20/5):** the page used to dump the sweep's full-window `details` array (every job-owner record in the window, both `hasDream:true` and `hasDream:false`) into a single pane labeled just "Details," directly under the "Lost jobs" stat — reading exactly like a list of lost jobs, which is what produced this tracker item's own reported anomaly (a `hasDream:true` row appearing to be "in the lost list"). The backend's own `lostJobs`/`accountsAffected` counters were independently re-verified correct (never include a `hasDream:true` row — see the passing `admin-diagnose-lost-generations.test.js` suite); this was a front-end display bug only. Now splits the dump into three explicitly labeled sections — lost (needs review, open by default), unresolved (job owner's account not found), and found (has a dream, explicitly marked "NOT part of the lost count") — plus a lost-jobs mediaType (video/image) breakdown, since `generate-image.js`'s completion path (`js/store.js`'s `finalizeDream`/`syncPrivateDreamBestEffort`) is identical in shape to the video path, not a thumbnail/preview mechanism carved out by design — a lost image job is genuine data loss too, just a cheaper one (10 tokens vs 100, see `generate-image.js`'s `IMAGE_TOKEN_COST`) | `test/audit-lost-generations-page.test.js` (real Chromium run: a mocked sweep response with a deliberate mix of lost/found/unresolved rows across both mediaTypes proves a `hasDream:true` row is never shown in the lost pane and lands in the found pane instead, the lost-count/mediaType-breakdown stats match, an all-clear sweep renders "(none)" rather than an empty string, and a wrong password shows an inline error without ever revealing the results panel) | Still cannot be run against real production from this sandbox (no owner password) — the actual 37/20/5 sweep result and its per-job disposition (healed-by-the-day's-earlier-retry-fix vs genuine-loss vs unresolved) needs someone with the real founder password to re-run `audit-x7q4.html` itself; this fix only corrects how that person reads the result once they do. |
| Publish/unpublish (`publish-dream.js`, `unpublish-dream.js`) | `test/publish-dream.test.js`, `test/publish-unpublish-dream-auth.test.js`, `test/publish-dream-avatar-thumbnail-behavioral.test.js`, `test/publish-dream-avatar-validation.test.js` | — |
| Publish/unpublish, live, real feed, real probe dream | `test/prod-smoke/session.test.js` (test 6) | — |
| Shared feed (`get-feed.js`), windowing | `test/feed-windowing-behavioral.test.js` | No dedicated `get-feed.test.js` at the plain-handler level — coverage is behavioral/client-side only |
| Explore feed rendering, avatar fallback | `test/explore-avatar-fallback-behavioral.test.js` | — |
| Likes (`like-dream.js`) | `test/like-dream.test.js`, `test/notify-likes-badge-behavioral.test.js` | — |
| Block / report (`block-user.js`, `report-dream.js`) | `test/block-user.test.js`, `test/report-dream.test.js`, `test/report-block-behavioral.test.js` | — (previously: admin-side moderation queue had no consumer/coverage at all — see the moderation review row below, which closed that gap) |
| Moderation review page (`moderation-x7q4.html`, tracker item `for-product-build-the-moderation-review--sbf5ol`) — the first (and, until this, only) consumer of `get-moderation-reports.js`, which previously had zero callers anywhere in the app despite `report-dream.js` filing real reports into it. Owner-email-gated (same client-cached-email + server 403-on-mismatch pattern as `admin.html`'s paywall toggle, not `media-library-x7q4.html`'s heavier real-password gate — matches `get-moderation-reports.js`'s own existing gate shape, no new auth mechanism introduced), unpaginated full-list newest-first (matches that endpoint's own no-pagination design, "volume is ~zero" per the tracker item). New additive `handled`/`dismissedAt` fields on the stored report record (`lib/moderation-store.js`'s `markHandled` — a one-way signal, no un-handle control, modeled directly on `tracker-store.js`'s `reviewedAt`/`startedAt` precedent) and a new small owner-gated write endpoint `mark-report-handled.js` (same error-code-scheme/owner-check shape as `admin-paywall-toggle.js`/`update-tracker-item.js`). Linked from `admin.html` alongside the media library/post pack links. | `test/mark-report-handled.test.js` (owner-gate + 403/404/400 rejections, real persistence across a second `getReports` read, the one-way-signal idempotency — marking an already-handled report handled again never overwrites the original `dismissedAt` — and that `markHandled` never touches an unrelated report), `test/moderation-review-page.test.js` (Playwright: login-redirect, non-owner 403 shows the unauthorized state not the list, newest-first ordering, dream-report fields (dream id/caption/reporter handle/reason) vs comment-report fields (comment text/author, null reporter renders "anonymous") both rendering correctly from the same list, the Handled tag/no-button visual state vs. the unhandled Mark-handled-button state, a genuine persistence test — click Mark handled, reload the page entirely, confirm a fresh GET already shows it handled — and a failed-mark-handled negative-proof reverting the button with no false "handled" state) | Same accepted "fine at current scale" unpaginated-full-scan tradeoff as `admin-media-library-data.js`/`get-moderation-reports.js` itself — revisit only if report volume ever meaningfully grows past today's near-zero baseline. No automated action on a report (no auto-hide/auto-suspend) — marking handled is purely a founder bookkeeping signal, unchanged from `report-dream.js`'s own original "no automated content filtering ML" scope decision. No `test/prod-smoke/` coverage — same posture as every other row in this section without one. |
| Sharing (`share-dream.js`, `get-shared-dream.js`) | `test/share-dream.test.js`, `test/share-sheet-behavioral.test.js` | — |
| Channel republish license consent | `test/channel-republish-license-behavioral.test.js` | — |
| **Social Layer v2 slice 1 — "profile shell + share"** (`docs/SOCIAL_LAYER_V2_DESIGN.md`, tracker item `for-product-build-social-layer-v2-direct-34047c`, Direction B "Dream Journal Feed"): new public-profile page `u.html` (no login gate, like `explore.html`) — identity block, bio, PUBLIC/PRIVATE segmented toggle (owner-only, pure navigation between this page and `profile.html`), Share-this-profile button, full-width 9:14 dream cards with windowed/virtualized muted-autoplay video (mirrors `explore.html`'s own batching+hydrate/dehydrate mechanism, scoped down for this page's normal-scroll — not scroll-snap — layout), heart/comment-count(real as of slice 2, see the row below)/share action row, reserved-but-inert interpretation seam, and the "interpretations stay private" note. Backing endpoints: `sync-profile.js` (token-gated write, called on `profile.html`'s Edit-profile save AND on first publish — see `js/store.js`'s `syncProfileToServer`), `get-profile.js` (public GET, feed filtered by `ownerHandle` + `publishedCount` + `commentCounts`, filled in as of slice 2 — see the row below), `share-profile.js` (og:-preview endpoint mirroring `share-dream.js`, bare-avatar `og:image` per this slice's own scope — a branded/hosted image is an explicit later fast-follow, see that file's own "KNOWN LIMITATION" comment). New durable store `dreamtube-profiles` (`lib/profile-store.js`, one record per lowercased username, via `lib/blobs-retry.js`'s bounded retry). `profile.html` gained the bio field (Edit-profile sheet, IG-parity 150-char cap, local cache in `state.profileBioByUser`) and the PUBLIC/PRIVATE toggle (private view unchanged; PUBLIC navigates to `u.html?handle=<me>`) plus a server-truth "N published" count fetched alongside the pre-existing device-local "M created" count. `js/share-sheet.js` gained an optional `shareUrl`/`hideSave` override on `show()` so the profile-share button can reuse its existing navigator.share → clipboard → unsupported-toast ladder against `share-profile.js` instead of `share-dream.js`. `mock-profile-a-x7q4.html`/`mock-profile-b-x7q4.html` (the throwaway Direction A/B visual mocks) deleted — no longer needed once the real page exists. | `test/profile-store.test.js` (lib-level: round-trip, full-overwrite-not-patch semantics, "@"/case normalization, read-failure degrade), `test/sync-profile.test.js` (method/JSON/field validation incl. the 150-char bio boundary and avatar size/type validation, auth-token verification, the E6 owner-mismatch security case — a verified token for one username cannot write a different handle's profile — and that a rejected request never writes anything), `test/get-profile.test.js` (missing-handle 400, the three `exists` shapes — neither profile nor dreams / profile-only / dreams-only, i.e. published-without-ever-syncing-a-profile — newest-first dream filtering, case/"@"-insensitive handle matching, confirms no viewer-relative `likedByMe`/`mine`/`blockedByMe` leak into this bare public GET, CORS), `test/share-profile.test.js` (mirrors `test/share-dream.test.js`'s conventions: missing/nonexistent-handle redirects to `u.html`, real og:title/description/image incl. singular "1 dream" phrasing, avatar-vs-branded-fallback image selection, a malicious `displayName` never leaking unescaped into the response, host-header handling), `test/social-layer-v2-profile-behavioral.test.js` (real Chromium: `u.html`'s not-found/loaded/zero-dreams/fetch-error+retry states, a `<script>`-laden caption rendering as inert literal text never executing (XSS check), optimistic like with server reconciliation AND a rapid-second-tap-while-pending per-instance guard, a signed-out visitor's like tap routing to the signup nudge with no network call, owner-preview toggle visibility + both counts + PRIVATE navigating back to `profile.html`, `profile.html`'s bio field/counter/save round-trip and its own PUBLIC toggle navigating to `u.html?handle=<me>`, and a 44px-minimum tap-target check on every new interactive element this slice introduces) | Slice 3 (follow — `dreamtube-follows` store, Explore "Following" chip) is NOT built here, per the design doc's own three-slice build order. The branded share-preview `og:image` (design doc's open decision #2) is an explicit, non-blocking fast-follow, not built here — see `share-profile.js`'s own header comment for the real-world data-URI-vs-hosted-URL caveat this leaves in place until that follow-up. No `test/prod-smoke/` coverage — this is ordinary new-feature-behind-no-flag coverage, same posture as every other row in this section that has none. |
| **Social Layer v2 slice 2 — "comments"** (`docs/SOCIAL_LAYER_V2_DESIGN.md`, tracker item `for-product-build-social-layer-v2-direct-34047c`, design doc's own state E): flat (no threading), public, first-ever hostile-UGC text surface in this app. New durable store `dreamtube-comments` (`lib/comment-store.js`, one record per dreamId -> comment array, `lib/blobs-retry.js`'s bounded retry, same append-only idempotency shape as `lib/moderation-store.js`). Endpoints: `get-comments.js` (public GET, newest-first), `add-comment.js` (token-gated POST, trims + rejects over 300 chars server-side independent of the client's own live counter, per-account daily rate limit via `lib/rate-limit.js`), `delete-comment.js` (token-gated POST, commenter-or-dream-owner permission model). `commentCount` denormalized onto the shared feed record (`lib/feed-comment-count.js`, mirrors `like-dream.js`'s own STALE-READ-CLOBBER-FIX idempotent-opId-marker shape almost verbatim, same accepted RMW race, same fail-open-on-exhaustion posture; `_lastCommentCountOp` joins the ALREADY-ACCEPTED, tracked leak of internal dedup markers into the public feed/profile response — tracker item `minor-like-dream-js-internal-dedup-marke-8qztvq` — not a new issue). `report-dream.js`/`lib/moderation-store.js` additively extended with a `targetType:'dream'\|'comment'` field (defaults to `'dream'`, every pre-existing caller unaffected) so a comment can be reported with its own client-supplied snapshot (`commentId`/`commentText`/`commentAuthorHandle`), same trust level as the existing dream-report snapshot fields. New shared `js/comment-sheet.js` bottom sheet (reuses `.sheet-overlay`/`.sheet`/`js/sheet-dismiss.js` — deliberately a SINGLE scroll region via a `position:sticky` composer rather than a second nested scrollable list, so `js/sheet-dismiss.js`'s existing drag-vs-scroll `scrollTop` check needs no changes — see that file's own SCROLL SHAPE header comment), mounted in TWO places: `u.html` (each Dream Journal card's real comment-count action + top-2 preview lines + "View all N"/"Add a thought…" teaser, `loadCommentPreviewFor`) and `explore.html`'s feed-action rail (design doc's own open decision #3 — the spec's recommended default was followed, but this is flagged in the PR itself as still open for founder confirmation, not silently treated as settled). Per-comment overflow menu: Delete (own comment, or any comment if you're the dream's owner), Report, Block — all `esc()`'d through a local copy (not a `js/dream-cards.js` dependency, since `explore.html` doesn't load that file), `dir=auto` on comment text/composer, per-instance `currentGen` token guard on the optimistic post/delete flows (this codebase's documented recurring stale-async-callback bug class). | `test/comments.test.js` (`lib/comment-store.js`/`lib/feed-comment-count.js` unit coverage incl. append/delete idempotency and the floor-at-0/not-found-returns-null cases, `get-comments.js`/`add-comment.js`/`delete-comment.js` full validation + auth + rate-limit + the E9 owner-mismatch SECURITY case — a verified token for one username cannot post as a different handle — + the PERMISSION matrix for delete (commenter / dream owner / forbidden third party / dream no longer on the feed), a server-side XSS negative-proof confirming storage preserves raw bytes verbatim — escaping is the RENDER-side responsibility only — `get-profile.js`'s `commentCounts` now genuinely filled in, and `report-dream.js`'s additive comment-target-type coverage), `test/comment-sheet-behavioral.test.js` (real Chromium against `explore.html`: signed-out "Log in to comment" with zero network call, a real XSS negative-proof — `<script>`/`<img onerror>` in both comment text AND a comment's handle render as inert escaped text and never execute — a per-instance-token-guard negative-proof (closing the sheet mid-post and reopening it for a DIFFERENT dream: the stale response must not contaminate the new sheet OR the original card's now-detached badge), optimistic post-with-reconciliation updating the card's badge via `onCountChange`, **a genuine failed-add-comment rollback test (mocked 500) asserting the optimistic entry is removed, the badge never bumps, and the error toast fires**, the empty state copy, the overflow menu's open/outside-close behavior and its Delete-only-on-your-own-comment visibility rule, a 44px tap-target check on the overflow button and Post button, an optimistic-success delete + block-hides-their-comments test, and a separate **genuine failed-delete-comment rollback test (mocked 500) asserting the comment reinserts at its ORIGINAL list position (not re-appended) and the error toast fires** — split out from a single prior test whose name claimed failure coverage its mock never actually exercised, flagged in review on this same branch. `test/social-layer-v2-profile-behavioral.test.js` (u.html) and `test/ui-behavioral.test.js` updated in place for the now-real (no longer inert-placeholder) comment action on both mount points, PLUS two new tests covering `u.html`'s own independent `loadCommentPreviewFor` render path specifically (also flagged as a real gap in the same review — it has its own separate `esc()` call site from `js/comment-sheet.js`'s): a comment-count>0 case asserting only the top-2 NEWEST comments render as preview lines, a `<script>`-bearing comment renders as literal escaped text and never executes, the 3rd/oldest comment is correctly excluded, and tapping the "View all N" teaser opens the shared sheet; and a comment-count===0 case asserting no preview markup renders and `get-comments.js` is never even called. | Threaded comments, comment likes, and notifications are explicitly out of scope per the design doc's own "Out of scope" section — flat comments with delete/report/block only. `u.html`'s top-2 preview-line fetch is a plain best-effort `fetch()` per rendered card (not batched/cached across cards) — fine at this app's current per-profile dream-count scale, worth revisiting only if a profile's dream count grows much larger. No `test/prod-smoke/` coverage — same posture as every other row in this section without one. |

## Tokens, entitlements, payments

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Entitlements core (balance, daily claim, refunds, purchases, achievements) | `test/entitlements-tokens.test.js`, `test/entitlements-daily-claim.test.js`, `test/entitlements-refund.test.js`, `test/entitlements-token-purchases.test.js`, `test/entitlements-achievements.test.js` | — |
| Daily claim (`claim-daily-tokens.js`, `get-token-status.js`) | `test/claim-daily-tokens.test.js`, `test/daily-claim-behavioral.test.js`, `test/token-daily-grant-copy-behavioral.test.js` | — |
| Daily claim, live, real server-confirmed balance change | `test/prod-smoke/session.test.js` (test 3) | — |
| Auto-refund on failed generation | `test/auto-refund-behavioral.test.js` | — |
| Install bonus (`claim-install-bonus.js`) | `test/claim-install-bonus.test.js` | — |
| Dodo Payments checkout + webhook, incl. "The Vault" pack ladder (pack099/199/499/999, $0.99/$1.99/$4.99/$9.99) and the pack099 starter one-time enforcement (E9) | `test/create-checkout-session-dodo.test.js`, `test/dodo-webhook.test.js`, `test/shop-pricing-copy-sweep.test.js` (repo-wide old-pricing/old-pack-id sweep) | A real end-to-end charge against the 4 new Dodo dashboard products is the only way to fully confirm the amount actually charged matches configuration — see `docs/PAYWALL_SETUP.md` |
| Stripe checkout + webhook (legacy/dormant path) | `test/stripe-webhook.test.js` | Live/branch merge status of the Stripe vs. Dodo path should be reconciled against `dodo-merge-decision`-style tracker history before assuming both are equally "live" — this row is about test coverage, not production status |
| Shop UI ("The Vault" — welcome-offer hero, pack cards, out-of-tokens sheet's single contextual offer, purchase conversion tracking incl. the `starter` flag) | `test/shop-behavioral.test.js`, `test/shop-purchase-conversion-behavioral.test.js`, `test/out-of-tokens-purchase-sheet-behavioral.test.js`, `test/purchase-sheet.test.js` | — (the shop's old "best value" pack-card palette A/B test and its own test file, `test/shop-palette-variant-behavioral.test.js`, were retired 2026-08-02 along with the page structure they targeted — see `docs/SHOP_PALETTE_REDESIGN_SPEC.md`'s superseded note; `test/shop-first-purchase-callout-behavioral.test.js` was likewise removed along with the +50%-bonus callout it covered, replaced by the starter-pack coverage above) |
| Owner top-up / bypass (`owner-topup-tokens.js`, `verify-owner-bypass.js`) | `test/owner-topup-tokens.test.js`, `test/owner-topup-target-behavioral.test.js`, `test/owner-bypass.test.js` | — |

## Interpretation / Chamber

| Feature/surface | Covered by | Gaps |
|---|---|---|
| `interpret-dream.js`, persona questions/readings | `test/interpret-dream.test.js`, `test/interpreter-personas.test.js` | — |
| Chamber dream strip (home embed) | `test/chamber-dream-strip-behavioral.test.js` | — |
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then a per-reading voice track + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`kokoroVoiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`). **VOICE VENDOR SWITCH, 2026-08-04 (tracker item `for-product-founder-decision-08-04-switc-cqveik`, founder decision — "much better" after listening to real samples of both):** the PRIMARY reading voice is now ElevenLabs Turbo v2.5 (`fal.run/fal-ai/elevenlabs/tts/turbo-v2.5`, voice `Brian`, `persona.voiceId`), reached through the same fal.ai infra/FAL_KEY this feature already used — measured 1257/1350/1321ms wall-clock at the exact hours Kokoro's own sync call was measured taking 18s+ under load. Captions come BUILT IN to that response (`timestamps:true` in the request) — the whisper leg is fully deleted for THIS tier. Kokoro (`am_onyx`, now `persona.kokoroVoiceId`, speed `0.9`) is UNCHANGED in mechanism but demoted to a two-tier FALLBACK (sync, then the legacy queue) — engages only if ElevenLabs sync fails/times out/throws, so a bad ElevenLabs day degrades rather than breaks the reading; kokoro's own whisper-based captions are unaffected on that fallback tier. Cost is founder-accepted by the choice (~$0.08/reading ElevenLabs vs $0.016 kokoro). `test/generate-interp-audio.test.js` extended: ElevenLabs-success-with-built-in-word-captions (asserts the exact request body incl. `timestamps:true` and no speed/stability override, and that ONLY the ElevenLabs endpoint is hit), ElevenLabs-succeeds-but-timestamps-missing/malformed degrading to sentence-level on the SAME tier (never falling back to kokoro just because captions failed — several malformed shapes covered), ElevenLabs-fails(http error)/throws(network) falling back to the kokoro sync tier with a full 3-endpoint cascade assertion, plus the pre-existing kokoro-sync-whisper-fails and both-sync-tiers-fail-to-queue tests re-verified still passing (now reached via an ElevenLabs-fails stub first, proving the fallback tiers still genuinely work, not just exist in code), plus dedicated `parseElevenLabsCaptions` unit tests for its defensive shape-parsing (accepts a `word`/`start`/`end` shape and this codebase's own whisper-chunk `{text,timestamp:[s,e]}` shape as a fallback convention, degrades to `null` on unrecognized/malformed input). **FLAGGED — NOT independently verified against a real ElevenLabs response** (no `FAL_KEY` in the build sandbox): the exact per-item field names inside the `timestamps` array (fal's own OpenAPI schema types it only as an untyped array, "for each word" — no field names published), whether timestamp values are seconds vs milliseconds (assumed seconds, matching every other timing convention already in this codebase), and whether ElevenLabs' DEFAULT pace (no speed/stability override sent, matching the founder's own approved sample) actually lands at a pace consistent with the 0.9 kokoro "feel" — a human with real listening/API access should verify both before/soon after this ships. `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff — kokoro's OWN legacy queue-fallback caption path, unaffected by the ElevenLabs switch — word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) — **NOTE: this row's "founder-preview gate (`?sagevoice=1`)" / gate-related language below predates the separate "GO WIDE" rollout (`main` commit `50cf2a3`) that removed that gate entirely; this voice-vendor-switch pass left that pre-existing drift as found, out of its own scope** | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Only `talmudic` ships voice at all this wave (scope: "sage persona first") — the other four personas have `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behave exactly like Wave 1 (silent, text-only). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Ships behind a founder-preview gate (`?sagevoice=1`) — code and assets are both ready; only the broad-rollout flip is still pending the founder's own preview walk. **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then Kokoro (`am_onyx`, speed `0.8`) voice + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`); the `?sagevoice=1` founder-preview gate this shipped behind is REMOVED as of `main` commit `50cf2a3` (founder "go wide" 2026-08-04) — voice eligibility is purely "does this persona have a voiceId," no switch to lose, live for every visitor | `test/generate-interp-audio.test.js` (validation, persona/voiceId gating, its OWN `interp-audio-ip` rate-limit scope, mock mode never bypassing a guardrail, PLUS — added 2026-08-04, tracker `cbdj3d` remaining-polish item — the sync-first happy path itself: a real one-round-trip kokoro+whisper success returning `done:true` with word-level captions and hitting `fal.run` never the queue, and the sentence-level degrade when whisper fails but kokoro still succeeds; this closed a real pre-existing coverage gap, since every prior test in this file exercised the LEGACY queue-submission path only, by relying on a stub response shape the sync leg would reject; **known remaining gap**: the whisper-SKIPPED-because-the-TTS-leg-already-ate-the-clock branch (`ttsElapsedMs > 11000`) has no dedicated test — not practically unit-testable without either a real 11+ second stub delay or mocking `Date.now()`, and out of scope for this pass since it predates it), `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff, word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Originally only `talmudic` shipped voice (scope: "sage persona first") — the other four personas had `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behaved exactly like Wave 1 (silent, text-only). **Superseded 2026-08-04** by the Chamber-makeover build (tracker `for-product-founder-spec-08-04-chamber-m-zf5ufo`, part 3 — see this file's own "Chamber makeover" row below for the full rename/tile-grid/interim-voice change): all five personas now carry the SAME `voiceId`/`introClipUrl`/`introVoiceUrl` values (jung/freud/gestalt/scientist temporarily borrow `talmudic`'s own `am_onyx` casting + intro assets verbatim, per the founder's own explicit instruction — see `js/interpreter-personas.js`'s header note), so every persona reaches a working voice stage today, not just The Sage; real per-persona casting stays a separate, later, founder-approved-per-persona effort (tracker `for-product-go-wide-follow-up-voice-the--bvqxvv`). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Live for every visitor (see the "REMOVED" note in this row's own Feature/surface column above). **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.
| **Chamber makeover — rename, tile-grid picker, interim voices for all personas** (tracker item `for-product-founder-spec-08-04-chamber-m-zf5ufo`, founder's own verbatim 3-part spec, 2026-08-04): (1) **NAME** — home.html's entry heading changed from the user-confusing "The Chamber" to "Dream Meaning" (recommended of 3 candidates — "Dream Meaning" / "What Your Dream Means" / "Interpretation" — all documented in a code comment at the rename site, home.html; result.html's `#interp-cta-btn` tooltip updated the same way for consistency); (2) **PICKER** — `js/interpret-experience.js`'s `renderPicker` replaced the horizontal swipeable `.itp-carousel`/dots with a 2-column `.itp-persona-grid` CSS grid (styled after style.html's `.style-card`, per the founder's own instruction to reuse that visual language) showing all 5 persona tiles at once, no scroll needed — the old carousel/dots CSS rules were deleted, not left dead; the 5th (odd) tile spans both grid columns generically off `personas.length % 2`; (3) **INTERIM VOICES** — `js/interpreter-personas.js`'s jung/freud/gestalt/scientist now carry the SAME `voiceId`/`introClipUrl`/`introVoiceUrl` as `talmudic` (see that file's own header note and the Speaking Sage row just above) | `test/chamber-makeover-behavioral.test.js` (new, real Chromium: home.html's hero heading is the new name not the old one, result.html's tooltip no longer says "Chamber," the picker container is a real CSS grid — not a horizontally-scrolling one — with the old carousel/dots fully gone, all 5 `.itp-persona-card` tiles render with the right `data-key`s, **all 5 tiles are simultaneously visible with no scroll needed at a real 390×844 mobile viewport** — the single most load-bearing assertion for part 2, both via Playwright's own per-element visibility check and a direct "does the last tile's bottom edge already fit within `#itp-body`'s visible bounds" geometry check, tapping a grid tile still advances the flow exactly like the old carousel cards did, and — one parameterized test per persona — each of the 4 non-Sage personas (jung/freud/gestalt/scientist) reaches a real working voice stage: the borrowed Sage intro clip mounts and can be skipped, `#itp-voice-stage` actually mounts (not `display:none`) for the reading, `interp_voice_play` fires carrying THAT persona's own key (not a hardcoded `talmudic`), and the plain Wave 1 text reading still renders alongside the voice — proving the "old silent/text-only experience" is genuinely gone for these personas, not just that the data field is set), `test/generate-interp-audio.test.js` (updated: the interim-voice change made `jung` a genuinely voice-eligible persona, so its old "rejects a persona with no voiceId" test — which used jung as its no-voiceId example — was repurposed into a positive "jung is now accepted" test, and the sibling mock-mode-validation test was moved onto a truly-nonexistent personaKey instead; the underlying `!persona \|\| !persona.voiceId` gate in `generate-interp-audio.js` is unchanged and still real defense-in-depth for any future persona added before it has voice casting, it just has no live real-persona example left to exercise that specific half of the OR today), plus the full pre-existing `test/interp-voice-behavioral.test.js`/`test/interp-analytics-behavioral.test.js`/`test/home-behavioral.test.js`/`test/interpreter-personas.test.js` suites re-run clean against these changes (all `.itp-persona-card[data-key="..."]` selectors those files already used were preserved verbatim, only the outer container/layout changed) | **Portraits are still placeholders for ALL FIVE personas, including `talmudic`/The Sage** — this build verified directly against `assets/interpreters/` that NO `.webp` portrait file exists for any persona today (only `assets/interpreters/intro/` — the Sage's intro video+voice — has real assets); every tile currently renders the pre-existing accent-gradient + initial-letter fallback (`.itp-persona-card-fallback`), which is the honest, already-graceful degrade this codebase established for a missing portrait, not a new gap this build introduced. Real portrait generation (fal.ai/flux, founder-pre-approved as a one-time negligible-cost spend) could not be executed from this build pass — no `FAL_KEY` in this sandbox (confirmed via `env`) — so all 5 image paths are wired and ready (`assets/interpreters/<key>.webp`) for whoever next has `FAL_KEY` access to actually generate and drop the files in; no code change will be needed once they exist. The interim-voice borrowing (part 3) means a non-Sage persona's intro clip will show the Sage's own visual likeness for now — the founder's own explicit, accepted tradeoff (see `js/interpreter-personas.js`'s header note), not an oversight. |
| **The interpretation offer DURING generation** (founder ask 2026-08-04, tracker item `for-product-founder-ask-08-04-offer-the--rlcai3`: "on processing while the dream generates, there is no offer to do the interpretation — there should be... entering the Chamber from processing must target the CURRENTLY-GENERATING dream... the reading can begin while the video is still rendering"). Three parts: (a) `home.html`'s forming room card gains `#d0-sage` ("✨ What does this dream mean?" — deliberately the same wording `result.html`'s completed-dream hero uses) in place of the dropped "Ready when your dream is" caption; (b) `chamberTargetDreamId()`'s priority inversion FIXED — it checked `completedDream` first, so any account with a generation in flight AND an earlier finished dream entered the Chamber on that older dream (the founder's exact report), and the pending branch was only ever reachable by an account that had never finished one; it now resolves pending-first, agreeing with `roomCardOwnedDreamId()`, which always had; (c) `InterpretExperience.notifyDreamResolved(pendingDreamId, realDreamId)` — a new public call `home.html`'s `onGenerationSettled` makes so an OPEN reading started against the synthetic `pending:<operationName>` id re-points at the real dream and swaps the finished video into the voice stage in place, reusing the pre-existing no-video path rather than adding a second one. **(d) Persistence-race fix, 2026-08-04** (independent-review finding, blocking, fixed on the same branch): (c)'s session-side swap turned out to be only half the mechanism. `js/store.js`'s `generateInterpretationReading` and `generateInterpAudio` both re-resolve `findDream(id)` at the moment their response arrives, and `findPendingDream` returned `null` the instant `pendingJob` was cleared — which `finalizeDream` does in the very same tick it creates the real dream. So a reading (or TTS track) still IN FLIGHT when the video landed had its persist-write skipped **silently**: it rendered fine in the live session (the swap deliberately tolerates the id change), and was simply gone on the next visit, with any real TTS spend behind it wasted. `generate-interp-audio.js`'s 18s `SYNC_TTS_BUDGET_MS` makes the audio call the likeliest of the two to lose this race. Fixed at the lookup rather than in each write path, so every `findDream()` caller is repaired at once: `findPendingDream` now falls through to a new `findResolvedDreamForOperation(operationName)` — the real, ownership-scoped dream whose `sourceOperationName` matches — whenever no live job matches, the same "use `sourceOperationName` to disambiguate a cleared `pendingJob`" move `isPendingJobStillCurrent` already makes for the same ambiguity. `notifyDreamResolved`'s header comment corrected in the same commit: it claimed the per-persona reading "was already persisted under the pending id," which was only ever true for writes that had already landed, never for one still in flight. Supporting: `js/store.js`'s `pendingDreamIdFor(operationName)` (the same synthetic id, computable after `pendingJob` is already cleared) and `interpret-experience.js`'s `stillTargetsDream()` staleness-guard tolerance for that one legitimate mid-session id change | `test/sage-during-generation-behavioral.test.js` (real Chromium, mobile viewport: the offer is visible/thumb-sized while forming and the old caption is gone; it steps aside in the ready state; tapping it produces a reading about the PENDING dream's text even with an older completed dream present; the Chamber pill does the same; the no-pending case still opens the completed dream; and the full swap-in — a reading mounted with no video, the video landing mid-session, the real url appearing on the SAME voice stage, the reading text not re-fetched, and the session's strip tile no longer naming a `pending:` id). "Which dream" is asserted via the `caption` actually POSTed to `interpret-dream` — the only unambiguous evidence, since an id-only assertion would pass against a stale session too. Plus, for (d), the persistence race itself: the reading response, and separately the TTS response, each held OPEN past the moment the video lands and `pendingJob` is cleared, then released — with the assertion made on the **durable record after a fresh page load**, never on what the still-open overlay is showing (the earlier tests all sequenced the race the other way, response-before-video, which is exactly why (d) shipped undetected). The reading case deliberately leaves its TTS call hanging forever: `generateInterpAudio`'s write block falls back to fabricating a `{ text, at, qa }` entry out of the text it was handed whenever `map[personaKey]` is missing, so it will otherwise resurrect a reading that was never actually saved and mask the very bug under test — that masking is real, it made a first draft of this test pass against the unfixed code. **Both (d) cases were verified as genuine regression tests: run against the pre-fix code they FAIL with exactly the reported symptom (record absent after reload while the live session rendered normally), and pass after the fix** | The swap-in path is covered with `GENERATION_MOCK_MODE`-style mocked `video-status`/TTS responses only — never against a real fal.ai generation (**this sandbox has no `FAL_KEY`**, see the note at the top of this file's own conventions). **REAL-MODE verification gate (`AGENT_POLICY.md`) — NOT satisfied, and explicitly deferred for this change rather than quietly skipped or vaguely assigned:** this sandbox has no `FAL_KEY`, no TTS credentials, no `.env`, and no Netlify CLI/deploy target (re-confirmed 2026-08-04 while fixing (d)), so a real fal.ai generation with real TTS latency cannot be run from here at all. **No real-mode pass is claimed for this row.** The honest cost of that deferral, stated rather than hand-waved: for (d) specifically it is narrow — the bug is entirely client-side (`findDream` resolution ordering against `localStorage`), and the mocked tests force the race *deterministically* by holding responses open, which exercises the ordering more reliably than real latency, which only produces it by chance. What real mode would still add is (i) confirmation that real TTS-vs-video latency actually lines up this way in production often enough to matter, and (ii) the iOS/autoplay/real-asset checks the gate exists for generally. A real-device walk of "start the reading, watch the video land under it" therefore remains a founder/Manager step — a known, accepted gap with a stated reason, not an open TODO this branch was supposed to close. The swap test asserts the real `src` lands on the SAME `<video>`, deliberately NOT that the bounce loop then advances `currentTime`: this sandbox's Chromium build has no proprietary codecs, so an mp4 never reaches `loadedmetadata` here at all (verified directly — `readyState` stays `0`, `duration` `null`), which is also why no pre-existing test in this repo asserts on real dream-video playback either; `nextBounceFrame`'s own math is unit-tested separately in `test/interp-voice-captions.test.js`. Also not covered: the same offer from `result.html` (that page has no pending-dream state of its own — it can only be reached with a real `?id=`), and the analytics events this adds (`home_day0_sage_tapped`, `interp_pending_dream_resolved`, `interp_pending_video_swapped_in`) have no dedicated assertions |
| **Speaking Sage — Option D** (`docs/SPEAKING_SAGE_SPEC.md` + its own "SUPERSEDED IN PART" note at the top, tracker item `for-product-build-speaking-sage-wave-fou-8uobuh`, founder GO on "Option D" 2026-08-02/08-03): one-time greeting intro, then Kokoro (`am_onyx`, speed `0.8`) voice + word-synced captions over the user's OWN dream video (bounce-looped) for the reading — `netlify/functions/generate-interp-audio.js`/`interp-audio-status.js` (new), `js/interpreter-personas.js` (`voiceId`/`introClipUrl`/`introVoiceUrl`), `js/interpret-experience.js` (voice stage state machine), `js/store.js` (`generateInterpAudio`, `hasIntroShown`/`markIntroShown`), founder-preview gate (`?sagevoice=1`, sticky per browser — no broad on/off flip yet) | `test/generate-interp-audio.test.js` (validation, persona/voiceId gating, its OWN `interp-audio-ip` rate-limit scope, mock mode never bypassing a guardrail, PLUS — added 2026-08-04, tracker `cbdj3d` remaining-polish item — the sync-first happy path itself: a real one-round-trip kokoro+whisper success returning `done:true` with word-level captions and hitting `fal.run` never the queue, and the sentence-level degrade when whisper fails but kokoro still succeeds; this closed a real pre-existing coverage gap, since every prior test in this file exercised the LEGACY queue-submission path only, by relying on a stub response shape the sync leg would reject; **known remaining gap**: the whisper-SKIPPED-because-the-TTS-leg-already-ate-the-clock branch (`ttsElapsedMs > 11000`) has no dedicated test — not practically unit-testable without either a real 11+ second stub delay or mocking `Date.now()`, and out of scope for this pass since it predates it), `test/interp-audio-status.test.js` (the two-stage Kokoro→Whisper poll handoff, word-chunk parsing incl. malformed-chunk tolerance, the sentence-level degrade path on either a submission or alignment failure, mock mode), `test/interp-voice-captions.test.js` (pure logic: the intro-eligibility gate incl. requiring BOTH intro files, the sentence-fallback proportional-time schedule, the caption-index lookup, the bounce-loop frame math), `test/interp-voice-behavioral.test.js` (real Chromium: the preview gate is off by default and stays on/sticky once granted, the full intro→crossfade→reading flow, Skip and mid-intro Close both mark the intro shown and fire the right `via`, a dream that already has a reading does NOT replay its intro, capability-detected autoplay-blocked tap-to-play on the reading track, `interp_voice_complete`/`interp_voice_listen_time` off a real `ended` event, the caption-fallback event + client-computed sentence schedule actually rendering text, a hard TTS failure hiding the whole voice stage while the plain Wave 1 text card still renders, the reading-phase loading window's persona-named "preparing" caption appearing and clearing the instant real playback is attempted, the tap overlay's label being explicit/persona-name-driven for the pre-first-play state and reading differently once it reappears as "replay", and a synchronously-throwing gesture-priming attempt never breaking the persona pick or reading flow) | **ASSET STATUS — LAUNCH-READY as of 2026-08-03:** `talmudic`'s (The Sage's) intro is now the real founder-approved Option D handoff, landed directly on `main` (commit `c0b9202`) — `assets/interpreters/intro/sage-intro-reference.mp4` (silent, looping visual, confirmed via direct MP4 atom inspection to carry only a `vide`-handler track, ~6.68s) + `assets/interpreters/intro/sage-intro-voice.wav` (the actual spoken greeting, mono/24kHz, ~7.25s — the real audio-driven timing/capability-detection source, since the two files' durations don't match exactly and are two separately-delivered takes, not one frame-locked mux). No placeholder remains — the earlier build pass's placeholder copy (`assets/interpreters/intro/sage.mp4`) has been removed. Only `talmudic` ships voice at all this wave (scope: "sage persona first") — the other four personas have `voiceId`/`introClipUrl`/`introVoiceUrl` all `null` and behave exactly like Wave 1 (silent, text-only). The caption-timing mechanism still deliberately deviates from the tracker's own ffmpeg `silencedetect` text — see `interp-audio-status.js`'s header comment for why (no ffmpeg dependency in this repo/sandbox; reuses this codebase's own already-integrated Whisper word-alignment call instead) — this is an intentional, documented substitution, not a gap. Ships behind a founder-preview gate (`?sagevoice=1`) — code and assets are both ready; only the broad-rollout flip is still pending the founder's own preview walk. **UX fix, tracker item `for-product-p1-bug-founder-repro-sage-re-cbdj3d`** (founder walk 2026-08-03 23:31: the dream video looped silently ~3x during real TTS latency with zero indication a voice reading was coming, then only a bare unlabeled play-triangle appeared once audio was ready — most users would never discover it): `js/interpret-experience.js` now shows persona-named "preparing" copy in the caption strip for the whole silent loading window (cleared the instant real playback is attempted, win or lose), gives the tap overlay an explicit persona-named label that differs across its three reuses (intro/first-play/resume/replay — `tapOverlayLabel`), and best-effort primes a real `<audio>` element during the persona-pick tap's own gesture so a later autoplay attempt on the reading has a better chance of being honored (`createPrimedAudioElement`, degrades silently to the labeled tap button if unsupported/blocked). The underlying kokoro TTS queue latency itself (the reason the loop happens at all) is explicitly OUT of this fix's scope — being handled server-side separately (see the tracker item's own comment history). **Remaining-polish pass, 2026-08-04 (same tracker item):** two independent build cycles worked this same "verify long readings + instrument the ratio" ask in parallel — one (`361dd20`/`5376755`) landed directly on `main` (raised `SYNC_TTS_BUDGET_MS` 12s→18s, added a whisper-skip-when-budget-spent optimization, bumped `READING_SPEED` 0.8→0.9 after a founder walk found 0.8 "slightly too slow" — **founder-confirmed working live, board notes: "reading voice fixed at root — 5.3s measured live for a full-length reading", "Sage founder-confirmed working"**), the other sat in a since-reconciled PR that added `outcome=`/`ttsMs=` log lines (matching no pre-existing convention — this is the first use of that shape in the repo) and fixed a real latent bug: both `clearTimeout` calls used to sit AFTER their awaited `fetch`/`.json()` calls, so a thrown fetch (network error, or the abort itself) skipped clearing the timer entirely — harmless in production (an abandoned `AbortController.abort()` is a no-op) but it kept every failure-path test in this file waiting out the full budget in real wall-clock time; both now use `finally`. The reconciled version keeps `main`'s founder-validated 18s/0.9-speed/whisper-skip logic and adds the orthogonal timer-fix + logging on top — see `SYNC_TTS_BUDGET_MS`'s own comment in `generate-interp-audio.js` for the still-unresolved "26s ceiling" sourcing question (this codebase's own `transcribe-audio.js` documents an empirically-tested 10s figure instead) that the new logging exists to help answer empirically, without touching the founder-confirmed-working value.

| **Scroll-lock release on page hide/bfcache-restore** (tracker item `for-product-bug-founder-video-08-07-resu-f4u8zy`, founder screen recording: result.html sometimes became entirely unscrollable — swiping down only rubber-banded into Safari's pull-to-refresh, intermittently). Root cause: `js/interpret-experience.js`'s `open()`/`close()` is the ONLY place on result.html that locks `document.body.style.overflow` (every other overlay — `.player-overlay`, `.sheet-overlay`, `.modal-overlay` — is `pointer-events:none` when closed, so a stuck `.open` class there is at worst a visibly-stuck overlay, never an invisible scroll block; confirmed by reading every such overlay's CSS/JS). `close()` already released the lock correctly on its three explicit triggers (topbar X, the post-reading Close link, the error-state Close link); the gap was every OTHER way the page can go away while the lock is held — most notably iOS Safari's native edge-swipe back gesture, which does not run this page's click handlers and can freeze the page into the back/forward cache (bfcache) with the lock still on, to be restored verbatim later with no fresh page load to ever reset it. Fixed with a `pagehide` listener that runs the SAME `close()` every explicit dismiss path already uses (so the lock, `#itp-root`'s `.open` class, and voice/audio teardown all release together, not just the CSS), plus a `pageshow{persisted:true}` listener as defense-in-depth that force-clears the lock unconditionally on any bfcache restore | `test/result-scroll-lock-behavioral.test.js` (new, real Chromium: sanity baseline that the topbar Close button already released the lock correctly; the actual regression — abandoning the overlay open with no explicit close, then dispatching `pagehide`/`pageshow{persisted:true}` (the same events a real bfcache freeze/restore fires, including via iOS Safari's edge-swipe back — the standard, portable way to test `pagehide`-driven cleanup since no engine offers a way to force a *real* bfcache cycle from automation, and this sandbox has no WebKit at all) — releases the lock and tears down the session; a `pagehide` with no active interpretation session is a verified no-op, not an accidental clear of `body.style.overflow` some OTHER unrelated feature had set. **Verified as a genuine regression test**: run against the pre-fix code, the middle test fails with exactly the reported symptom (`body.style.overflow` stays `'hidden'` forever); passes after the fix) | **Not verified against a real iOS Safari bfcache freeze/restore** — this sandbox has no WebKit engine at all (only Chromium is installed), and Chromium's own bfcache is known to be unreliable under DevTools Protocol/Playwright automation, so no test here forces a genuine bfcache cycle end-to-end; the fix and its test instead target the exact `pagehide`/`pageshow` events every engine (including iOS Safari) is documented to fire around such a cycle. A real-device walk (open the Chamber reading, edge-swipe back, edge-swipe/tap forward, confirm result.html scrolls) remains a founder/Manager real-mode check, not run from this sandbox. |

## Retention / notifications / analytics

| Feature/surface | Covered by | Gaps |
|---|---|---|
| First-dream retention email | `test/automatic-first-dream-email.test.js`, `test/send-first-dream-email.test.js`, `test/first-dream-email-store.test.js` | — |
| **Unsubscribe + email suppression + redesigned template** (tracker item `for-product-email-redesign-unsubscribe-l-16ysmp`, founder ask: "make the email content look better + add unsubscribe", founder-approved and shipped): `unsubscribe-email.js` (GET endpoint, verifies `lib/unsubscribe-token.js`'s per-email HMAC token, records the unsubscribe via `lib/email-suppression-store.js`), scoped to the two retention/marketing senders in scope today — `lib/first-dream-email-sender.js`'s `sendIfEligible` and `dream-webhook.js`'s `sendReadyEmail` — both now check `isSuppressed` before sending and carry a real unsubscribe link; both templates rebuilt on the shared `lib/email-layout.js` night-aesthetic shell (logo header, dark card, unsubscribe+address footer), whose footer mailing address is the founder's own real address ("10 Dgania, Herzliya, Israel", CAN-SPAM-required), rendered deliberately small/faint (his own explicit ask) via a dedicated `ADDRESS_COLOR` a step fainter than the unsubscribe line's own text color | `test/unsubscribe-token.test.js` (deterministic signing, cross-email/tampered-token rejection, secret-rotation changes the token, graceful no-env-var fallback — confirms `UNSUBSCRIBE_TOKEN_SECRET`'s fallback to `RESEND_API_KEY`/a hardcoded dev constant works correctly with no dedicated env var ever set), `test/email-suppression-store.test.js` (suppress/isSuppressed, idempotent double-suppress, normalization, unrelated emails unaffected), `test/unsubscribe-email.test.js` (valid token unsubscribes and is idempotent, invalid/tampered/cross-email token is rejected WITHOUT suppressing, missing-params/wrong-method errors), `test/send-first-dream-email.test.js` (a suppressed email is actually skipped end-to-end with no Resend call, an unrelated account is unaffected, redesigned template asserts logo + unsubscribe link present), `test/dream-webhook.test.js` (same suppression-skip proof for the second in-scope sender, redesigned template asserts logo + unsubscribe link present, rest of the webhook flow still completes when the email itself is skipped) | Explicitly OUT of scope, by the tracker item's own framing (transactional, not marketing — must never be suppressible or carry an unsubscribe link): `request-password-reset.js`, `lib/verification-email-sender.js` (used by `register-account-passwordless.js`/`resend-verification-code.js`), `submit-support-message.js` — all left on their prior plain (non-redesigned) template, unchanged. `UNSUBSCRIBE_TOKEN_SECRET` is still not a dedicated env var in the real Netlify environment — confirmed as an accepted, working default (not a blocker): the fallback chain (`RESEND_API_KEY`, then a hardcoded dev constant) still produces a real, unguessable per-email token either way, see `lib/unsubscribe-token.js`'s own header comment. No live end-to-end proof against a real Resend inbox (same sandbox limitation every other email-sending row in this registry already accepts). |
| Retention-email real first-frame thumbnail + THUMBNAIL-GATED SEND (`upload-dream-thumbnail.js`, `lib/first-dream-email-sender.js`'s `buildHtml`/`absoluteImageUrl`, `js/store.js`'s `DreamStore.saveThumbnailBestEffort`, result.html's capture IIFE, `lib/first-dream-email-pending-store.js`, `send-pending-first-dream-emails.js`) — tracker item `for-product-dream-ready-email-real-first-qr9fbj`, extended by `for-product-email-redesign-unsubscribe-l-16ysmp`'s founder-approved follow-up (2026-08-03/04, "wait up to 3 MINUTES for the captured image... if not, SEND ANYWAY at the 3-minute mark"). **ROOT-CAUSE FIX (2026-08-07, tracker item `for-product-p1-regression-evidence-found-7lbwkx`, founder-confirmed regression: he had never once received a real thumbnail):** the capture IIFE's canvas draw used the VISIBLE, playback `#result-video` element, which never set `crossOrigin` — and `dream.videoUrl` is routinely cross-origin (video-file.mjs 302-redirects to fal's own CDN past an 18MB threshold, the common case for a real veo3.1 720p/8s/audio-on clip). Drawing a cross-origin media element with no `crossOrigin` attribute ALWAYS taints the canvas and throws on `toDataURL()`, regardless of whether the remote server sends CORS headers — the old code's catch block silently swallowed this on essentially every real dream. Fixed by capturing from a SEPARATE, detached `<video>` element (created fresh per attempt, `crossOrigin='anonymous'` set before `.src`, never inserted visibly, bounded 6s timeout, always cleaned up) instead of the real playback element — `#result-video` itself is never touched (no `crossOrigin`, no src changes), so playback can never regress even if fal's CDN turns out not to send CORS headers. | `test/upload-dream-thumbnail.test.js` (endpoint validation, authToken identity, per-account key namespacing, idempotency, and proving the write is served back out unchanged by `image-file.mjs`), `test/send-first-dream-email.test.js` (real-`<img>` vs flat-color-banner branch, relative→absolute url resolution, `absoluteImageUrl` unit cases), `test/first-dream-email-pending-store.test.js` (the enqueue store in isolation: idempotent CAS enqueue, list/get/remove, fail-closed on a write error), `test/send-pending-first-dream-emails.test.js` (the scheduled scan's own decide logic: sends immediately once a thumbnail is synced regardless of the deadline, waits with no send while still within the window, sends the fallback once the 3-minute deadline passes — inclusive boundary — multiple pending records decided independently in one scan, a record whose account already got its once-ever email via a different path is a harmless dequeued no-op), `test/automatic-first-dream-email.test.js` (mark-generation-completed.js now only ENQUEUES — every existing job-owners/pending-dreams race-hardening test updated to flush the deferred scan before asserting on an actual send; the CONCURRENCY test now races two overlapping *scans* instead of two overlapping *mark* calls, since the enqueue step itself can no longer collide across two different operationNames; both send-orderings between the automatic path and the client-triggered fallback are covered), `test/funnel-real-chain-behavioral.test.js` (the real-browser-driven, real-handler end-to-end chain also updated to force its own enqueued record past the deadline and run the scan directly before asserting the real Resend send — proves the deferred send still genuinely fires off the REAL chain, not just the isolated unit tests), **`test/result-thumbnail-crossorigin-capture-behavioral.test.js`** (new, 2026-08-07, real Chromium + two real standalone cross-origin HTTP servers on `127.0.0.1` serving a real decodable video file — not mocks: (1) no `Access-Control-Allow-Origin` on the remote video → capture stays a silent no-op, `upload-dream-thumbnail` never called, no uncaught error escapes the page — proves the fix is no worse than before; (2) `Access-Control-Allow-Origin: *` present → capture succeeds, `upload-dream-thumbnail` called once with a real `data:image/jpeg` payload — proves the fix actually works when a CDN cooperates; (3) in BOTH cases, `#result-video` never gets a `crossOrigin` attribute and still decodes/plays a real frame — the hard regression guard; (4) `dream.imageUrl` already set → capture never runs, existing guard preserved; also proves the detached capture `<video>` is fully removed from the DOM afterward, never left leaking) | Correlating a pending operationName to its thumbnail relies on `lib/dream-store.js`'s already-synced private-dream record (`sourceOperationName`/`imageUrl`) — if the client never reaches result.html at all (tab closed, no sync ever lands), or the dream was already published (out of `dream-store.js`'s scope by design) before the 3-minute window elapses, the scan correctly falls back to the flat-color banner rather than finding no dream at all; this is the intended honest degrade, not a bug. The scheduled scan runs every minute (`netlify.toml`), so the real-world bound is "up to roughly 3 minutes plus one sweep interval" either direction, not the exact 3:00 mark — see `send-pending-first-dream-emails.js`'s own header comment. No live end-to-end proof against a real deployed Netlify scheduled-function invocation (same sandbox limitation `test/send-daily-claim-pushes.test.js`, this repo's first scheduled function, already accepts). Capture is also only wired on result.html, not explore.html's separate resume-completion path (`fireFirstVideoCreatedIfEligible`) — that path has no dedicated full-size `<video>` element to capture a frame from, so a dream completed that way keeps the flat-color banner until it's later opened on result.html. **Still genuinely unknown from this sandbox (safe either way, but determines whether the fix visibly changes anything for real users): whether fal's actual production CDN (`fal.media`) sends `Access-Control-Allow-Origin` on its redirected video responses** — no network access from here to check. If it does, real thumbnails should now start appearing; if it doesn't, behavior is unchanged from before this fix (still the flat-color banner) but is provably no worse — see this fix's own PR description for the full reasoning. |
| FirstVideoCreated conversion event + durable markers (`mark-generation-completed.js`, `consume-generation-marker.js`) | `test/first-video-created-behavioral.test.js`, `test/first-video-created-explore-resume-behavioral.test.js`, `test/generation-completion-marker.test.js`, `test/retention-email-first-video-behavioral.test.js` | — |
| Web push (daily-claim-available, dedup) | `test/send-daily-claim-pushes.test.js`, `test/push-dedup-store.test.js`, `test/push-subscription-store.test.js` | — |
| Morning Capture Ritual — WhatsApp channel (opt-in field, `save-whatsapp-number.js`, `send-whatsapp-morning-capture.js`'s per-day dedup + PILOT-ONLY GATE, `admin-submit-whatsapp-template.js`'s one-off Meta template submission) — tracker item `for-product-build-whatsapp-morning-captu-skez3n` | `test/account-store.test.js` (whatsappNumber field), `test/save-whatsapp-number.test.js`, `test/send-whatsapp-morning-capture.test.js` (send/dedup mechanics, plus its own section 3: `isPilotOnly`/`isSendEligible`, `scanAndSend`'s `pilotSkipped` counting, proving a pilot-skip never consumes the per-day dedup slot so a later go-live flip still reaches that account same-day), `test/whatsapp-morning-capture-opt-in-behavioral.test.js`, `test/admin-submit-whatsapp-template.test.js` (the exact Meta Message-Templates-create request shape, owner-email gating, missing-config reporting, Graph API/network error surfacing) | Cannot exercise a real Meta send or a real template submission — no live `WHATSAPP_ACCESS_TOKEN`/`WHATSAPP_WABA_ID`/approved template in this sandbox, same honest-degrade limitation `test/send-daily-claim-pushes.test.js` already accepts for real push delivery; the real template submission and the first real pilot send stay manual steps for Manager/Ron (see `admin-submit-whatsapp-template.js`'s own header comment). No email/push sibling channel exists yet for this same ritual (this is currently the only channel) — see `send-whatsapp-morning-capture.js`'s own header comment. The pilot roster is `lib/test-identity.js`'s existing founder-alias list, not a dedicated new one — see `send-whatsapp-morning-capture.js`'s PILOT-ONLY GATE header comment for why. |
| Meta CAPI / conversion tracking (`track-conversion.js`) | `test/meta-capi.test.js`, `test/meta-capi-behavioral.test.js`, `test/funnel-meta-attribution-cookies-behavioral.test.js` | — |
| PostHog identity/test-traffic tagging | `test/posthog-identity-merge-behavioral.test.js`, `test/posthog-test-traffic-tagging-behavioral.test.js`, `test/server-analytics-containment.test.js` | — |
| Support/feedback inbox (`submit-support-message.js`, `get-support-messages.js`) | `test/support-feedback.test.js`, `test/support-feedback-behavioral.test.js` | Whether `get-support-messages.js` (the admin-read side) has its OWN direct handler-level test, vs. only being exercised indirectly through the behavioral submit flow, should be spot-checked next time this row is touched |

## Owner/admin tooling & coordination

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Paywall toggle | `test/admin-paywall-toggle.test.js` | — |
| WhatsApp morning-capture template submission (`admin-submit-whatsapp-template.js`, owner-gated, never runs automatically) — tracker item `for-product-build-whatsapp-morning-captu-skez3n` | `test/admin-submit-whatsapp-template.test.js` (exact Meta Message-Templates-create payload shape/category, GET preview vs POST auth split, owner-email gating, missing-config reporting, Graph API/network error surfacing) | No real Meta submission exercised (see the Morning Capture Ritual row above) — the real call is a manual step for Manager/Ron. |
| tracker.html (`get/add/update/delete-tracker-item.js`) | `test/tracker.test.js`, `test/tracker-behavioral.test.js`, `test/tracker-comments-behavioral.test.js`, `test/tracker-reviewed-behavioral.test.js`, `test/tracker-speak-behavioral.test.js`, `test/tracker-waiting-for-behavioral.test.js` | — |
| Media library page + backfill sweep (`media-library-x7q4.html`, `admin-backfill-media-rehost.js`, `admin-media-library-data.js`) — tracker items `for-product-owner-media-library-page-fou-1fwxaw`, `for-product-media-library-founder-feedba-wnr3iw` (Published/Private filter + desktop-scroll fix), `for-product-media-library-clicking-a-vid-bnlcus` (card-tap lightbox), `for-product-media-library-founder-08-03--6h7fmv` (dream-text-per-card) | `test/admin-backfill-media-rehost.test.js` (auth, idempotency, pagination across the accounts→feed phases, per-field re-host outcomes), `test/admin-media-library-data.test.js` (auth, item shape, status classification matching `lib/media-status.js` directly, both/neither-media-field edge cases, and the `storyText` field: a private dream's distinct `storyText` returned verbatim over its `caption`, a pre-split private dream with no distinct `storyText` falling back to `caption`, a published feed item — which never carries a separate `storyText` on its own wire shape — using its `caption`, and a dream with no text on file at all returning `null` rather than an empty string), `test/media-library-page.test.js` (Playwright: gate → unlock → grid render → status/account/published filters including all three combined, a 1280×800 desktop-viewport test that seeds 60 items to force real overflow and proves the grid's internally-scrolling region actually reaches content past the fold, the card-tap lightbox — opens a real unmuted controls-enabled `<video>` or a bigger `<img>` using the item's real url rather than the in-grid muted/lazy thumbnail, and closes via its own close button, Escape, or tap-outside-the-media, while a tap ON the media itself does not close it — and per-card dream text: a card whose item carries `storyText` renders it in a `.interp-text` element with the exact text, a card with no `storyText` on file renders no description element at all) | Both admin endpoints are unpaginated on the FEED side and the data-listing endpoint is unpaginated on the accounts side too (documented in each file's own header comment as an accepted "fine at current scale" tradeoff, same posture `send-daily-claim-pushes.js` already established) — would need real pagination if the account base grows large. The Published/Private filter's underlying data (private items, `isPublished` flag) was already correct in both the backend and the per-card badge before this change — verified by reading `admin-media-library-data.js`/`lib/dream-store.js` before building; only the dedicated filter control was missing, so no backend change was needed for that part. The desktop-scroll bug's root cause: `#app` (no `.scroll-shell`/`.scroll-area`, the pattern `tracker.html` already uses for the same long-content-owner-page shape) gets a fixed height + `overflow:hidden` once the desktop phone-frame breakpoint (`@media min-width:560px` in `css/styles.css`) applies, with nothing to scroll content past that height — invisible on mobile, where `#app` is plain block flow and the whole document scrolls normally instead. The lightbox's tap-outside-to-close deliberately reuses `js/sheet-dismiss.js`'s `SheetDismiss.wire()` for its same-spot re-tap guard rather than a bespoke listener (see `media-library-x7q4.html`'s own inline comment); Escape-to-close is a small standalone `keydown` listener since that module doesn't cover it. The founder's own phrasing asked for the dream text "rendered the same way" as "the interpretation text" — verified before building that NO existing interpretation-text (or any dream-text) element had ever been rendered on this page at all, so this reuses `css/styles.css`'s own `.interp-text` class verbatim (the one place this codebase actually renders something called "interpretation text," `js/interpret-experience.js`'s reading card) rather than inventing new typography; the field itself is `storyText` (falling back to `caption` for a pre-split dream), never `promptText` (the engineered generation prompt, not what the user typed) — see `admin-media-library-data.js`'s own header comment for the full fallback reasoning. |

| Instagram/TikTok post pack MVP — Phase 1, manual-export only, no posting APIs (`assemble-instagram-tiktok-postpack.js` nightly scheduled job, `lib/postpack-selector.js`, `lib/postpack-caption.js`, `lib/postpack-store.js`, `admin-postpack-data.js`, `postpack-h4mv.html`) — tracker item `for-product-instagram-tiktok-auto-postin-cahr76`, Manager's SPEC v1: reuses the existing `channelLicenseGrantedAt`/`channelLicenseRevokedAt`/`okToFeatureOnChannels` consent fields (tracker item `for-product-terms-republish-license-per--fhpcxk`) already carried on every shared-feed record, and reuses `lib/push-dedup-store.js`'s existing `markSentOnce`/`hasSent` (not a new store) for both the max-1-per-day-per-channel guard (`'postpack-daily:'+channel+':'+utcDate`) and the durable, permanent never-repeat-a-dream-across-all-history guard (`'postpack-dream:'+dreamId`, global across BOTH channels — once a dream is used for either channel it is never reused for the other) | `test/postpack-selector.test.js` (pure eligibility rules: requires a real `channelLicenseGrantedAt`, excludes a revoked or per-dream-opted-out dream, excludes ANY dream with a moderation report on file regardless of reason, requires real media, newest-`publishedAt`-first ordering incl. a missing-`publishedAt` record sorting last), `test/postpack-caption.test.js` (caption includes the dream's own text/style/@-credited handle without a double `@`, hashtags are a deterministic per-style set — all four real app styles plus an unrecognized-style/generic fallback — deduped and capped), `test/assemble-instagram-tiktok-postpack.test.js` (end-to-end against a seeded fake feed: builds one item per channel with the newest-first + cross-channel-distinctness behavior the dedup markers actually produce, output item shape incl. video-vs-image `mediaType`/`mediaUrl` passthrough, each individual selection-policy rule skipping the right dream, max-1-per-day-per-channel via a same-day re-run building nothing new and persisting no duplicates, and the GLOBAL never-repeat marker excluding an already-used dream even with fresh unclaimed daily markers), `test/admin-postpack-data.test.js` (owner-gate/rate-limit shape duplicated from `admin-media-library-data.test.js`, newest-`builtAt`-first ordering, empty-store handling), `test/postpack-page.test.js` (Playwright: login-redirect, wrong-password error, correct-password grid render with channel badges, channel filter, empty state, the per-card caption+hashtags readonly textarea matching the built item exactly, the copy button's "Copied!" confirmation, and the download link's real media url) | Phase 2 (actually posting to Instagram/TikTok via their real APIs) is explicitly out of scope for this MVP and unbuilt by design — see the tracker item's own "DO NOT build Phase 2" instruction; this only assembles a pack for the founder to manually copy/download/post. No real Meta/TikTok developer account or credential exists anywhere in this build. Both the nightly job and the owner data endpoint are unpaginated full-store scans, same accepted "fine at current scale" tradeoff as `admin-media-library-data.js`/`send-daily-claim-pushes.js`. |

## Cross-cutting / infrastructure

| Feature/surface | Covered by | Gaps |
|---|---|---|
| Rate limiting (`lib/rate-limit.js`) | Exercised indirectly inside several endpoint tests (e.g. `test/check-email.test.js`, `test/claim-daily-tokens.test.js`) | No single dedicated `rate-limit.test.js` isolating the shared helper itself |
| Netlify Blobs retry helper | `test/blobs-retry.test.js` | — |
| Shared bottom-sheet dismissal (`js/sheet-dismiss.js` — tap-outside, drag-to-dismiss, wired into all 13 `.sheet-overlay` call sites app-wide) | `test/sheet-dismiss-behavioral.test.js` (per-instance five-point suite across profile.html/result.html/create.html/wizard.html/js/purchase-sheet.js, plus a dedicated rapid-re-tap-after-open regression — tracker item `for-product-urgent-founder-posthog-recor-75ob70`: a tap-outside click landing within the sheet's own opening transition window used to self-dismiss it immediately, reading as "the sheet never opens" on a real PostHog recording) | — |
| Web push helper | covered via `test/push-dedup-store.test.js`/`test/push-subscription-store.test.js` and `test/helpers/mock-web-push.js` | — |
| **Live, real-origin, end-to-end smoke coverage** (as opposed to mocked-backend coverage) | `test/prod-smoke/*.test.js` (this file's own registry entry) | Intentionally narrow — see `test/prod-smoke/README.md`. Does NOT cover: payments/checkout, Facebook Login, admin/owner tooling, transcription, interpretation/Chamber, push notifications, or most retention email paths. Those stay covered only by the mocked `test/*.test.js` suite. |
| **Reliability net: recurring both-domain health checks** — a scheduled Netlify Function (`both-domain-smoke.js`, every 4h) hits both `dreamtube1.netlify.app` and `dreamtube.life` for every key page + a curated set of core function endpoints (`get-feed`, `get-token-status`, `get-tracker-items`, `check-email`), asserting a real 200 + content marker, and writes results to `lib/smoke-status-store.js` (read back via the public `get-smoke-status.js`) — tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 1 | `test/both-domain-smoke.test.js` (checkPage/checkFunction/runDomainChecks/scanAndCheck pass/fail logic, with a fully faked fetch — no real network calls), `test/smoke-status-store.test.js`, `test/get-smoke-status.test.js` | Deliberately does NOT auto-file a tracker item on a real failure (this build has no owner-email credential to call `add-tracker-item.js` with) — writes to the status store only; whether to add automatic tracker-filing later is an open decision for Manager/Ron, not attempted here. Never actually run against a live Netlify deploy from this sandbox (no way to trigger a real scheduled invocation here) — `runDomainChecks`/`checkPage`/`checkFunction` WERE run directly (not via the scheduled `exports.handler` wrapper) against real production for both domains as part of this build's own verification, and passed cleanly on both. |
| **Reliability net: nightly state-seeded journeys, both domains** — `test/prod-smoke/session.test.js` (Journey 2 signup → claim → generate → edit → publish/unpublish → nav walk, now also asserting shop.html renders a real balance + pack cards), `test/prod-smoke/journey1-funnel-render.test.js` (Journey 1 entry), `test/prod-smoke/media-serving.test.js` (real media playback) run nightly against BOTH domains via `npm run smoke:prod:dreamtube1`/`npm run smoke:prod:life`, fired by a scheduled Routine (this environment's own external cron mechanism, NOT a Netlify Function — see `test/prod-smoke/README.md`'s "Nightly scheduling" section for why a real Playwright/Chromium run cannot execute inside a Netlify Function's own runtime), reporting its result to `report-smoke-status.js` — tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 2 | The three prod-smoke files listed (already covered, and cited, in their own rows above) + `test/report-smoke-status.test.js` (the reporting endpoint's own auth/validation logic) | `SMOKE_STATUS_REPORT_TOKEN` (the shared secret `report-smoke-status.js` requires) is not yet set in Netlify — until a human sets it, the nightly run's own status report soft-fails (503 `not_configured`, logged, never a hard error) rather than actually landing in the shared store; the Routine itself still runs the real tests and reports pass/fail in its own session regardless. See this build's own report for the one-line step to close this gap. |
| **Domain routing matrix** (which domain serves what, and why both are legitimately live at once) — `docs/DOMAIN_ROUTING_MATRIX.md`, tracker item `for-product-reliability-net-spec-v1-smok-x1o5zc`, piece 3 | `test/prod-smoke/domain-routing-matrix.test.js` (run against real production as part of this build — see that file's own header comment; all 5 assertions passed live against both real domains) | — |

## Last reconciled

2026-08-04, alongside tracker item `for-product-founder-08-04-evening-music--jfjco0`
(mood-keyed music beds): added the mood-keyed-bed row above covering the
`dream.mood` persistence fix (the wizard's Mood answer used to reach the
generated prompt text and nothing else), the two-tier `urlForDream`
selection layer, and `bed-audition-x7q4.html`. The row's Gaps column
discloses the real, un-worked-around blocker: no `FAL_KEY` in the build
sandbox, so the 12 candidate tracks don't exist and selection tier 1 is
inert — shipped as a tested layer that provably changes nothing a user
hears today, and switches on with files only.

Corrected later the same day, after review found a real bug on the same
branch: that row's coverage column had claimed the mood-survives-cross-
device-sync path was proven, citing `test/dream-sync.test.js`. It wasn't
— that test drives the server handler with a hand-built payload and never
exercises the real client, which was in fact dropping `mood` from its
outgoing sync body entirely (`js/store.js`'s `attemptPrivateDreamSync`
builds that body from an explicit hand-maintained field list). Server
allowlist correct + local state correct + wire silently missing the field
is exactly the shape a handler-only test cannot see. The column now says
which half each test actually covers, and the client-side wire contract
has its own coverage. **General lesson worth carrying:** for any field
that has to cross a client→server boundary, coverage of the server
handler and coverage of local state can both be green while the field
never leaves the browser — assert on the outgoing request body itself.

2026-08-04, alongside tracker item `for-product-surface-deploys-day-as-a-vis-xld1vk`
("Surface deploys/day as a visible number"): added the race-free
deploy-log write/count design above (`lib/deploy-log-store.js`,
`get-deploy-count.js`, wired into `scripts/generate-version.js`'s
existing per-build step and folded into `admin.html`'s existing
release-visibility line rather than a new page). See the new
Cross-cutting/infrastructure row above for the honestly-disclosed gap
(build-time write needs a founder-provided Netlify PAT env var; not yet
exercised against a real Netlify deploy from this sandbox).

2026-08-03, alongside tracker item
`for-product-reliability-net-spec-v1-smok-x1o5zc` ("SPEC v1: smoke +
seeded journeys now; nightly real-generation probe HELD for founder spend
approval"): added the reliability-net buildout's three no-spend pieces —
`both-domain-smoke.js` (scheduled, both domains, key pages + core
function endpoints), a nightly external-Routine-driven run of
`test/prod-smoke/*.test.js` against both domains (see that directory's
own README "Nightly scheduling" section for why this couldn't be a
Netlify Function instead), and `docs/DOMAIN_ROUTING_MATRIX.md` +
`test/prod-smoke/domain-routing-matrix.test.js`. Also extended
`test/prod-smoke/session.test.js`'s existing nav-walk with a shop.html
render check. See the three new Cross-cutting/infrastructure rows above.
The nightly real-generation probe (real fal.ai spend) remains explicitly
NOT built — see this build's own report for the Board-vs-task-text
discrepancy on whether that spend has actually been approved.

2026-08-02, alongside tracker item `for-product-wizard-characters-step-is-si-
paxp07` (founder repro): fixed `wizard.html`'s Subject step (Step 1) from a
single-active-subject model to real independent multi-select toggles
(`staged.subjectCharacterIds` + `subjectOtherKind` composing together), and
extended `js/wizard-chips.js`'s `assembleCaption`/`buildDeterministicStory`
with a new, backward-compatible `subjects`-array input to carry multiple
selections all the way into the assembled generation prompt — see the
Onboarding funnel row above for the full writeup and its test coverage.
Also fixed a real, pre-existing bug found while touching this exact code
path: the in-place-login (already-registered-email) branch's post-flush
`draft.characterIds` id remap used to run BEFORE the `draftPatch` setDraft
call that immediately overwrote it back to the stale pre-flush local id(s)
— reordered to match the normal-signup branch's own (already-correct)
ordering.

2026-08-02, SECURITY FIX (round-2 independent review of tracker item
`for-product-build-passwordless-signup-fo-at2fko`): closed a real account-
takeover bug — a bare POST of an already-registered email to
`register-account-passwordless.js` used to mint a fully usable session with
zero ownership proof. Fixed via a new `login-with-email-code.js` endpoint
(the real access-control gate — a genuine login, mailed-code-as-credential)
and `verify-email-link.js` now also minting a `lib/session-transfer-
token.js` token on success. See the new SECURITY FIX row above for the full
writeup and its test coverage.

2026-08-02, alongside tracker item `for-product-build-passwordless-signup-fo-
at2fko` (founder-decided passwordless-signup hybrid): added the passwordless
signup path (`register-account-passwordless.js`, mock-only —
`SIGNUP_PASSWORDLESS_LIVE` is `false`), deferred email verification
(`verify-email-code.js`, `resend-verification-code.js`,
`verify-email-link.js`'s implicit-verification-via-link-click,
`js/email-verify-sheet.js`'s "next visit" prompt), the gate list
(`create-checkout-session-dodo.js`'s E10, `publish-dream.js`'s E8 — see
`lib/account-store.js`'s GATE LIST header comment), and Resend hard-bounce
handling (`resend-bounce-webhook.js`) — see the Auth & accounts rows above.

2026-08-02, alongside tracker item `for-product-urgent-founder-posthog-recor-
75ob70`: fixed `js/sheet-dismiss.js`'s tap-outside-to-close listener
self-dismissing a sheet within ~50ms of its own open (a rapid re-tap at the
trigger button's coordinates landing on the just-opened overlay backdrop,
before the `.sheet` panel had visibly slid into view) — a same-position
re-tap guard (not a flat time window, which regressed
`test/out-of-tokens-purchase-sheet-behavioral.test.js`'s several
genuinely-intentional immediate tap-outside-to-dismiss cases; see
`js/sheet-dismiss.js`'s own comment for why position, not time, is the
right signal) now ignores a tap-outside only when it lands within ~48px of
the most recent click that happened somewhere else on the page. Also added
the Cross-cutting/infrastructure row above for this shared module (which
had no dedicated registry row despite already having its own
`test/sheet-dismiss-behavioral.test.js` file).

2026-08-02, alongside tracker item `for-product-dream-ready-email-real-first-
qr9fbj`: added the retention email's real first-frame thumbnail (new
`upload-dream-thumbnail.js` endpoint, `lib/first-dream-email-sender.js`'s
`buildHtml` real-`<img>`/flat-banner branch and `absoluteImageUrl` helper,
`js/store.js`'s `DreamStore.saveThumbnailBestEffort`, result.html's
client-side canvas capture IIFE) — see the Retention row above.

2026-08-02, alongside tracker item `for-product-build-whatsapp-morning-captu-
skez3n`: added the Morning Capture Ritual's WhatsApp channel (`whatsappNumber`
account field, `save-whatsapp-number.js`'s profile.html Settings opt-in,
`send-whatsapp-morning-capture.js`'s daily scheduled sender + per-day dedup
via `lib/push-dedup-store.js`) — see the Retention row above.

2026-08-02, alongside tracker item
`for-product-owner-media-library-page-fou-1fwxaw`: added own-storage media
re-hosting (video-status.js/image-status.js/dream-webhook.js completion
paths, `lib/media-rehost.js`, `lib/media-status.js`, `image-file.mjs`), the
owner-only backfill sweep (`admin-backfill-media-rehost.js`), and the owner
media library page + its data endpoint (`media-library-x7q4.html`,
`admin-media-library-data.js`) — see the rows above.

2026-08-01, alongside tracker item
`for-product-build-founder-directed-produ-miyfp4` (founder-directed,
HIGH priority) — built `test/prod-smoke/` and this file from scratch.
Whoever next touches a feature area above should update that row's
"Covered by"/"Gaps" columns in the SAME merge (see `AGENT_POLICY.md`'s
build/review definition-of-done).

2026-08-02, alongside tracker item
`for-product-add-a-prod-smoke-assertion-f-bqt2sh` (a direct follow-up to
`for-product-urgent-reopen-video-repair-p-cyp8np`'s module-load-crash
incident): added `test/prod-smoke/media-serving.test.js` — a real,
currently-deployed-origin check that `video-file.mjs`/`image-file.mjs`
actually serve stored media (200/302 + correct content-type) against a
known-good production video fixture, plus a clean-404 check. Image
coverage is opt-in via `MEDIA_SMOKE_IMAGE_KEY` and currently skips (no
verified real image key was available to hardcode) — see that Own-storage
media re-host row above and the test file's own header comment.

2026-08-03, alongside tracker item
`for-product-media-library-founder-08-03--6h7fmv` (founder's last open
note on the media library page: "also show the text they wrote"): added
the per-card `storyText` field to `admin-media-library-data.js`'s
`itemsForRecord` (falling back to `caption` for a pre-split dream, never
`promptText`) and its rendering in `media-library-x7q4.html`'s `cardHTML`,
reusing `css/styles.css`'s `.interp-text` class verbatim — see the Media
library row above.

2026-08-03, alongside tracker item
`for-product-build-whatsapp-morning-captu-skez3n` (the template-submission
+ sender-wiring follow-up round, per Manager's explicit GO): added
`admin-submit-whatsapp-template.js` (a deliberately-not-automatic one-off
tool that submits the founder-drafted morning-capture template to Meta's
Graph API's Message Templates endpoint, MARKETING category), and retrofitted
`send-whatsapp-morning-capture.js`'s `scanAndSend` with a PILOT-ONLY GATE
(`isPilotOnly`/`isSendEligible`, gated on the `WHATSAPP_MORNING_CAPTURE_GO_LIVE`
env var, defaulting closed) so the real sender cannot reach any real end user
until Manager/Ron explicitly flips it — reusing `lib/test-identity.js`'s
existing founder-alias allowlist rather than a new one. See the Morning
Capture Ritual row above.

2026-08-03/04, alongside tracker item
`for-product-email-redesign-unsubscribe-l-16ysmp` (founder ask: "make the
email content look better + add unsubscribe" — branch
`email-redesign-unsubscribe-16ysmp`): added the unsubscribe mechanism
(`unsubscribe-email.js`, `lib/unsubscribe-token.js`'s stateless per-email
HMAC token, `lib/email-suppression-store.js`) and wired it into the two
retention/marketing senders in scope (`lib/first-dream-email-sender.js`,
`dream-webhook.js`'s `sendReadyEmail`), plus rebuilt both templates on a
new shared night-aesthetic shell (`lib/email-layout.js`). Founder-approved
follow-up (08-04): the first-dream retention send now gates on the
captured first-frame thumbnail — send immediately once it's available,
or send anyway with the existing no-thumbnail template variant once 3
minutes have elapsed since the trigger (the no-thumbnail template is a
real fallback, not dead code); footer now carries the real CAN-SPAM
mailing address (10 Dgania, Herzliya, Israel), styled small/faint. See
the Retention row above for the full writeup and its test coverage.

2026-08-07, alongside the same tracker item's closing round (branch
`mood-music-candidates`): the mood-bed audition ran its full course in one
day — the 12 fal-ai/stable-audio candidate tracks were generated and
committed, the founder picked one winner per mood (peaceful-A, joyful-B,
dreamy-A, mysterious-B, tense-B, epic-B), the winners were promoted to
`assets/music-beds/moods/<mood>.wav`, `js/music-bed.js`'s `MOOD_FILES`
went live with all six (selection tier 1 now actually fires), and the six
losing candidates plus `bed-audition-x7q4.html` itself were deleted per
the item's own no-dead-assets instruction. The mood-keyed-beds row above
was rewritten to the live state: its coverage column now describes the
live-tier-1 + assets-integrity tests that replaced the audition-page
picking-flow tests (removed with the page), and its gaps column narrowed
to the one remaining REAL-MODE post-deploy phone check.
